openapi: 3.1.0
info:
  title: Synapse API
  version: 0.1.0
  description: >
    Security & pentest operations – software composition analysis core.
    Authorized security testing only: every scan is gated by an engagement's
    scope and authorization window, enforced server-side.

    Auth: human/control-plane endpoints except `/healthz` and `/readyz` require
    the operator bearer token (`Authorization: Bearer <SYNAPSE_API_TOKEN>`).
    Agent-plane `/api/v1/fleet/*` endpoints use the enrolled agent bearer credential
    (or the configured trusted mTLS-proxy certificate path) and pin protocol version 1.
    After operator authentication, every endpoint except the AUP endpoints also
    requires that the current Acceptable Use Policy has been accepted, else `403`.

    Response objects use snake_case. A few endpoints that are still serialized directly from a
    domain struct answer in PascalCase; each is documented with the field names it actually returns.

servers:
- url: http://localhost:8080
  description: Local API

security:
- bearerAuth: []

tags:
- name: users
  description: Operator identities, roles, and API-key lifecycle
- name: platform
  description: Deployment-wide configuration a client needs before it renders navigation
- name: health
- name: aup
- name: engagements
- name: findings
- name: sca
- name: export
- name: projects
  description: Long-lived code-quality Projects and immutable analysis read models
- name: rules
  description: Built-in code-quality and security rule catalog
- name: assets
  description: Business-level Asset Management and security posture
- name: dashboard
  description: Tenant-scoped security operations summaries
- name: vulnerability-intelligence
  description: Tenant-scoped vulnerability feeds, exposure correlation, risk transitions, and durable operations
- name: remediation-sla
  description: Risk-based remediation deadlines, immutable assessments, policy versions, and human-owned lifecycle governance
- name: fleet
  description: Agent-plane fleet telemetry transport

paths:
  /healthz:
    get:
      tags: [health]
      summary: Liveness probe
      security: []
      responses:
        "200":
          description: Service is up
          content:
            application/json:
              schema:
                type: object
                properties:
                  status: {type: string, example: ok}
                  service: {type: string, example: synapse-api}

  /readyz:
    get:
      tags: [health]
      summary: Dependency readiness probe
      description: Checks PostgreSQL, embedded migration state, and the object store when configured.
      security: []
      responses:
        "200":
          description: Every configured dependency is ready
          content:
            application/json:
              schema: {$ref: "#/components/schemas/Readiness"}
        "503":
          description: One or more configured dependencies failed or timed out
          content:
            application/json:
              schema: {$ref: "#/components/schemas/Readiness"}

  /api/v1/aup:
    get:
      tags: [aup]
      summary: Current Acceptable Use Policy + acceptance state
      responses:
        "200":
          description: The active AUP
          content:
            application/json:
              schema: {$ref: "#/components/schemas/AupStatus"}
        "401": {$ref: "#/components/responses/Unauthorized"}

  /api/v1/aup/accept:
    post:
      tags: [aup]
      summary: Accept the current AUP (recorded with actor + version + timestamp)
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required: [version]
              properties:
                version: {type: string, example: "1.0"}
      responses:
        "200": {description: Accepted}
        "400": {$ref: "#/components/responses/BadRequest"}
        "401": {$ref: "#/components/responses/Unauthorized"}

  /api/v1/capabilities:
    get:
      tags: [platform]
      summary: List optional subsystems and whether this deployment enables them
      description: >
        Every optional subsystem registers its routes only when its `SYNAPSE_*` switch is on, so a
        disabled subsystem and a broken one both answer 404. This endpoint reports, per subsystem, a
        stable key, a human name, whether it is enabled, and the environment variable that controls
        it, so a client can render "disabled" rather than a dead link. It returns configuration
        booleans and variable NAMES only, never a configured value, and is gated at the view floor.
      responses:
        "200":
          description: The deployment's capability catalog
          content:
            application/json:
              schema:
                type: object
                required: [capabilities]
                properties:
                  capabilities:
                    type: array
                    items: {$ref: "#/components/schemas/Capability"}
        "401": {$ref: "#/components/responses/Unauthorized"}
        "403": {$ref: "#/components/responses/Forbidden"}

  /api/v1/engagements:
    get:
      tags: [engagements]
      summary: List engagements
      responses:
        "200":
          description: Engagements
          content:
            application/json:
              schema:
                type: array
                items: {$ref: "#/components/schemas/Engagement"}
        "401": {$ref: "#/components/responses/Unauthorized"}
        "403": {$ref: "#/components/responses/Forbidden"}
    post:
      tags: [engagements]
      summary: Create an engagement
      requestBody:
        required: true
        content:
          application/json:
            schema: {$ref: "#/components/schemas/CreateEngagementRequest"}
          multipart/form-data:
            schema:
              type: object
              required: [metadata, source]
              properties:
                metadata:
                  type: string
                  description: JSON-encoded CreateEngagementRequest.
                source:
                  type: string
                  format: binary
                  description: Source package in .zip, .tar, .tar.gz, or .tgz format; maximum compressed size is 512 MiB.
      responses:
        "201":
          description: Created
          content:
            application/json:
              schema: {$ref: "#/components/schemas/Engagement"}
        "400": {$ref: "#/components/responses/BadRequest"}
        "401": {$ref: "#/components/responses/Unauthorized"}
        "403": {$ref: "#/components/responses/Forbidden"}
        "409": {description: Engagement already has a different uploaded source package}
        "413": {description: Uploaded source package exceeds 512 MiB}

  /api/v1/engagements/{id}:
    parameters:
    - $ref: "#/components/parameters/EngagementId"
    patch:
      tags: [engagements]
      summary: Apply a lifecycle transition to an engagement
      description: >
        Requires the `operate` capability. The legal transitions are draft to active, active to
        completed, and draft, active, or completed to archived; archived is terminal. An illegal
        transition is rejected with `400`. `PUT /api/v1/engagements/{id}/status` applies the same
        change on the status sub-resource.
      requestBody:
        required: true
        content:
          application/json:
            schema: {$ref: "#/components/schemas/EngagementTransitionRequest"}
      responses:
        "200":
          description: The transitioned engagement
          content:
            application/json:
              schema: {$ref: "#/components/schemas/Engagement"}
        "400": {$ref: "#/components/responses/BadRequest"}
        "401": {$ref: "#/components/responses/Unauthorized"}
        "403": {$ref: "#/components/responses/Forbidden"}
        "404": {$ref: "#/components/responses/NotFound"}

  /api/v1/engagements/{id}/status:
    parameters:
    - $ref: "#/components/parameters/EngagementId"
    put:
      tags: [engagements]
      summary: Set an engagement's lifecycle status
      description: >
        Requires the `operate` capability. Identical in effect to
        `PATCH /api/v1/engagements/{id}`: the same legal transitions, the same authorization gate,
        and the same audit record.
      requestBody:
        required: true
        content:
          application/json:
            schema: {$ref: "#/components/schemas/EngagementTransitionRequest"}
      responses:
        "200":
          description: The transitioned engagement
          content:
            application/json:
              schema: {$ref: "#/components/schemas/Engagement"}
        "400": {$ref: "#/components/responses/BadRequest"}
        "401": {$ref: "#/components/responses/Unauthorized"}
        "403": {$ref: "#/components/responses/Forbidden"}
        "404": {$ref: "#/components/responses/NotFound"}

  /api/v1/engagements/{id}/offensive-roe:
    parameters:
    - $ref: "#/components/parameters/EngagementId"
    put:
      tags: [engagements]
      summary: Set an engagement's offensive rules of engagement
      description: >
        Requires the `operate` capability. Records the customer and emergency contacts, the risk
        ceiling (`low`/`medium`/`high`/`prohibited`, or empty to leave unset), and that the
        out-of-scope list was reviewed. The offensive governance policy refuses adversary emulation
        and exploitation chains until these rules of engagement are complete. This replaces the whole
        rules-of-engagement set: send all four fields, since an omitted field is written as its empty
        value.
      requestBody:
        required: true
        content:
          application/json:
            schema: {$ref: "#/components/schemas/SetOffensiveRoERequest"}
      responses:
        "200":
          description: The updated engagement
          content:
            application/json:
              schema: {$ref: "#/components/schemas/Engagement"}
        "400": {$ref: "#/components/responses/BadRequest"}
        "401": {$ref: "#/components/responses/Unauthorized"}
        "403": {$ref: "#/components/responses/Forbidden"}
        "404": {$ref: "#/components/responses/NotFound"}

  /api/v1/engagements/{id}/exploitation/rehearsals:
    parameters:
    - $ref: "#/components/parameters/EngagementId"
    post:
      tags: [engagements, redteam]
      summary: Rehearse a governed exploitation chain (no-host simulation)
      description: >
        Requires the `operate` capability. Rehearses an operator-declared exploitation chain: each step is
        admitted through the engagement's offensive rules of engagement (the same #418 governance the rest
        of the offensive pillar uses), sealed as per-step evidence, verified by a distinct system verifier,
        and its cleanup obligation discharged. The rehearsal executes with a no-host SIMULATION executor, so
        it proves the chain is policy-admissible and its custody chain is sound WITHOUT touching a host; it
        is not a claim of real compromise. The running chain is registered with the offensive kill switch so
        `POST /api/v1/redteam/halt` can stop it mid-run. A real host executor and an independent verifier are
        a deliberate, review-gated extension point. Available when the fleet transport is enabled (the
        offensive kill switch, which this route registers its chain with, is wired with the fleet stores).
      requestBody:
        required: true
        content:
          application/json:
            schema: {$ref: "#/components/schemas/RehearseChainRequest"}
      responses:
        "200":
          description: The rehearsed chain's terminal state
          content:
            application/json:
              schema: {$ref: "#/components/schemas/ChainRehearsalResult"}
        "400": {$ref: "#/components/responses/BadRequest"}
        "401": {$ref: "#/components/responses/Unauthorized"}
        "403": {$ref: "#/components/responses/Forbidden"}
        "404": {$ref: "#/components/responses/NotFound"}

  /api/v1/engagements/{id}/emulation:
    parameters:
    - $ref: "#/components/parameters/EngagementId"
    post:
      tags: [engagements, blueteam]
      summary: Run adversary emulation and compute purple-team coverage
      description: >
        Requires the `operate` capability. Runs the adversary-emulation technique catalogue against the
        target asset, admitting each technique through the engagement's offensive rules of engagement
        (the same #418 governance the exploitation chains use), then joins the run's expected detections
        against the detections that actually fired and persists the purple-team coverage the
        `GET .../purple-coverage` route serves. Emulation executes through a no-host simulation executor;
        a technique the rules of engagement do not permit is recorded as not executed (verdict unknown), never run.
      requestBody:
        required: true
        content:
          application/json:
            schema: {$ref: "#/components/schemas/RunEmulationRequest"}
      responses:
        "200":
          description: The emulation run and the purple coverage it produced
          content:
            application/json:
              schema: {$ref: "#/components/schemas/EmulationRunResult"}
        "400": {$ref: "#/components/responses/BadRequest"}
        "401": {$ref: "#/components/responses/Unauthorized"}
        "403": {$ref: "#/components/responses/Forbidden"}
        "404": {$ref: "#/components/responses/NotFound"}

  /api/v1/engagements/{id}/source:
    parameters:
    - $ref: "#/components/parameters/EngagementId"
    get:
      tags: [engagements, sca]
      summary: Get the immutable uploaded source package metadata for an engagement
      responses:
        "200":
          description: Uploaded source package metadata
          content:
            application/json:
              schema: {$ref: "#/components/schemas/UploadedSourcePackage"}
        "401": {$ref: "#/components/responses/Unauthorized"}
        "403": {$ref: "#/components/responses/Forbidden"}
        "404": {$ref: "#/components/responses/NotFound"}

  /api/v1/engagements/{id}/cspm/runs:
    parameters:
    - $ref: "#/components/parameters/EngagementId"
    post:
      tags: [engagements, findings, assets]
      summary: Run a bounded read-only live cloud posture scan
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required: [targets]
              properties:
                targets:
                  type: array
                  minItems: 1
                  maxItems: 32
                  items:
                    type: object
                    required: [provider, root, credential_ref]
                    properties:
                      provider: {type: string, enum: [aws, azure, gcp]}
                      root: {type: string, minLength: 1}
                      credential_ref: {type: string, minLength: 1, description: Vault reference; never credential material}
      responses:
        "202":
          description: Durable CSPM run accepted for isolated worker execution
          content:
            application/json:
              schema: {$ref: "#/components/schemas/CSPMRun"}
        "400": {$ref: "#/components/responses/BadRequest"}
        "401": {$ref: "#/components/responses/Unauthorized"}
        "403": {$ref: "#/components/responses/Forbidden"}

  /api/v1/engagements/{id}/cspm/runs/{rid}:
    parameters:
    - $ref: "#/components/parameters/EngagementId"
    - name: rid
      in: path
      required: true
      schema: {type: string}
    get:
      tags: [engagements, findings, assets]
      summary: Get a durable CSPM run
      responses:
        "200": {description: CSPM run status, content: {application/json: {schema: {$ref: "#/components/schemas/CSPMRun"}}}}
        "401": {$ref: "#/components/responses/Unauthorized"}
        "403": {$ref: "#/components/responses/Forbidden"}
        "404": {$ref: "#/components/responses/NotFound"}

  /api/v1/engagements/{id}/dast/runs/{rid}:
    parameters:
    - $ref: "#/components/parameters/EngagementId"
    - name: rid
      in: path
      required: true
      schema: {type: string}
    get:
      tags: [engagements, findings]
      operationId: getDASTRun
      summary: Get a durable DAST verification run
      description: >
        Returns the status of a governed DAST verification probe executed as a worker job (#823). The run
        route enqueues the probe and answers 202 with a queued run; this route polls it to completion. The
        record is secret-free: it carries the verdict class, the observed HTTP status, and the
        sealed-evidence id, never the probe body or any request or response content.
      responses:
        "200":
          description: DAST verification run status
          content:
            application/json:
              schema: {$ref: "#/components/schemas/DASTRun"}
        "401": {$ref: "#/components/responses/Unauthorized"}
        "403": {$ref: "#/components/responses/Forbidden"}
        "404": {$ref: "#/components/responses/NotFound"}

  /api/v1/engagements/{id}/detection-provenance:
    parameters:
    - $ref: "#/components/parameters/EngagementId"
    get:
      tags: [engagements, findings]
      operationId: listDetectionProvenance
      summary: List durable detection-provenance states for an engagement
      description: Returns tenant-scoped custody and health projections independently of the ordinary detection projection.
      responses:
        "200":
          description: Current detection-provenance states
          content:
            application/json:
              schema: {$ref: "#/components/schemas/DetectionProvenanceList"}
        "401": {$ref: "#/components/responses/Unauthorized"}
        "403": {$ref: "#/components/responses/Forbidden"}
        "404": {$ref: "#/components/responses/NotFound"}
        "500": {$ref: "#/components/responses/InternalServerError"}

  /api/v1/engagements/{id}/detections/{did}/provenance:
    parameters:
    - $ref: "#/components/parameters/EngagementId"
    - $ref: "#/components/parameters/DetectionId"
    get:
      tags: [engagements, findings]
      operationId: listDetectionProvenanceTransitions
      summary: List append-only provenance transitions for one detection
      description: Returns the ordered custody history, including causal telemetry references and the sealed evidence identity when available.
      responses:
        "200":
          description: Oldest-to-newest detection-provenance history
          content:
            application/json:
              schema: {$ref: "#/components/schemas/DetectionProvenanceTransitionList"}
        "401": {$ref: "#/components/responses/Unauthorized"}
        "403": {$ref: "#/components/responses/Forbidden"}
        "404": {$ref: "#/components/responses/NotFound"}
        "500": {$ref: "#/components/responses/InternalServerError"}

  /api/v1/engagements/{id}/findings:
    parameters:
    - $ref: "#/components/parameters/EngagementId"
    get:
      tags: [findings]
      summary: List an engagement's findings (highest risk first)
      description: Ordered by CISA KEV, then EPSS x CVSS, then severity – never raw
        CVSS.
      responses:
        "200":
          description: Findings
          content:
            application/json:
              schema:
                type: array
                items: {$ref: "#/components/schemas/Finding"}
        "401": {$ref: "#/components/responses/Unauthorized"}
        "403": {$ref: "#/components/responses/Forbidden"}
    post:
      tags: [findings]
      summary: Create a manual finding
      requestBody:
        required: true
        content:
          application/json:
            schema: {$ref: "#/components/schemas/CreateManualFindingRequest"}
      responses:
        "201":
          description: Created finding
          content:
            application/json:
              schema: {$ref: "#/components/schemas/Finding"}
        "400": {$ref: "#/components/responses/BadRequest"}
        "401": {$ref: "#/components/responses/Unauthorized"}
        "403": {$ref: "#/components/responses/Forbidden"}
        "404": {$ref: "#/components/responses/NotFound"}

  /api/v1/engagements/{id}/findings/{fid}:
    parameters:
    - $ref: "#/components/parameters/EngagementId"
    - name: fid
      in: path
      required: true
      schema: {type: string}
    patch:
      tags: [findings]
      summary: Change a finding's triage status (audited)
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required: [status]
              properties:
                status:
                  type: string
                  enum: [open, triage, confirmed, false_positive, remediated]
                note: {type: string}
      responses:
        "200":
          description: The updated finding
          content:
            application/json:
              schema: {$ref: "#/components/schemas/Finding"}
        "400": {$ref: "#/components/responses/BadRequest"}
        "401": {$ref: "#/components/responses/Unauthorized"}
        "403": {$ref: "#/components/responses/Forbidden"}
        "404": {$ref: "#/components/responses/NotFound"}

  /api/v1/engagements/{id}/sarif:
    parameters:
    - $ref: "#/components/parameters/EngagementId"
    post:
      tags: [findings]
      summary: Import a third-party SARIF report
      parameters:
      - name: asset_id
        in: query
        schema: {type: string}
        description: Authoritative asset ID; required and validated when fleet asset attribution is configured.
      requestBody:
        required: true
        content:
          application/sarif+json:
            schema: {type: object}
      responses:
        "200": {description: Ingest summary}
        "400": {$ref: "#/components/responses/BadRequest"}
        "401": {$ref: "#/components/responses/Unauthorized"}
        "403": {$ref: "#/components/responses/Forbidden"}
        "404": {$ref: "#/components/responses/NotFound"}

  /api/v1/engagements/{id}/export/sarif:
    parameters:
    - $ref: "#/components/parameters/EngagementId"
    get:
      tags: [export]
      summary: Export findings as SARIF 2.1.0
      responses:
        "200":
          description: SARIF document
          content:
            application/sarif+json:
              schema: {type: object}
        "401": {$ref: "#/components/responses/Unauthorized"}
        "403": {$ref: "#/components/responses/Forbidden"}

  /api/v1/engagements/{id}/export/openvex:
    parameters:
    - $ref: "#/components/parameters/EngagementId"
    get:
      tags: [export]
      summary: Export findings as OpenVEX 0.2
      responses:
        "200":
          description: OpenVEX document
          content:
            application/json:
              schema: {type: object}
        "401": {$ref: "#/components/responses/Unauthorized"}
        "403": {$ref: "#/components/responses/Forbidden"}

  /api/v1/engagements/{id}/report.pdf:
    parameters:
    - $ref: "#/components/parameters/EngagementId"
    get:
      tags: [export]
      summary: Deterministic PDF report, SHA-256 sealed
      description: Templated from stored data (no LLM). The body's SHA-256 is returned
        in `X-Report-SHA256`.
      responses:
        "200":
          description: PDF report
          headers:
            X-Report-SHA256:
              description: Hex SHA-256 of the PDF bytes (integrity seal)
              schema: {type: string}
          content:
            application/pdf:
              schema: {type: string, format: binary}
        "401": {$ref: "#/components/responses/Unauthorized"}
        "403": {$ref: "#/components/responses/Forbidden"}

  /api/v1/sca/scans:
    post:
      tags: [sca]
      summary: Run an SCA scan against an in-scope target
      description: >
        Scope + authorization window are enforced before any tool runs; an
        out-of-scope or expired target is rejected with 403.
      requestBody:
        required: true
        content:
          application/json:
            schema: {$ref: "#/components/schemas/ScanRequest"}
      responses:
        "200":
          description: Scan result (SBOM, vulnerabilities, licenses, findings, provenance)
          content:
            application/json:
              schema: {$ref: "#/components/schemas/ScanResult"}
        "400": {$ref: "#/components/responses/BadRequest"}
        "401": {$ref: "#/components/responses/Unauthorized"}
        "403": {$ref: "#/components/responses/Forbidden"}

  /api/v1/sca/scans/{id}:
    get:
      tags: [sca]
      summary: Read one asynchronous scan job by its own id
      description: >
        Reads a specific scan job rather than the engagement's latest, so a
        concurrent scan on the same engagement cannot mask this job's status. The
        job is resolved first, then its engagement is checked against the caller's
        tenant; an unknown or cross-tenant job is 404 and reveals nothing.
      parameters:
      - name: id
        in: path
        required: true
        schema: {type: string}
        description: Scan job id returned when the scan was accepted
      responses:
        "200":
          description: Scan job status, stage, and progress
          content:
            application/json:
              schema: {$ref: "#/components/schemas/ScanJob"}
        "400": {$ref: "#/components/responses/BadRequest"}
        "401": {$ref: "#/components/responses/Unauthorized"}
        "403": {$ref: "#/components/responses/Forbidden"}
        "404": {$ref: "#/components/responses/NotFound"}

  /api/v1/attack-paths:
    get:
      tags: [findings]
      summary: List bounded, tenant-scoped derived attack paths
      description: >
        Non-empty target, entrypoint, and finding filters compose with AND. Unknown
        or cross-tenant filter IDs return an empty result to avoid disclosure.
      parameters:
      - name: target
        in: query
        schema: {type: string}
        description: Asset ID that a returned path must contain
      - name: entrypoint
        in: query
        schema: {type: string}
        description: Starting exposure asset ID
      - name: finding
        in: query
        schema: {type: string}
        description: Finding ID at the path endpoint; without finding_kind, matches either target kind
      - name: finding_kind
        in: query
        schema: {type: string, enum: [canonical, imported]}
        description: Optional endpoint kind; requires finding and selects its typed target
      responses:
        "200":
          description: Deterministically ordered paths and the applied traversal bounds
          content:
            application/json:
              schema: {$ref: "#/components/schemas/AttackPathResult"}
        "400": {$ref: "#/components/responses/BadRequest"}
        "401": {$ref: "#/components/responses/Unauthorized"}
        "403": {$ref: "#/components/responses/Forbidden"}
        "500": {$ref: "#/components/responses/InternalServerError"}

  /api/v1/projects/{key}:
    parameters:
    - $ref: '#/components/parameters/ProjectKey'
    delete:
      tags: [projects]
      summary: Delete a Project and its retained source artifacts
      operationId: deleteProject
      responses:
        "204": {description: Project deleted}
        "401": {$ref: "#/components/responses/Unauthorized"}
        "403": {$ref: "#/components/responses/Forbidden"}
        "404": {$ref: "#/components/responses/NotFound"}
        "500": {$ref: "#/components/responses/InternalServerError"}

  /api/v1/projects/{key}/analyses/import:
    parameters:
    - $ref: '#/components/parameters/ProjectKey'
    post:
      tags: [projects]
      summary: Record a pipeline-produced scan result as the Project's next analysis
      operationId: importProjectAnalysis
      description: >
        The CI path into the console. `synapse-cli scan --server URL --project KEY` runs the scan on
        the pipeline's own checkout and posts the result here. The server records it through the
        same recorder as a server-side analysis, so it takes its place in the project history, moves
        the trend, evaluates the project's managed quality gate, and carries ratings, issues and
        hotspots. The gate in the payload is ignored: the server's managed gate is the policy, and a
        client that could ship its own gate could ship a passing one. The analysis is marked
        `origin: ci` and carries the pipeline's own account of the run, which the server does not
        verify. Requires the operate permission. Body ceiling 64 MiB.
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required: [result]
              additionalProperties: false
              properties:
                ci:
                  type: object
                  description: The pipeline's own account of the run; every field optional and unverified.
                  additionalProperties: false
                  properties:
                    provider: {type: string, maxLength: 512, description: 'e.g. github-actions, gitlab-ci, jenkins'}
                    run_url: {type: string, format: uri, maxLength: 512}
                    run_id: {type: string, maxLength: 512}
                    branch: {type: string, maxLength: 512}
                    actor: {type: string, maxLength: 512}
                result:
                  type: object
                  description: A synapse-cli scan result, exactly as `synapse-cli scan --json` prints it.
                  additionalProperties: true
      responses:
        "201":
          description: The recorded analysis, in the same shape as the analysis history returns it
          content:
            application/json:
              schema:
                type: object
                required: [id, created_at, origin, gate, gate_info, issues, new_code, measures, rating]
                properties:
                  id: {type: string}
                  created_at: {type: string, format: date-time}
                  origin: {type: string, enum: [server, ci]}
                  ci:
                    type: object
                    properties:
                      provider: {type: string}
                      run_url: {type: string}
                      run_id: {type: string}
                      branch: {type: string}
                      actor: {type: string}
                  source_ref: {type: string}
                  source_commit: {type: string}
                  gate:
                    type: object
                    properties:
                      passed: {type: boolean}
                      results: {type: array, items: {type: object}}
                  gate_info: {type: object}
                  issues: {type: object}
                  new_code: {type: object}
                  delta: {type: object, nullable: true}
                  measures: {type: object, additionalProperties: {type: number}}
                  coverage: {type: object, nullable: true}
                  duplication: {type: object}
                  rating: {type: object}
        "400": {$ref: "#/components/responses/BadRequest"}
        "401": {$ref: "#/components/responses/Unauthorized"}
        "403": {$ref: "#/components/responses/Forbidden"}
        "404": {$ref: "#/components/responses/NotFound"}
        "413": {description: The scan result exceeds the import size limit}
        "500": {$ref: "#/components/responses/InternalServerError"}
  /api/v1/projects/{key}/analyses/{id}/source:
    parameters:
    - $ref: '#/components/parameters/ProjectKey'
    - $ref: '#/components/parameters/AnalysisId'
    post:
      tags: [projects]
      summary: Publish an immutable source snapshot for one server-owned Project analysis
      operationId: publishProjectAnalysisSource
      description: >
        Authenticated CI contribution path for Code Quality source. The server derives tenant
        and actor from the bearer token, re-resolves the analysis under that tenant/project,
        applies configured source-capture policy and limits, and creates the snapshot once.
      parameters:
      - name: X-Synapse-Tool-Version
        in: header
        required: true
        schema: {type: string, minLength: 1}
      requestBody:
        required: true
        content:
          application/x-tar:
            schema: {type: string, format: binary}
      responses:
        "201":
          description: Source snapshot published and attached to the immutable analysis
          content:
            application/json:
              schema:
                type: object
                required: [files, writer, digest]
                properties:
                  files:
                    type: array
                    items:
                      type: object
                      required: [path, bytes, lines, generated, available]
                      properties:
                        path: {type: string, minLength: 1}
                        digest: {type: string}
                        bytes: {type: integer, format: int64, minimum: 0}
                        lines: {type: integer, minimum: 0}
                        generated: {type: boolean}
                        available: {type: boolean}
                        reason: {type: string}
                  truncated: {type: boolean}
                  writer:
                    type: object
                    required: [actor, tool_version, published_at]
                    properties:
                      actor: {type: string, minLength: 1}
                      tool_version: {type: string, minLength: 1}
                      published_at: {type: string, format: date-time}
                  digest: {type: string, pattern: '^sha256:[0-9a-f]{64}$'}
        "400": {$ref: "#/components/responses/BadRequest"}
        "401": {$ref: "#/components/responses/Unauthorized"}
        "403": {$ref: "#/components/responses/Forbidden"}
        "404": {$ref: "#/components/responses/NotFound"}
        "409": {description: Analysis already has source or source artifact state conflicts}
        "413": {description: Source archive exceeds the transport upload limit}
        "415": {description: Content-Type is not application/x-tar}
        "500": {$ref: "#/components/responses/InternalServerError"}
        "503": {description: Source artifact storage is temporarily unavailable}

  /api/v1/projects/{key}/analyses/{id}/code/files:
    parameters:
    - $ref: '#/components/parameters/ProjectKey'
    - $ref: '#/components/parameters/AnalysisId'
    get:
      tags: [projects]
      summary: List immutable source files for one Project analysis
      operationId: listProjectCodeFiles
      parameters:
      - name: include_generated
        in: query
        schema: {type: boolean, default: false}
      - name: changed
        in: query
        schema: {type: boolean}
      - name: has_findings
        in: query
        schema: {type: boolean}
      - name: prefix
        in: query
        schema: {type: string, maxLength: 256}
      - name: status
        in: query
        schema: {type: string, enum: [unchanged, added, modified, deleted, renamed, copied, mode_only]}
      responses:
        "200":
          description: Immutable analysis source inventory and capabilities
          headers:
            ETag:
              description: Immutable file-index entity tag
              schema: {type: string}
          content:
            application/json:
              schema: {$ref: "#/components/schemas/ProjectCodeFilesResponse"}
        "304": {description: File index is unchanged}
        "400": {$ref: "#/components/responses/BadRequest"}
        "401": {$ref: "#/components/responses/Unauthorized"}
        "403": {$ref: "#/components/responses/Forbidden"}
        "404": {$ref: "#/components/responses/NotFound"}
        "500": {$ref: "#/components/responses/InternalServerError"}

  /api/v1/projects/{key}/analyses/{id}/code/diff:
    parameters:
    - $ref: '#/components/parameters/ProjectKey'
    - $ref: '#/components/parameters/AnalysisId'
    get:
      tags: [projects]
      summary: Read a persisted immutable Git diff for one Project analysis
      operationId: getProjectCodeDiff
      parameters:
      - name: path
        in: query
        required: true
        schema: {type: string, minLength: 1}
      - name: view
        in: query
        schema: {type: string, enum: [unified, split], default: unified}
      - name: context
        in: query
        schema: {type: integer, minimum: 0, maximum: 100}
      responses:
        "200":
          description: Persisted immutable diff representation
          headers:
            ETag:
              description: Immutable diff entity tag
              schema: {type: string}
          content:
            application/json:
              schema: {$ref: "#/components/schemas/ProjectCodeDiffResponse"}
        "304": {description: Diff is unchanged}
        "400": {$ref: "#/components/responses/BadRequest"}
        "401": {$ref: "#/components/responses/Unauthorized"}
        "403": {$ref: "#/components/responses/Forbidden"}
        "404": {$ref: "#/components/responses/NotFound"}
        "409": {description: Source artifact integrity mismatch}
        "410": {description: Comparison data was not retained or is unavailable}
        "413": {description: Diff content exceeds retained limits}
        "415": {description: Binary or unsupported diff content}
        "503": {description: Source artifact storage is temporarily unavailable}
        "500": {$ref: "#/components/responses/InternalServerError"}

  /api/v1/projects/{key}/analyses/{id}/code/file:
    parameters:
    - $ref: '#/components/parameters/ProjectKey'
    - $ref: '#/components/parameters/AnalysisId'
    get:
      tags: [projects]
      summary: Read a bounded immutable source window for one Project analysis
      operationId: getProjectCodeFile
      parameters:
      - name: path
        in: query
        required: true
        schema: {type: string, minLength: 1}
      - name: from_line
        in: query
        schema: {type: integer, minimum: 1, default: 1}
      - name: to_line
        in: query
        schema: {type: integer, minimum: 1, maximum: 1000}
      responses:
        "200":
          description: Immutable source window
          headers:
            ETag:
              description: Immutable source-window entity tag
              schema: {type: string}
          content:
            application/json:
              schema: {$ref: "#/components/schemas/ProjectCodeFileResponse"}
        "304": {description: Source window is unchanged}
        "400": {$ref: "#/components/responses/BadRequest"}
        "401": {$ref: "#/components/responses/Unauthorized"}
        "403": {$ref: "#/components/responses/Forbidden"}
        "404": {$ref: "#/components/responses/NotFound"}
        "409": {description: Source artifact integrity mismatch}
        "410": {description: Source was not retained for this analysis}
        "413": {description: Source content exceeds retained limits}
        "415": {description: Binary or unsupported source content}
        "503": {description: Source artifact storage is temporarily unavailable}
        "500": {$ref: "#/components/responses/InternalServerError"}

  /api/v1/projects/{key}/overview:
    parameters:
    - name: key
      in: path
      required: true
      schema: {type: string}
      example: payments-api
    get:
      tags: [projects]
      summary: Get the latest Project quality overview
      operationId: getProjectOverview
      description: >
        Returns the latest immutable Project analysis overview without loading the
        full scan-result blob. Existing Projects without an analysis return
        `state=not_analyzed` with explicit unavailable metric values. The optional
        `branch` query parameter restricts the overview to the newest analysis on
        that branch; omitting it reflects the latest analysis across all branches.
      parameters:
      - name: branch
        in: query
        required: false
        description: Restrict the overview to the newest analysis on this branch. Omit for the latest overall.
        schema: {type: string}
        example: main
      responses:
        "200":
          description: Project overview
          content:
            application/json:
              schema: {$ref: "#/components/schemas/ProjectOverview"}
              examples:
                analyzed:
                  value:
                    state: analyzed
                    project: {key: payments-api, name: Payments API}
                    latest_analysis:
                      id: analysis-42
                      created_at: "2026-07-17T10:00:00Z"
                      source_ref: main
                      source_commit: abc123
                      new_code:
                        first_analysis: false
                        has_baseline: true
                        baseline_analysis_id: analysis-41
                    gate:
                      status: failed
                      key: synapse-way
                      name: Synapse way
                      source: managed
                      failed_conditions:
                      - {metric: new_high, operator: "<=", threshold: 0, actual: 2}
                    issue_summary:
                      new_code_total: {availability: available, value: 4, unavailable_reason: null}
                      accepted_overall_total: {availability: unavailable, value: null,
                        unavailable_reason: issue_lifecycle_not_available}
                    lenses:
                      overall:
                        security: {availability: available, grade: B, unavailable_reason: null}
                        reliability: {availability: available, grade: A, unavailable_reason: null}
                        maintainability: {availability: available, grade: C, unavailable_reason: null}
                        security_hotspots_reviewed: {availability: unavailable, value: null,
                          unavailable_reason: security_hotspots_not_available}
                        coverage: {availability: available, value: 72.349, unavailable_reason: null}
                        duplications: {availability: available, value: 4.25, unavailable_reason: null}
                      new_code:
                        security: {availability: available, grade: A, unavailable_reason: null}
                        reliability: {availability: available, grade: B, unavailable_reason: null}
                        maintainability: {availability: unavailable, grade: null,
                          unavailable_reason: changed_line_metrics_not_available}
                        security_hotspots_reviewed: {availability: unavailable, value: null,
                          unavailable_reason: security_hotspots_not_available}
                        coverage: {availability: unavailable, value: null, unavailable_reason: changed_line_metrics_not_available}
                        duplications: {availability: unavailable, value: null, unavailable_reason: changed_line_metrics_not_available}
                notAnalyzed:
                  value:
                    state: not_analyzed
                    project: {key: payments-api, name: Payments API}
                    latest_analysis:
                    gate:
                    issue_summary:
                      new_code_total: {availability: unavailable, value: null, unavailable_reason: no_analysis}
                      accepted_overall_total: {availability: unavailable, value: null,
                        unavailable_reason: no_analysis}
                    lenses:
                      overall:
                        security: {availability: unavailable, grade: null, unavailable_reason: no_analysis}
                        reliability: {availability: unavailable, grade: null, unavailable_reason: no_analysis}
                        maintainability: {availability: unavailable, grade: null,
                          unavailable_reason: no_analysis}
                        security_hotspots_reviewed: {availability: unavailable, value: null,
                          unavailable_reason: no_analysis}
                        coverage: {availability: unavailable, value: null, unavailable_reason: no_analysis}
                        duplications: {availability: unavailable, value: null, unavailable_reason: no_analysis}
                      new_code:
                        security: {availability: unavailable, grade: null, unavailable_reason: no_analysis}
                        reliability: {availability: unavailable, grade: null, unavailable_reason: no_analysis}
                        maintainability: {availability: unavailable, grade: null,
                          unavailable_reason: no_analysis}
                        security_hotspots_reviewed: {availability: unavailable, value: null,
                          unavailable_reason: no_analysis}
                        coverage: {availability: unavailable, value: null, unavailable_reason: no_analysis}
                        duplications: {availability: unavailable, value: null, unavailable_reason: no_analysis}
        "401": {$ref: "#/components/responses/Unauthorized"}
        "403": {$ref: "#/components/responses/Forbidden"}
        "404": {$ref: "#/components/responses/NotFound"}
        "500": {$ref: "#/components/responses/InternalServerError"}

  /api/v1/projects/{key}/branches:
    parameters:
    - name: key
      in: path
      required: true
      schema: {type: string}
      example: payments-api
    get:
      tags: [projects]
      summary: List the branches a Project has analyses on
      operationId: listProjectBranches
      description: >
        Returns the distinct, sorted set of branch values recorded across the
        Project's immutable analyses. A caller uses it to populate a branch filter
        for the overview and analysis-history routes. A legacy analysis that
        recorded no branch contributes an empty-string entry.
      responses:
        "200":
          description: Project branches
          content:
            application/json:
              schema: {$ref: "#/components/schemas/ProjectBranches"}
              example:
                branches: [feature/x, main]
        "401": {$ref: "#/components/responses/Unauthorized"}
        "403": {$ref: "#/components/responses/Forbidden"}
        "404": {$ref: "#/components/responses/NotFound"}
        "500": {$ref: "#/components/responses/InternalServerError"}

  /api/v1/projects/{key}/dependency-graph:
    parameters:
    - name: key
      in: path
      required: true
      schema: {type: string}
      example: payments-api
    get:
      tags: [projects]
      summary: Get the latest Project dependency graph
      operationId: getProjectDependencyGraph
      description: >
        Projects the latest immutable analysis SBOM into deterministic nodes,
        edges, roots, vulnerability matches, license policy risk, and stored
        reachability facts. The full scan-result blob is not returned.
      responses:
        "200":
          description: Bounded dependency graph projection
          headers:
            Cache-Control:
              schema: {type: string}
              description: Private no-cache policy for the tenant-scoped projection.
          content:
            application/json:
              schema: {$ref: "#/components/schemas/ProjectDependencyGraph"}
        "401": {$ref: "#/components/responses/Unauthorized"}
        "403": {$ref: "#/components/responses/Forbidden"}
        "404": {$ref: "#/components/responses/NotFound"}
        "400": {$ref: "#/components/responses/BadRequest"}
        "500": {$ref: "#/components/responses/InternalServerError"}

  /api/v1/projects/{key}/dependency-graph/export:
    parameters:
    - name: key
      in: path
      required: true
      schema: {type: string}
      example: payments-api
    get:
      tags: [projects]
      summary: Export a Project dependency subtree as CycloneDX
      operationId: exportProjectDependencySubtree
      description: >
        Exports the selected component and every dependency reachable below it
        from the latest immutable Project SBOM. Omitting root exports the full SBOM.
      parameters:
      - name: root
        in: query
        required: false
        description: Exact graph node ID (normally a package URL), limited to 2048 UTF-8 bytes.
        schema: {type: string}
      responses:
        "200":
          description: CycloneDX 1.6 JSON document
          headers:
            Content-Disposition:
              schema: {type: string}
              description: Safe attachment filename generated by the server.
            Cache-Control:
              schema: {type: string}
              description: Private no-store policy for the exported artifact.
          content:
            application/vnd.cyclonedx+json:
              schema:
                type: object
                description: CycloneDX 1.6 BOM serialized from stored Project SBOM data.
        "400": {$ref: "#/components/responses/BadRequest"}
        "401": {$ref: "#/components/responses/Unauthorized"}
        "403": {$ref: "#/components/responses/Forbidden"}
        "404": {$ref: "#/components/responses/NotFound"}
        "500": {$ref: "#/components/responses/InternalServerError"}

  /api/v1/projects/{key}/hotspots:
    parameters:
    - name: key
      in: path
      required: true
      schema: {type: string}
    get:
      tags: [projects]
      summary: List Project Security Hotspot projections
      operationId: listProjectHotspots
      description: >
        Returns catalog-classified Project Security Hotspots without loading a full
        scan-result blob.
      parameters:
      - name: status
        in: query
        schema: {$ref: "#/components/schemas/ProjectHotspotStatus"}
      - name: rule
        in: query
        schema: {type: string, maxLength: 256}
      - name: severity
        in: query
        schema: {$ref: "#/components/schemas/ProjectHotspotSeverity"}
      - name: search
        in: query
        schema: {type: string, maxLength: 256}
      - name: limit
        in: query
        schema: {type: integer, minimum: 1, maximum: 100, default: 25}
      - name: before_last_seen_at
        in: query
        schema: {type: string, format: date-time}
      - name: before_id
        in: query
        schema: {type: string}
      - $ref: '#/components/parameters/LensParam'
      responses:
        "200":
          description: Project hotspot page
          content:
            application/json:
              schema: {$ref: "#/components/schemas/ProjectHotspotPage"}
        "400": {$ref: "#/components/responses/BadRequest"}
        "401": {$ref: "#/components/responses/Unauthorized"}
        "403": {$ref: "#/components/responses/Forbidden"}
        "404": {$ref: "#/components/responses/NotFound"}
        "500": {$ref: "#/components/responses/InternalServerError"}

  /api/v1/projects/{key}/hotspots/{id}:
    parameters:
    - name: key
      in: path
      required: true
      schema: {type: string}
    - name: id
      in: path
      required: true
      schema: {type: string}
    get:
      tags: [projects]
      summary: Get one Project Security Hotspot projection
      operationId: getProjectHotspot
      responses:
        "200":
          description: Project hotspot
          content:
            application/json:
              schema: {$ref: "#/components/schemas/ProjectHotspot"}
        "401": {$ref: "#/components/responses/Unauthorized"}
        "403": {$ref: "#/components/responses/Forbidden"}
        "404": {$ref: "#/components/responses/NotFound"}
        "500": {$ref: "#/components/responses/InternalServerError"}
        '400':
          description: Bad request
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'

  /api/v1/projects/{key}/measures:
    parameters:
    - name: key
      in: path
      required: true
      schema: {type: string}
    get:
      tags: [projects]
      summary: Get immutable measure node and its children for a given path
      operationId: getProjectMeasures
      description: >
        Returns the measure snapshot node corresponding to the given path, along
        with its direct children. Used for rendering code-quality tree and drill-downs.
      parameters:
      - name: path
        in: query
        description: Target path in the snapshot. Empty returns the project root.
        schema: {type: string, default: ""}
      - name: limit
        in: query
        description: Number of children to return.
        schema: {type: integer, default: 50, maximum: 200, minimum: 1}
      - name: cursor
        in: query
        description: Opaque pagination cursor.
        schema: {type: string}
      - name: domain
        in: query
        description: Repeatable domains filter.
        style: form
        explode: true
        schema:
          type: array
          items:
            type: string
            enum: [size, complexity, coverage, duplication, issues, debt, ratings]
      responses:
        "200":
          description: Project measure node and children
          content:
            application/json:
              schema: {$ref: "#/components/schemas/ProjectMeasureResponse"}
              examples:
                rootAnalyzed:
                  summary: Project root (Analyzed)
                  value:
                    state: analyzed
                    project: {key: payments-api, name: Payments API}
                    analysis: {id: analysis-123, created_at: "2026-07-20T10:00:00Z", source_ref: main, source_commit: abc123def}
                    path: ""
                    included_domains: [size, coverage]
                    node:
                      path: ""
                      name: ""
                      kind: project
                      language: ""
                      size:
                        files: {availability: available, value: 42, unavailable_reason: null}
                        ncloc: {availability: available, value: 1200, unavailable_reason: null}
                        comment_lines: {availability: available, value: 100, unavailable_reason: null}
                        blank_lines: {availability: available, value: 50, unavailable_reason: null}
                        functions: {availability: available, value: 120, unavailable_reason: null}
                        comment_density: {availability: available, value: 7.69, unavailable_reason: null}
                      coverage:
                        covered_lines: {availability: available, value: 800, unavailable_reason: null}
                        coverable_lines: {availability: available, value: 1000, unavailable_reason: null}
                        coverage: {availability: available, value: 80.0, unavailable_reason: null}
                        new_code_coverage: {availability: available, value: 85.0, unavailable_reason: null}
                    children:
                      items:
                        - path: src
                          name: src
                          kind: directory
                          language: ""
                          size:
                            files: {availability: available, value: 40, unavailable_reason: null}
                            ncloc: {availability: available, value: 1100, unavailable_reason: null}
                            comment_lines: {availability: available, value: 90, unavailable_reason: null}
                            blank_lines: {availability: available, value: 45, unavailable_reason: null}
                            functions: {availability: available, value: 110, unavailable_reason: null}
                            comment_density: {availability: available, value: 7.56, unavailable_reason: null}
                          coverage:
                            covered_lines: {availability: available, value: 700, unavailable_reason: null}
                            coverable_lines: {availability: available, value: 900, unavailable_reason: null}
                            coverage: {availability: available, value: 77.7, unavailable_reason: null}
                            new_code_coverage: {availability: not_applicable, value: null, unavailable_reason: null}
                      next_cursor: "cursor_for_next_page"
                directoryAnalyzed:
                  summary: Directory node (Analyzed)
                  value:
                    state: analyzed
                    project: {key: payments-api, name: Payments API}
                    analysis: {id: analysis-123, created_at: "2026-07-20T10:00:00Z", source_ref: main, source_commit: abc123def}
                    path: src
                    included_domains: [ratings]
                    node:
                      path: src
                      name: src
                      kind: directory
                      language: ""
                      ratings:
                        security: {availability: not_applicable, grade: null, unavailable_reason: null}
                        reliability: {availability: not_applicable, grade: null, unavailable_reason: null}
                        maintainability: {availability: not_applicable, grade: null, unavailable_reason: null}
                    children:
                      items:
                        - path: src/main.go
                          name: main.go
                          kind: file
                          language: go
                          ratings:
                            security: {availability: not_applicable, grade: null, unavailable_reason: null}
                            reliability: {availability: not_applicable, grade: null, unavailable_reason: null}
                            maintainability: {availability: not_applicable, grade: null, unavailable_reason: null}
                      next_cursor: null
                fileAnalyzed:
                  summary: File node (Analyzed)
                  value:
                    state: analyzed
                    project: {key: payments-api, name: Payments API}
                    analysis: {id: analysis-123, created_at: "2026-07-20T10:00:00Z", source_ref: main, source_commit: abc123def}
                    path: src/main.go
                    included_domains: [ratings]
                    node:
                      path: src/main.go
                      name: main.go
                      kind: file
                      language: go
                      ratings:
                        security: {availability: not_applicable, grade: null, unavailable_reason: null}
                        reliability: {availability: not_applicable, grade: null, unavailable_reason: null}
                        maintainability: {availability: not_applicable, grade: null, unavailable_reason: null}
                    children:
                      items: []
                      next_cursor: null
                notAnalyzed:
                  summary: Project not analyzed
                  value:
                    state: not_analyzed
                    project: {key: payments-api, name: Payments API}
                    analysis: null
                    path: ""
                    included_domains: [size]
                    node: null
                    children:
                      items: []
                      next_cursor: null
        "400": {$ref: "#/components/responses/BadRequest"}
        "401": {$ref: "#/components/responses/Unauthorized"}
        "403": {$ref: "#/components/responses/Forbidden"}
        "404": {$ref: "#/components/responses/NotFound"}
        "500": {$ref: "#/components/responses/InternalServerError"}

  /api/v1/rules:
    get:
      tags: [rules]
      summary: List rule catalog
      operationId: listRules
      description: Returns the immutable first-party rule catalog with optional filters.
      parameters:
      - name: q
        in: query
        description: Search term. Max length 256. (Total filter values across all
          dimensions limit is 64).
        schema:
          type: string
          maxLength: 256
      - name: language
        in: query
        style: form
        explode: true
        schema:
          type: array
          maxItems: 32
          items:
            type: string
            maxLength: 128
      - name: type
        in: query
        style: form
        explode: true
        schema:
          type: array
          maxItems: 32
          items:
            $ref: "#/components/schemas/RuleType"
      - name: severity
        in: query
        style: form
        explode: true
        schema:
          type: array
          maxItems: 32
          items:
            $ref: "#/components/schemas/RuleSeverity"
      - name: tag
        in: query
        style: form
        explode: true
        schema:
          type: array
          maxItems: 32
          items:
            type: string
            maxLength: 128
      - name: cwe
        in: query
        style: form
        explode: true
        schema:
          type: array
          maxItems: 32
          items:
            type: string
            maxLength: 128
      responses:
        "200":
          description: A list of rule summaries
          content:
            application/json:
              schema:
                type: array
                items: {$ref: "#/components/schemas/RuleSummary"}
        "400": {$ref: "#/components/responses/BadRequest"}
        "401": {$ref: "#/components/responses/Unauthorized"}
        "403": {$ref: "#/components/responses/Forbidden"}
        "500": {$ref: "#/components/responses/InternalServerError"}

  /api/v1/rules/{key}:
    parameters:
    - name: key
      in: path
      required: true
      schema:
        type: string
      example: go:sql-injection
    get:
      tags: [rules]
      summary: Get rule by exact key
      operationId: getRule
      responses:
        "200":
          description: The full rule detail
          content:
            application/json:
              schema: {$ref: "#/components/schemas/RuleDetail"}
        "400": {$ref: "#/components/responses/BadRequest"}
        "401": {$ref: "#/components/responses/Unauthorized"}
        "403": {$ref: "#/components/responses/Forbidden"}
        "500": {$ref: "#/components/responses/InternalServerError"}
        "404": {$ref: "#/components/responses/NotFound"}
  /api/v1/projects/{key}/hotspots/{id}/transitions:
    post:
      tags:
      - projects
      summary: Transition a project security hotspot
      operationId: transitionProjectHotspot
      parameters:
      - $ref: '#/components/parameters/ProjectKey'
      - name: id
        in: path
        required: true
        schema:
          type: string
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/ProjectHotspotTransitionRequest'
      responses:
        '200':
          description: Hotspot successfully transitioned
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ProjectHotspotTransitionResponse'
        '400':
          description: Bad request
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '403':
          $ref: '#/components/responses/Forbidden'
        '404':
          $ref: '#/components/responses/NotFound'
        '409':
          description: Conflict (version mismatch)
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '500':
          $ref: '#/components/responses/InternalServerError'
  /api/v1/projects/{key}/hotspots/{id}/history:
    get:
      tags:
      - projects
      summary: Get project security hotspot history
      operationId: getProjectHotspotHistory
      parameters:
      - $ref: '#/components/parameters/ProjectKey'
      - name: id
        in: path
        required: true
        schema:
          type: string
      responses:
        '200':
          description: Hotspot history
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: '#/components/schemas/ProjectHotspotReviewEvent'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '403':
          $ref: '#/components/responses/Forbidden'
        '404':
          $ref: '#/components/responses/NotFound'
        '500':
          $ref: '#/components/responses/InternalServerError'

  /api/v1/dashboard/security-operations:
    get:
      tags: [dashboard]
      operationId: getDashboardSecurityOperations
      summary: Get tenant-scoped Asset and Finding analytics for the Dashboard
      parameters:
      - name: range
        in: query
        schema: {type: string, enum: [7d, 30d, 90d], default: 30d}
      responses:
        '200':
          description: Security operations summary
          content:
            application/json:
              schema: {$ref: '#/components/schemas/DashboardSecurityOperations'}
        '400': {$ref: '#/components/responses/BadRequest'}
        '401': {$ref: '#/components/responses/Unauthorized'}
        '403': {$ref: '#/components/responses/Forbidden'}
        '500': {$ref: '#/components/responses/InternalServerError'}

  /api/v1/redteam/policy:
    get:
      tags: [redteam]
      operationId: getOffensivePolicy
      summary: Show the offensive policy register the running binary enforces
      description: >
        The register (policy.yaml) is parsed and validated at startup; an invalid register stops the
        process. This route returns it read-only: legal review status and every classified technique with
        its risk class, approval mode, blast radius and whether the binary treats it as prohibited or
        production-safe.
      responses:
        '200': {description: Policy register, content: {application/json: {schema: {$ref: '#/components/schemas/OffensivePolicy'}}}}
        '401': {$ref: '#/components/responses/Unauthorized'}
        '403': {$ref: '#/components/responses/Forbidden'}

  /api/v1/blueteam/engagements/{id}/response/plan:
    parameters: [{$ref: '#/components/parameters/EngagementId'}]
    post:
      tags: [blueteam]
      operationId: planResponse
      summary: Dry-run a governed defensive response action
      description: >
        Enumerates exactly what a response action (isolate_host, quarantine_file, stop_process) would do
        — the argv-only apply and its mandatory reversal — and executes NOTHING. Validates the action
        shape (an unknown kind is a 400) and resolves the engagement in the caller tenant (404 on
        cross-tenant/unknown). It does NOT authorize the target against the engagement scope; that check
        runs at apply, through the admission gate. Registered only when the response subsystem is wired.
      requestBody:
        required: true
        content: {application/json: {schema: {$ref: '#/components/schemas/ResponseActionRequest'}}}
      responses:
        '200': {description: The plan, content: {application/json: {schema: {$ref: '#/components/schemas/ResponsePlan'}}}}
        '400': {$ref: '#/components/responses/BadRequest'}
        '401': {$ref: '#/components/responses/Unauthorized'}
        '403': {$ref: '#/components/responses/Forbidden'}

  /api/v1/blueteam/engagements/{id}/response/apply:
    parameters: [{$ref: '#/components/parameters/EngagementId'}]
    post:
      tags: [blueteam]
      operationId: applyResponse
      summary: Apply a governed defensive response action
      description: >
        Applies a response action to an in-scope target through the SAME admission gate exploitation and
        DAST use: server-side scope authorization, a recorded human approver (a machine identity is
        refused), and a blast-radius check on the executed effect. The action id is server-minted. A
        second-approval requirement answers 202. The default executor performs no host effect; a real
        host executor is a deliberate, review-gated extension.
      requestBody:
        required: true
        content: {application/json: {schema: {$ref: '#/components/schemas/ResponseActionRequest'}}}
      responses:
        '200': {description: The applied action record, content: {application/json: {schema: {$ref: '#/components/schemas/ResponseRecord'}}}}
        '202': {description: Recorded pending a second human approval}
        '400': {$ref: '#/components/responses/BadRequest'}
        '401': {$ref: '#/components/responses/Unauthorized'}
        '403': {$ref: '#/components/responses/Forbidden'}
        '404': {$ref: '#/components/responses/NotFound'}

  /api/v1/blueteam/response/{id}/revert:
    parameters: [{name: id, in: path, required: true, schema: {type: string}, description: The response action id to revert}]
    post:
      tags: [blueteam]
      operationId: revertResponse
      summary: Revert an applied defensive response action
      description: >
        Reverses an applied action through the same admission gate and audit — a reversal is a
        first-class governed action. Only an applied action can be reverted, and the target must match
        the action's target.
      requestBody:
        required: true
        content: {application/json: {schema: {$ref: '#/components/schemas/ResponseRevertRequest'}}}
      responses:
        '200': {description: The reverted action record, content: {application/json: {schema: {$ref: '#/components/schemas/ResponseRecord'}}}}
        '400': {$ref: '#/components/responses/BadRequest'}
        '401': {$ref: '#/components/responses/Unauthorized'}
        '403': {$ref: '#/components/responses/Forbidden'}
        '404': {$ref: '#/components/responses/NotFound'}

  /api/v1/blueteam/response:
    get:
      tags: [blueteam]
      operationId: listResponses
      summary: List the tenant's response actions in a state
      description: >
        The tenant's response actions in a state (default pending) — the admitted-but-not-applied set the
        kill switch cancels.
      parameters:
        - {name: state, in: query, required: false, schema: {type: string, enum: [pending, applied, reverted, cancelled, violation]}, description: 'Filter by state (default: pending)'}
      responses:
        '200': {description: Response actions, content: {application/json: {schema: {$ref: '#/components/schemas/ResponseList'}}}}
        '401': {$ref: '#/components/responses/Unauthorized'}
        '403': {$ref: '#/components/responses/Forbidden'}

  /api/v1/assets/{assetID}/packages:
    parameters: [{$ref: '#/components/parameters/HostAssetId'}]
    get:
      tags: [assets]
      operationId: getHostPackages
      summary: Get the OS package inventory recorded for a fleet host
      description: >
        The components of the host's active package SBOM (name, version, package URL), sorted by name.
        A host that never reported packages returns an empty list and no engagement id.
      responses:
        '200': {description: Host packages, content: {application/json: {schema: {$ref: '#/components/schemas/HostPackages'}}}}
        '400': {$ref: '#/components/responses/BadRequest'}
        '401': {$ref: '#/components/responses/Unauthorized'}
        '403': {$ref: '#/components/responses/Forbidden'}
        '404': {$ref: '#/components/responses/NotFound'}

  /api/v1/alerts/test:
    post:
      tags: [fleet]
      operationId: testAlert
      summary: Deliver a test alert to every configured alert sink
      description: >
        Sends a synthetic `alert.test` alert through the configured sinks (the webhook set by
        SYNAPSE_ALERT_WEBHOOK_URL) so an operator can prove the alert path works before relying on it.
        The test alert bypasses the severity floor. Registered only when alerting is configured.
      responses:
        '200': {description: At least one sink acknowledged, content: {application/json: {schema: {$ref: '#/components/schemas/AlertTestResult'}}}}
        '401': {$ref: '#/components/responses/Unauthorized'}
        '403': {$ref: '#/components/responses/Forbidden'}
        '502': {description: No sink acknowledged the alert, content: {application/json: {schema: {$ref: '#/components/schemas/AlertTestResult'}}}}

  /api/v1/connectors:
    get:
      tags: [settings]
      operationId: listConnectors
      summary: List the tenant's source-control connectors
      description: >
        Every source-control connector configured for the caller's tenant, as metadata only. The
        personal access token is never returned. Registered only when a connector store is wired.
      responses:
        '200': {description: Connectors, content: {application/json: {schema: {type: object, properties: {connectors: {type: array, items: {$ref: '#/components/schemas/SCMConnector'}}}}}}}
        '401': {$ref: '#/components/responses/Unauthorized'}
        '403': {$ref: '#/components/responses/Forbidden'}
    post:
      tags: [settings]
      operationId: createConnector
      summary: Add a source-control connector so scans can clone a private repository
      description: >
        Stores a tenant-scoped git-host + username + personal access token. The token is sealed at
        rest (AES-256-GCM) and is write-only: it is accepted here and never returned. A server-initiated
        scan of a Project whose git source host matches this connector authenticates the clone with it.
        One connector per host per tenant. Requires the administer permission.
      requestBody:
        required: true
        content: {application/json: {schema: {$ref: '#/components/schemas/SCMConnectorCreate'}}}
      responses:
        '201': {description: Created, content: {application/json: {schema: {$ref: '#/components/schemas/SCMConnector'}}}}
        '400': {$ref: '#/components/responses/BadRequest'}
        '401': {$ref: '#/components/responses/Unauthorized'}
        '403': {$ref: '#/components/responses/Forbidden'}
        '409': {description: A connector already exists for this host}

  /api/v1/connectors/{id}:
    parameters: [{name: id, in: path, required: true, schema: {type: string}}]
    delete:
      tags: [settings]
      operationId: deleteConnector
      summary: Remove a source-control connector
      description: Deletes the connector and its sealed token. Requires the administer permission.
      responses:
        '204': {description: Deleted}
        '401': {$ref: '#/components/responses/Unauthorized'}
        '403': {$ref: '#/components/responses/Forbidden'}
        '404': {$ref: '#/components/responses/NotFound'}

  /api/v1/assets/hosts:
    get:
      tags: [assets]
      operationId: listHostVulnerabilities
      summary: List fleet host assets with their vulnerability summary
      description: >
        Every Kind=host asset of the tenant, worst first (critical, then high, then total). A host
        that reported OS packages carries the id of its hidden vulnerability context, the size and
        recording time of its active package SBOM, the latest scan job and the finding counts; a host
        that never reported packages has an empty summary and no engagement id. Registered only when
        SYNAPSE_FLEET_HOST_INGEST_ENABLED is set.
      responses:
        '200': {description: Host rows, content: {application/json: {schema: {type: array, items: {$ref: '#/components/schemas/HostVulnerabilitySummary'}}}}}
        '401': {$ref: '#/components/responses/Unauthorized'}
        '403': {$ref: '#/components/responses/Forbidden'}

  /api/v1/assets/{assetID}/vulnerabilities:
    parameters: [{$ref: '#/components/parameters/HostAssetId'}]
    get:
      tags: [assets, findings]
      operationId: getHostVulnerabilities
      summary: Get one fleet host's recorded packages, latest scan and vulnerability findings
      description: >
        The host's summary row plus the advisory-backed findings of its hidden vulnerability
        context, highest risk first, in the same shape as the engagement findings list. License
        findings derived from the same packages are excluded. A host that never reported packages
        returns an empty findings list; a non-host asset is a 400.
      responses:
        '200': {description: Host vulnerabilities, content: {application/json: {schema: {$ref: '#/components/schemas/HostVulnerabilities'}}}}
        '400': {$ref: '#/components/responses/BadRequest'}
        '401': {$ref: '#/components/responses/Unauthorized'}
        '403': {$ref: '#/components/responses/Forbidden'}
        '404': {$ref: '#/components/responses/NotFound'}

  /api/v1/appsec/assets:
    get:
      tags: [assets]
      operationId: listBusinessAssets
      summary: List tenant-scoped business Assets
      parameters:
      - {name: q, in: query, schema: {type: string, maxLength: 256}}
      - {name: type, in: query, schema: {type: string, enum: [product, application, system, business_service]}}
      - {name: criticality, in: query, schema: {type: string, enum: [critical, high, medium, low]}}
      - {name: lifecycle, in: query, schema: {type: string, enum: [draft, active, decommissioning, retired]}}
      - {name: owner, in: query, schema: {type: string, maxLength: 256}}
      - {name: limit, in: query, schema: {type: integer, minimum: 1, maximum: 200, default: 50}}
      - {name: offset, in: query, schema: {type: integer, minimum: 0, default: 0}}
      responses:
        '200': {description: Asset page, content: {application/json: {schema: {$ref: '#/components/schemas/BusinessAssetPage'}}}}
        '401': {$ref: '#/components/responses/Unauthorized'}
        '403': {$ref: '#/components/responses/Forbidden'}
    post:
      tags: [assets]
      operationId: createBusinessAsset
      summary: Create a business Asset
      requestBody: {required: true, content: {application/json: {schema: {$ref: '#/components/schemas/CreateBusinessAssetRequest'}}}}
      responses:
        '201': {description: Created Asset, content: {application/json: {schema: {$ref: '#/components/schemas/BusinessAsset'}}}}
        '400': {$ref: '#/components/responses/BadRequest'}
        '409': {description: Tenant key conflict, content: {application/json: {schema: {$ref: '#/components/schemas/Error'}}}}

  /api/v1/appsec/assets/{assetID}:
    parameters: [{$ref: '#/components/parameters/BusinessAssetId'}]
    get:
      tags: [assets]
      operationId: getBusinessAsset
      summary: Get a business Asset
      responses:
        '200': {description: Asset, content: {application/json: {schema: {$ref: '#/components/schemas/BusinessAsset'}}}}
        '404': {$ref: '#/components/responses/NotFound'}
    patch:
      tags: [assets]
      operationId: updateBusinessAsset
      summary: Update metadata or advance Asset lifecycle with optimistic versioning
      requestBody: {required: true, content: {application/json: {schema: {$ref: '#/components/schemas/UpdateBusinessAssetRequest'}}}}
      responses:
        '200': {description: Updated Asset, content: {application/json: {schema: {$ref: '#/components/schemas/BusinessAsset'}}}}
        '400': {$ref: '#/components/responses/BadRequest'}
        '404': {$ref: '#/components/responses/NotFound'}
        '409': {description: Version conflict, content: {application/json: {schema: {$ref: '#/components/schemas/Error'}}}}

  /api/v1/appsec/assets/{assetID}/projects:
    parameters: [{$ref: '#/components/parameters/BusinessAssetId'}]
    get:
      tags: [assets]
      operationId: listBusinessAssetProjects
      responses: {'200': {description: Project memberships, content: {application/json: {schema: {$ref: '#/components/schemas/AssetMemberships'}}}}}
    put:
      tags: [assets]
      operationId: replaceBusinessAssetProjects
      requestBody: {required: true, content: {application/json: {schema: {$ref: '#/components/schemas/AssetMemberships'}}}}
      responses: {'204': {description: Memberships replaced}, '400': {$ref: '#/components/responses/BadRequest'}, '404': {$ref: '#/components/responses/NotFound'}}

  /api/v1/appsec/assets/{assetID}/technical-assets:
    parameters: [{$ref: '#/components/parameters/BusinessAssetId'}]
    get:
      tags: [assets]
      operationId: listBusinessAssetTechnicalAssets
      responses: {'200': {description: Technical Asset memberships, content: {application/json: {schema: {$ref: '#/components/schemas/AssetMemberships'}}}}}
    put:
      tags: [assets]
      operationId: replaceBusinessAssetTechnicalAssets
      requestBody: {required: true, content: {application/json: {schema: {$ref: '#/components/schemas/AssetMemberships'}}}}
      responses: {'204': {description: Memberships replaced}, '400': {$ref: '#/components/responses/BadRequest'}, '404': {$ref: '#/components/responses/NotFound'}}

  /api/v1/appsec/assets/{assetID}/engagements:
    parameters: [{$ref: '#/components/parameters/BusinessAssetId'}]
    get:
      tags: [assets]
      operationId: listBusinessAssetEngagements
      responses: {'200': {description: Assigned Engagements, content: {application/json: {schema: {type: array, items: {type: object}}}}}, '404': {$ref: '#/components/responses/NotFound'}}

  /api/v1/appsec/assets/{assetID}/findings:
    parameters: [{$ref: '#/components/parameters/BusinessAssetId'}]
    get:
      tags: [assets]
      operationId: listBusinessAssetFindings
      responses:
        '200':
          description: First-party and imported Findings resolved through assigned Engagements
          content:
            application/json:
              schema:
                type: array
                items: {$ref: '#/components/schemas/BusinessAssetFinding'}
        '404': {$ref: '#/components/responses/NotFound'}

  /api/v1/appsec/assets/{assetID}/coverage:
    parameters: [{$ref: '#/components/parameters/BusinessAssetId'}]
    get:
      tags: [assets]
      operationId: getBusinessAssetCoverage
      responses: {'200': {description: Coverage and freshness projection, content: {application/json: {schema: {$ref: '#/components/schemas/BusinessAssetCoverage'}}}}, '404': {$ref: '#/components/responses/NotFound'}}

  /api/v1/appsec/assets/{assetID}/posture:
    parameters: [{$ref: '#/components/parameters/BusinessAssetId'}]
    get:
      tags: [assets]
      operationId: getBusinessAssetPosture
      responses: {'200': {description: Deterministic posture projection, content: {application/json: {schema: {$ref: '#/components/schemas/BusinessAssetPosture'}}}}, '404': {$ref: '#/components/responses/NotFound'}}

  /api/v1/appsec/assets/{assetID}/history:
    parameters: [{$ref: '#/components/parameters/BusinessAssetId'}]
    get:
      tags: [assets]
      operationId: getBusinessAssetHistory
      responses: {'200': {description: Assessment history, content: {application/json: {schema: {type: array, items: {$ref: '#/components/schemas/BusinessAssetHistoryItem'}}}}}, '404': {$ref: '#/components/responses/NotFound'}}

  /api/v1/engagements/{id}/asset:
    parameters: [{$ref: '#/components/parameters/EngagementId'}]
    put:
      tags: [assets, engagements]
      operationId: assignEngagementBusinessAsset
      summary: Assign or clear an Engagement's primary business Asset
      requestBody: {required: true, content: {application/json: {schema: {type: object, required: [asset_id], properties: {asset_id: {type: string, description: Empty string clears assignment}}}}}}
      responses: {'204': {description: Assignment updated}, '400': {$ref: '#/components/responses/BadRequest'}, '404': {$ref: '#/components/responses/NotFound'}}

  /api/v1/ai-triage/reviews:
    get:
      tags: [ai-triage]
      operationId: listAITriageReviews
      summary: List the tenant's durable human-review queue
      parameters:
      - {name: severity, in: query, schema: {type: string, enum: [critical, high, medium, low, info, unknown]}}
      - {name: cwe, in: query, schema: {type: string}}
      - {name: project, in: query, schema: {type: string}}
      - {name: state, in: query, schema: {type: string, enum: [pending, accepted, rejected]}}
      responses:
        '200':
          description: Review queue
          content:
            application/json:
              schema:
                type: object
                required: [reviews]
                properties:
                  reviews: {type: array, items: {$ref: '#/components/schemas/AITriageReview'}}
        '400': {$ref: '#/components/responses/BadRequest'}
        '401': {$ref: '#/components/responses/Unauthorized'}
        '403': {$ref: '#/components/responses/Forbidden'}

  /api/v1/ai-triage/observability:
    get:
      tags: [ai-triage]
      operationId: getAITriageObservability
      summary: Aggregate tenant-scoped AI triage safety, cost, and input-distribution metrics
      responses:
        '200': {description: Latest per-project AI triage metrics, baseline alerts, and drift-ready distribution snapshot, content: {application/json: {schema: {$ref: '#/components/schemas/AITriageDashboard'}}}}
        '401': {$ref: '#/components/responses/Unauthorized'}
        '403': {$ref: '#/components/responses/Forbidden'}

  /api/v1/redteam/halt:
    post:
      tags: [redteam]
      operationId: haltOffensiveWork
      summary: Kill switch — halt all in-flight offensive work for the tenant
      description: >-
        One operator action (PermAdminister) that cancels every in-flight offensive work order. The
        response reports the MEASURED duration against the stated bound rather than asserting
        compliance, and reports partial failure as failure. The bound covers the CONTROL PLANE only:
        a technique already running on a host stops within one further agent poll interval, which is
        why estate_stop_note is part of the response. See docs/redteam/offensive-policy.md section 8.
      requestBody:
        required: true
        content:
          application/json:
            schema: {$ref: '#/components/schemas/OffensiveHaltRequest'}
      responses:
        '200': {description: Every in-flight offensive work order was cancelled, content: {application/json: {schema: {$ref: '#/components/schemas/OffensiveHaltResult'}}}}
        '400': {$ref: '#/components/responses/BadRequest'}
        '401': {$ref: '#/components/responses/Unauthorized'}
        '403': {$ref: '#/components/responses/Forbidden'}
        '500': {description: The halt did not fully succeed; the partial result names the orders still in flight, content: {application/json: {schema: {$ref: '#/components/schemas/OffensiveHaltResult'}}}}

  /api/v1/ai-triage/reviews/{rid}/claim:
    parameters: [{$ref: '#/components/parameters/AITriageReviewId'}]
    post:
      tags: [ai-triage]
      operationId: claimAITriageReview
      summary: Assign an unowned review to the authenticated reviewer
      requestBody:
        required: true
        content:
          application/json:
            schema: {$ref: '#/components/schemas/AITriageReviewClaimRequest'}
      responses:
        '200': {description: Claimed review, content: {application/json: {schema: {$ref: '#/components/schemas/AITriageReview'}}}}
        '400': {$ref: '#/components/responses/BadRequest'}
        '401': {$ref: '#/components/responses/Unauthorized'}
        '403': {$ref: '#/components/responses/Forbidden'}
        '404': {$ref: '#/components/responses/NotFound'}
        '409': {description: Version or ownership conflict, content: {application/json: {schema: {$ref: '#/components/schemas/Error'}}}}

  /api/v1/ai-triage/reviews/{rid}/decision:
    parameters: [{$ref: '#/components/parameters/AITriageReviewId'}]
    post:
      tags: [ai-triage]
      operationId: decideAITriageReview
      summary: Accept or reject an owned AI false-positive recommendation
      requestBody:
        required: true
        content:
          application/json:
            schema: {$ref: '#/components/schemas/AITriageReviewDecisionRequest'}
      responses:
        '200': {description: Decided review, content: {application/json: {schema: {$ref: '#/components/schemas/AITriageReview'}}}}
        '400': {$ref: '#/components/responses/BadRequest'}
        '401': {$ref: '#/components/responses/Unauthorized'}
        '403': {$ref: '#/components/responses/Forbidden'}
        '404': {$ref: '#/components/responses/NotFound'}
        '409': {description: Version, state, or ownership conflict, content: {application/json: {schema: {$ref: '#/components/schemas/Error'}}}}

  /api/v1/vulnerability/overview:
    get:
      tags: [vulnerability-intelligence]
      summary: Get vulnerability intelligence operational overview
      description: Requires view permission. Counts active exposure from occurrences whose current state is `detected`; historical or no-longer-detected occurrences are excluded.
      responses:
        '200': {description: Tenant overview, content: {application/json: {schema: {$ref: '#/components/schemas/VulnerabilityOverview'}}}}
        '401': {$ref: '#/components/responses/Unauthorized'}
        '403': {$ref: '#/components/responses/Forbidden'}

  /api/v1/vulnerability/advisories:
    get:
      tags: [vulnerability-intelligence]
      summary: List canonical vulnerability advisories
      description: Requires view permission. `active_affected_count` counts current tenant occurrences in `detected` state only. Aggregate tenant filters are applied before the returned cursor page. Pagination is ordered by canonical advisory ID and continued with `after`.
      parameters:
      - {name: after, in: query, schema: {type: string}, description: Last advisory ID from the previous page}
      - {name: search, in: query, schema: {type: string}}
      - {name: status, in: query, schema: {type: array, items: {type: string, enum: [active, rejected, withdrawn]}}, explode: true}
      - {name: kev, in: query, schema: {type: boolean}}
      - {name: min_cvss, in: query, schema: {type: number, minimum: 0, maximum: 10}}
      - {name: max_cvss, in: query, schema: {type: number, minimum: 0, maximum: 10}}
      - {name: source, in: query, schema: {type: string}}
      - {name: risk_priority, in: query, schema: {type: array, items: {type: integer, minimum: 1, maximum: 5}}, explode: true}
      - {name: risk_trend, in: query, schema: {type: array, items: {type: string, enum: [none, new, increased, decreased, unchanged]}}, explode: true}
      - {name: affected_asset, in: query, schema: {type: string}, description: Case-insensitive asset, engagement, component, package, fingerprint, or CPE search}
      - {name: detection_state, in: query, schema: {type: array, items: {type: string, enum: [detected, no_longer_detected, withdrawn]}}, explode: true}
      - {name: action_state, in: query, schema: {type: array, items: {type: string, enum: [open, acknowledged, resolved, none]}}, explode: true}
      - {$ref: '#/components/parameters/VulnerabilityLimit'}
      responses:
        '200': {description: Advisory page, content: {application/json: {schema: {$ref: '#/components/schemas/VulnerabilityAdvisoryPage'}}}}
        '400': {$ref: '#/components/responses/BadRequest'}
        '401': {$ref: '#/components/responses/Unauthorized'}
        '403': {$ref: '#/components/responses/Forbidden'}

  /api/v1/vulnerability/advisories/{id}:
    parameters: [{$ref: '#/components/parameters/VulnerabilityAdvisoryId'}]
    get:
      tags: [vulnerability-intelligence]
      summary: Get the current canonical advisory
      responses:
        '200': {description: Current advisory, content: {application/json: {schema: {$ref: '#/components/schemas/VulnerabilityAdvisoryItem'}}}}
        '401': {$ref: '#/components/responses/Unauthorized'}
        '403': {$ref: '#/components/responses/Forbidden'}
        '404': {$ref: '#/components/responses/NotFound'}

  /api/v1/vulnerability/advisories/{id}/revisions:
    parameters: [{$ref: '#/components/parameters/VulnerabilityAdvisoryId'}]
    get:
      tags: [vulnerability-intelligence]
      summary: List immutable advisory revision history
      parameters:
      - {name: before_revision, in: query, schema: {type: integer, minimum: 1}, description: Continue with revisions lower than this value}
      - {$ref: '#/components/parameters/VulnerabilityLimit'}
      responses:
        '200': {description: Revision page, content: {application/json: {schema: {$ref: '#/components/schemas/VulnerabilityAdvisoryRevisionPage'}}}}
        '400': {$ref: '#/components/responses/BadRequest'}
        '401': {$ref: '#/components/responses/Unauthorized'}
        '403': {$ref: '#/components/responses/Forbidden'}

  /api/v1/vulnerability/advisories/{id}/revisions/{revision}:
    parameters:
    - {$ref: '#/components/parameters/VulnerabilityAdvisoryId'}
    - {name: revision, in: path, required: true, schema: {type: integer, minimum: 1}}
    get:
      tags: [vulnerability-intelligence]
      summary: Get one immutable advisory revision
      responses:
        '200': {description: Advisory revision, content: {application/json: {schema: {$ref: '#/components/schemas/VulnerabilityAdvisoryRevisionItem'}}}}
        '400': {$ref: '#/components/responses/BadRequest'}
        '401': {$ref: '#/components/responses/Unauthorized'}
        '403': {$ref: '#/components/responses/Forbidden'}
        '404': {$ref: '#/components/responses/NotFound'}

  /api/v1/vulnerability/occurrences:
    get:
      tags: [vulnerability-intelligence]
      summary: List tenant vulnerability occurrences
      description: Requires view permission. Both cursor fields are required together. Results are ordered by `UpdatedAt` and `ID` descending.
      parameters:
      - {name: engagement_id, in: query, schema: {type: string}}
      - {name: advisory_id, in: query, schema: {type: string}}
      - {name: state, in: query, schema: {type: array, items: {type: string, enum: [detected, no_longer_detected, withdrawn]}}, explode: true}
      - {$ref: '#/components/parameters/VulnerabilityCursorTime'}
      - {$ref: '#/components/parameters/VulnerabilityCursorId'}
      - {$ref: '#/components/parameters/VulnerabilityLimit'}
      responses:
        '200': {description: Occurrence page, content: {application/json: {schema: {$ref: '#/components/schemas/VulnerabilityOccurrencePage'}}}}
        '400': {$ref: '#/components/responses/BadRequest'}
        '401': {$ref: '#/components/responses/Unauthorized'}
        '403': {$ref: '#/components/responses/Forbidden'}

  /api/v1/vulnerability/occurrences/{id}/assessments:
    parameters: [{$ref: '#/components/parameters/VulnerabilityOccurrenceId'}]
    get:
      tags: [vulnerability-intelligence]
      summary: List immutable risk assessments for an occurrence
      parameters:
      - {$ref: '#/components/parameters/VulnerabilityCursorTime'}
      - {$ref: '#/components/parameters/VulnerabilityCursorId'}
      - {$ref: '#/components/parameters/VulnerabilityLimit'}
      responses:
        '200': {description: Assessment page, content: {application/json: {schema: {$ref: '#/components/schemas/VulnerabilityAssessmentPage'}}}}
        '400': {$ref: '#/components/responses/BadRequest'}
        '401': {$ref: '#/components/responses/Unauthorized'}
        '403': {$ref: '#/components/responses/Forbidden'}

  /api/v1/vulnerability/occurrences/{id}/transitions:
    parameters: [{$ref: '#/components/parameters/VulnerabilityOccurrenceId'}]
    get:
      tags: [vulnerability-intelligence]
      summary: List immutable risk transitions for an occurrence
      parameters:
      - {$ref: '#/components/parameters/VulnerabilityCursorTime'}
      - {$ref: '#/components/parameters/VulnerabilityCursorId'}
      - {$ref: '#/components/parameters/VulnerabilityLimit'}
      responses:
        '200': {description: Transition page, content: {application/json: {schema: {$ref: '#/components/schemas/VulnerabilityTransitionPage'}}}}
        '400': {$ref: '#/components/responses/BadRequest'}
        '401': {$ref: '#/components/responses/Unauthorized'}
        '403': {$ref: '#/components/responses/Forbidden'}

  /api/v1/vulnerability/sources:
    get:
      tags: [vulnerability-intelligence]
      summary: List configured vulnerability sources and health
      description: Requires view permission. Credential material is never returned; only `credential_configured` is exposed.
      parameters:
      - {name: include_archived, in: query, schema: {type: boolean, default: false}}
      responses:
        '200': {description: Source views, content: {application/json: {schema: {type: array, items: {$ref: '#/components/schemas/VulnerabilitySource'}}}}}
        '400': {$ref: '#/components/responses/BadRequest'}
        '401': {$ref: '#/components/responses/Unauthorized'}
        '403': {$ref: '#/components/responses/Forbidden'}

  /api/v1/vulnerability/sync-runs:
    get:
      tags: [vulnerability-intelligence]
      summary: List durable provider synchronization runs
      description: Requires view permission. Active states are `queued` and `running`; all other states are terminal. The ordinary view omits source snapshots, credential references, and raw provider payloads.
      parameters:
      - {name: source_id, in: query, schema: {type: string}}
      - {name: state, in: query, schema: {type: array, items: {$ref: '#/components/schemas/VulnerabilitySyncState'}}, explode: true}
      - {name: mode, in: query, schema: {type: array, items: {type: string, enum: [incremental, full]}}, explode: true}
      - {name: trigger, in: query, schema: {type: string}}
      - {name: from, in: query, schema: {type: string, format: date}, description: Inclusive UTC creation date}
      - {name: to, in: query, schema: {type: string, format: date}, description: Inclusive UTC creation date}
      - {$ref: '#/components/parameters/VulnerabilityCursorTime'}
      - {$ref: '#/components/parameters/VulnerabilityCursorId'}
      - {$ref: '#/components/parameters/VulnerabilityLimit'}
      responses:
        '200': {description: Safe sync-run page, content: {application/json: {schema: {$ref: '#/components/schemas/VulnerabilitySyncRunPage'}}}}
        '400': {$ref: '#/components/responses/BadRequest'}
        '401': {$ref: '#/components/responses/Unauthorized'}
        '403': {$ref: '#/components/responses/Forbidden'}

  /api/v1/vulnerability/sync-runs/{id}:
    parameters: [{$ref: '#/components/parameters/VulnerabilityRunId'}]
    get:
      tags: [vulnerability-intelligence]
      summary: Get a safe durable synchronization run view
      description: Source snapshots, credential references, and raw provider payloads are not returned.
      responses:
        '200': {description: Safe sync run, content: {application/json: {schema: {$ref: '#/components/schemas/VulnerabilitySyncRun'}}}}
        '401': {$ref: '#/components/responses/Unauthorized'}
        '403': {$ref: '#/components/responses/Forbidden'}
        '404': {$ref: '#/components/responses/NotFound'}

  /api/v1/vulnerability/actions:
    get:
      tags: [vulnerability-intelligence]
      summary: List tenant vulnerability risk actions
      parameters:
      - {name: status, in: query, schema: {type: array, items: {type: string, enum: [open, acknowledged, resolved]}}, explode: true}
      - {name: before_created_at, in: query, schema: {type: string, format: date-time}, description: Must be supplied together with `before_id`}
      - {name: before_id, in: query, schema: {type: string}, description: Must be supplied together with `before_created_at`}
      - {$ref: '#/components/parameters/VulnerabilityLimit'}
      responses:
        '200': {description: Action page, content: {application/json: {schema: {$ref: '#/components/schemas/VulnerabilityActionPage'}}}}
        '400': {$ref: '#/components/responses/BadRequest'}
        '401': {$ref: '#/components/responses/Unauthorized'}
        '403': {$ref: '#/components/responses/Forbidden'}

  /api/v1/vulnerability/sources/{id}/sync:
    parameters: [{$ref: '#/components/parameters/VulnerabilitySourceId'}]
    post:
      tags: [vulnerability-intelligence]
      summary: Enqueue a durable source synchronization
      description: Requires operate permission. Reuse one idempotency key for retries of the same operator action.
      requestBody: {content: {application/json: {schema: {$ref: '#/components/schemas/VulnerabilitySyncRequest'}}}}
      responses:
        '202': {description: Durable synchronization accepted, content: {application/json: {schema: {$ref: '#/components/schemas/VulnerabilitySyncAccepted'}}}}
        '400': {$ref: '#/components/responses/BadRequest'}
        '401': {$ref: '#/components/responses/Unauthorized'}
        '403': {$ref: '#/components/responses/Forbidden'}

  /api/v1/vulnerability/sources/test:
    post:
      tags: [vulnerability-intelligence]
      summary: Test an unsaved vulnerability source draft
      description: Requires administer permission. The draft is normalized and tested with the code-owned adapter but is never persisted. When `existing_source_id` is supplied and `credential_ref` is omitted, the stored credential reference is resolved without returning it.
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required: [source]
              properties:
                source: {type: object, additionalProperties: true}
                existing_source_id: {type: string}
      responses:
        '200': {description: Draft connectivity succeeded}
        '400': {$ref: '#/components/responses/BadRequest'}
        '401': {$ref: '#/components/responses/Unauthorized'}
        '403': {$ref: '#/components/responses/Forbidden'}

  /api/v1/vulnerability/advisories/{id}/reconcile:
    parameters: [{$ref: '#/components/parameters/VulnerabilityAdvisoryId'}]
    post:
      tags: [vulnerability-intelligence]
      summary: Enqueue tenant correlation for one advisory
      description: Requires operate permission. The HTTP request only creates durable work and returns `202`.
      requestBody: {content: {application/json: {schema: {$ref: '#/components/schemas/VulnerabilityReconcileRequest'}}}}
      responses:
        '202': {description: Durable reconciliation accepted, content: {application/json: {schema: {$ref: '#/components/schemas/VulnerabilityReconcileAccepted'}}}}
        '400': {$ref: '#/components/responses/BadRequest'}
        '401': {$ref: '#/components/responses/Unauthorized'}
        '403': {$ref: '#/components/responses/Forbidden'}

  /api/v1/vulnerability/reconcile/tenant:
    post:
      tags: [vulnerability-intelligence]
      summary: Enqueue a tenant-wide full reconciliation
      description: Requires operate permission. Rollback or disabling this operation stops new mutation only and never deletes historical occurrences, assessments, transitions, actions, or runs.
      requestBody: {content: {application/json: {schema: {$ref: '#/components/schemas/VulnerabilityReconcileRequest'}}}}
      responses:
        '202': {description: Durable reconciliation accepted, content: {application/json: {schema: {$ref: '#/components/schemas/VulnerabilityReconcileAccepted'}}}}
        '400': {$ref: '#/components/responses/BadRequest'}
        '401': {$ref: '#/components/responses/Unauthorized'}
        '403': {$ref: '#/components/responses/Forbidden'}

  /api/v1/vulnerability/reconcile-runs/{id}:
    parameters: [{$ref: '#/components/parameters/VulnerabilityRunId'}]
    get:
      tags: [vulnerability-intelligence]
      summary: Get a durable reconciliation run
      description: Active states are `queued` and `running`; `succeeded` and `failed` are terminal.
      responses:
        '200': {description: Reconciliation run, content: {application/json: {schema: {$ref: '#/components/schemas/VulnerabilityReconcileRun'}}}}
        '401': {$ref: '#/components/responses/Unauthorized'}
        '403': {$ref: '#/components/responses/Forbidden'}
        '404': {$ref: '#/components/responses/NotFound'}

  /api/v1/vulnerability/reconcile-runs/{id}/diffs:
    parameters: [{$ref: '#/components/parameters/VulnerabilityRunId'}]
    get:
      tags: [vulnerability-intelligence]
      summary: List durable dry-run reconciliation differences
      description: >-
        Requires view permission. Cursor fields are required as one complete set and results are ordered by mismatch class,
        engagement, advisory, and component fingerprint. Dry-run count fields map to persisted classes:
        added=`missing_occurrence`, updated=`changed_occurrence`, unchanged=`in_sync`, retired=`stale_occurrence`,
        unmatchable=`unmatchable_input`; processed is their total.
      parameters:
      - {name: class, in: query, schema: {$ref: '#/components/schemas/VulnerabilityReconcileDiffClass'}}
      - {$ref: '#/components/parameters/VulnerabilityLimit'}
      - {name: after_class, in: query, schema: {$ref: '#/components/schemas/VulnerabilityReconcileDiffClass'}, description: Must be supplied with all other `after_*` fields}
      - {name: after_engagement_id, in: query, schema: {type: string}, description: Must be supplied with all other `after_*` fields}
      - {name: after_advisory_id, in: query, schema: {type: string}, description: Must be supplied with all other `after_*` fields}
      - {name: after_component_fingerprint, in: query, schema: {type: string}, description: Must be supplied with all other `after_*` fields}
      responses:
        '200': {description: Reconciliation difference page, content: {application/json: {schema: {$ref: '#/components/schemas/VulnerabilityReconcileDiffPage'}}}}
        '400': {$ref: '#/components/responses/BadRequest'}
        '401': {$ref: '#/components/responses/Unauthorized'}
        '403': {$ref: '#/components/responses/Forbidden'}
        '404': {$ref: '#/components/responses/NotFound'}

  /api/v1/sla/policies:
    get:
      tags: [remediation-sla]
      operationId: listSLAPolicies
      summary: List immutable SLA policy versions and the active version
      description: Requires view permission. The built-in policy is installed on first use when no tenant policy exists.
      responses:
        '200':
          description: Active policy and append-only tenant policy history
          content:
            application/json:
              schema: {$ref: '#/components/schemas/SLAPolicyList'}
        '401': {$ref: '#/components/responses/Unauthorized'}
        '403': {$ref: '#/components/responses/Forbidden'}
    post:
      tags: [remediation-sla]
      operationId: activateSLAPolicy
      summary: Validate, append, and activate an SLA policy version
      description: Requires administer permission and a human principal. Existing assessments are never rewritten.
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required: [config]
              properties:
                config: {$ref: '#/components/schemas/SLAConfig'}
      responses:
        '200': {description: Existing identical policy selected, content: {application/json: {schema: {$ref: '#/components/schemas/SLAPolicyActivation'}}}}
        '201': {description: New immutable policy created and selected, content: {application/json: {schema: {$ref: '#/components/schemas/SLAPolicyActivation'}}}}
        '400': {$ref: '#/components/responses/BadRequest'}
        '401': {$ref: '#/components/responses/Unauthorized'}
        '403': {$ref: '#/components/responses/Forbidden'}

  /api/v1/engagements/{id}/slas:
    parameters: [{$ref: '#/components/parameters/EngagementId'}]
    get:
      tags: [remediation-sla, findings]
      operationId: listEngagementSLAs
      summary: List current remediation SLA views for an engagement
      description: Time-sensitive overdue and acceptance-expiry fields are calculated at read time.
      responses:
        '200':
          description: Current SLA views
          content:
            application/json:
              schema:
                type: object
                required: [slas]
                properties:
                  slas: {type: array, items: {$ref: '#/components/schemas/SLAView'}}
        '401': {$ref: '#/components/responses/Unauthorized'}
        '403': {$ref: '#/components/responses/Forbidden'}
        '404': {$ref: '#/components/responses/NotFound'}

  /api/v1/engagements/{id}/slas/{fid}:
    parameters:
    - $ref: '#/components/parameters/EngagementId'
    - $ref: '#/components/parameters/FindingId'
    get:
      tags: [remediation-sla, findings]
      operationId: getFindingSLA
      summary: Get the current SLA assessment and remediation lifecycle for a finding
      responses:
        '200': {description: Current SLA view, content: {application/json: {schema: {$ref: '#/components/schemas/SLAView'}}}}
        '401': {$ref: '#/components/responses/Unauthorized'}
        '403': {$ref: '#/components/responses/Forbidden'}
        '404': {$ref: '#/components/responses/NotFound'}

  /api/v1/engagements/{id}/slas/{fid}/assessments:
    parameters:
    - $ref: '#/components/parameters/EngagementId'
    - $ref: '#/components/parameters/FindingId'
    get:
      tags: [remediation-sla, findings]
      operationId: listSLAAssessmentHistory
      summary: List immutable SLA assessments for a finding
      responses:
        '200':
          description: Oldest-to-newest assessment history
          content:
            application/json:
              schema:
                type: object
                required: [assessments]
                properties:
                  assessments: {type: array, items: {$ref: '#/components/schemas/SLAAssessment'}}
        '401': {$ref: '#/components/responses/Unauthorized'}
        '403': {$ref: '#/components/responses/Forbidden'}
        '404': {$ref: '#/components/responses/NotFound'}

  /api/v1/engagements/{id}/slas/{fid}/events:
    parameters:
    - $ref: '#/components/parameters/EngagementId'
    - $ref: '#/components/parameters/FindingId'
    get:
      tags: [remediation-sla, findings]
      operationId: listSLALifecycleEvents
      summary: List append-only human remediation lifecycle events
      responses:
        '200':
          description: Oldest-to-newest lifecycle audit history
          content:
            application/json:
              schema:
                type: object
                required: [events]
                properties:
                  events: {type: array, items: {$ref: '#/components/schemas/SLALifecycleEvent'}}
        '401': {$ref: '#/components/responses/Unauthorized'}
        '403': {$ref: '#/components/responses/Forbidden'}
        '404': {$ref: '#/components/responses/NotFound'}

  /api/v1/engagements/{id}/slas/{fid}/transition:
    parameters:
    - $ref: '#/components/parameters/EngagementId'
    - $ref: '#/components/parameters/FindingId'
    post:
      tags: [remediation-sla, findings]
      operationId: transitionFindingSLA
      summary: Apply an optimistic-concurrency protected human remediation decision
      description: Machine principals are rejected. Accepted risk requires a reason, compensating control, and future expiry; remediated is terminal.
      requestBody:
        required: true
        content:
          application/json:
            schema: {$ref: '#/components/schemas/SLATransitionRequest'}
      responses:
        '200': {description: Updated current SLA view, content: {application/json: {schema: {$ref: '#/components/schemas/SLAView'}}}}
        '400': {$ref: '#/components/responses/BadRequest'}
        '401': {$ref: '#/components/responses/Unauthorized'}
        '403': {$ref: '#/components/responses/Forbidden'}
        '404': {$ref: '#/components/responses/NotFound'}
        '409': {description: Lifecycle version changed since it was loaded, content: {application/json: {schema: {$ref: '#/components/schemas/Error'}}}}

  /api/v1/agents/{id}/keys:
    parameters:
    - $ref: '#/components/parameters/AgentId'
    get:
      tags: [fleet]
      operationId: listAgentSigningKeys
      summary: List an agent's content-signing keys
      responses:
        '200':
          description: Signing-key lifecycle records
          content:
            application/json:
              schema: {$ref: '#/components/schemas/AgentSigningKeyList'}
        '401': {$ref: '#/components/responses/Unauthorized'}
        '403': {$ref: '#/components/responses/Forbidden'}
        '404': {$ref: '#/components/responses/NotFound'}

  /api/v1/agents/{id}/keys/{keyID}/revoke:
    parameters:
    - $ref: '#/components/parameters/AgentId'
    - $ref: '#/components/parameters/SigningKeyId'
    post:
      tags: [fleet]
      operationId: revokeAgentSigningKey
      summary: Revoke one agent content-signing key
      responses:
        '200':
          description: Key revoked monotonically
          content:
            application/json:
              schema:
                type: object
                required: [agent_id, key_id, state]
                properties:
                  agent_id: {type: string}
                  key_id: {type: string}
                  state: {type: string, const: revoked}
        '400': {$ref: '#/components/responses/BadRequest'}
        '401': {$ref: '#/components/responses/Unauthorized'}
        '403': {$ref: '#/components/responses/Forbidden'}
        '404': {$ref: '#/components/responses/NotFound'}

  /api/v1/fleet/keys:
    post:
      tags: [fleet]
      operationId: registerFleetSigningKey
      summary: Register an agent content-signing key with proof-of-possession
      description: The canonical agent identity comes from the bearer credential, never the request body.
      security:
      - fleetAgentAuth: []
      parameters:
      - $ref: '#/components/parameters/FleetProtocolVersion'
      requestBody:
        required: true
        content:
          application/json:
            schema: {$ref: '#/components/schemas/AgentSigningKeyRegistration'}
      responses:
        '201':
          description: Key registered or idempotently re-registered
          content:
            application/json:
              schema: {$ref: '#/components/schemas/AgentSigningKeyRegistrationResult'}
        '400': {$ref: '#/components/responses/BadRequest'}
        '401': {$ref: '#/components/responses/Unauthorized'}
        '403': {$ref: '#/components/responses/Forbidden'}
        '404': {$ref: '#/components/responses/NotFound'}

  /api/v1/fleet/detections:
    post:
      tags: [fleet]
      operationId: ingestFleetDetectionBatch
      summary: Verify and ingest an independently signed agent detection batch
      description: Accepts only V2 batches. Verifies authenticated agent identity, key lifecycle, signature, exact membership, causal durable telemetry, privacy-policy attribution, and per-detection content digests before exactly-once evidence sealing.
      security:
      - fleetAgentAuth: []
      parameters:
      - $ref: '#/components/parameters/FleetProtocolVersion'
      requestBody:
        required: true
        content:
          application/json:
            schema: {$ref: '#/components/schemas/AgentDetectionBatchRequest'}
      responses:
        '200':
          description: The complete batch was admitted or idempotently skipped
          content:
            application/json:
              schema: {$ref: '#/components/schemas/AgentDetectionBatchResult'}
        '400': {$ref: '#/components/responses/BadRequest'}
        '401': {$ref: '#/components/responses/Unauthorized'}
        '403': {$ref: '#/components/responses/Forbidden'}
        '404': {$ref: '#/components/responses/NotFound'}

  /api/v1/fleet/privacy-policies/active:
    get:
      tags: [fleet]
      operationId: getActiveFleetPrivacyPolicy
      summary: Get the active immutable source-privacy policy assignment
      description: Human/operator read of the tenant's active source-redaction policy. Tenant identity comes only from the authenticated principal.
      security:
      - bearerAuth: []
      responses:
        '200':
          description: Active privacy-policy assignment
          content:
            application/json:
              schema: {$ref: '#/components/schemas/PrivacyPolicyAssignmentEnvelope'}
        '401': {$ref: '#/components/responses/Unauthorized'}
        '403': {$ref: '#/components/responses/Forbidden'}
        '404': {$ref: '#/components/responses/NotFound'}
        '500': {$ref: '#/components/responses/InternalServerError'}

  /api/v1/fleet/privacy-policies:
    get:
      tags: [fleet]
      operationId: listFleetPrivacyPolicyHistory
      summary: List immutable source-privacy policy history
      description: Human/operator read of every immutable policy assignment admitted for the authenticated tenant.
      security:
      - bearerAuth: []
      responses:
        '200':
          description: Ordered immutable privacy-policy assignment history
          content:
            application/json:
              schema: {$ref: '#/components/schemas/PrivacyPolicyHistoryResponse'}
        '401': {$ref: '#/components/responses/Unauthorized'}
        '403': {$ref: '#/components/responses/Forbidden'}
        '500': {$ref: '#/components/responses/InternalServerError'}
    post:
      tags: [fleet]
      operationId: admitFleetPrivacyPolicy
      summary: Admit an immutable source-privacy policy assignment
      description: Human administrator admission of immutable policy content. Activation is a separate revisioned governance operation.
      security:
      - bearerAuth: []
      requestBody:
        required: true
        content:
          application/json:
            schema: {$ref: '#/components/schemas/PrivacyPolicyAdmissionRequest'}
      responses:
        '200':
          description: An identical immutable assignment already existed
          content:
            application/json:
              schema: {$ref: '#/components/schemas/PrivacyPolicyAdmissionResponse'}
        '201':
          description: Immutable privacy-policy assignment admitted
          content:
            application/json:
              schema: {$ref: '#/components/schemas/PrivacyPolicyAdmissionResponse'}
        '400': {$ref: '#/components/responses/BadRequest'}
        '401': {$ref: '#/components/responses/Unauthorized'}
        '403': {$ref: '#/components/responses/Forbidden'}
        '409': {$ref: '#/components/responses/Conflict'}
        '500': {$ref: '#/components/responses/InternalServerError'}

  /api/v1/fleet/privacy-policies/activate:
    post:
      tags: [fleet]
      operationId: activateFleetPrivacyPolicy
      summary: Activate an admitted immutable source-privacy policy assignment
      description: Human administrator mutation of the active-policy pointer. Every transition requires a caller-stable operation ID and appends an immutable revision.
      security:
      - bearerAuth: []
      requestBody:
        required: true
        content:
          application/json:
            schema: {$ref: '#/components/schemas/PrivacyPolicyActivationRequest'}
      responses:
        '200':
          description: Privacy-policy assignment activated
          content:
            application/json:
              schema: {$ref: '#/components/schemas/PrivacyPolicyAssignmentEnvelope'}
        '400': {$ref: '#/components/responses/BadRequest'}
        '401': {$ref: '#/components/responses/Unauthorized'}
        '403': {$ref: '#/components/responses/Forbidden'}
        '404': {$ref: '#/components/responses/NotFound'}
        '409': {$ref: '#/components/responses/Conflict'}
        '500': {$ref: '#/components/responses/InternalServerError'}

  /api/v1/fleet/privacy-policy:
    get:
      tags: [fleet]
      operationId: getFleetAgentPrivacyPolicy
      summary: Deliver the active source-privacy policy to an authenticated fleet agent
      description: >
        Returns only the active assignment for the authenticated agent's tenant. Agent identity and tenant
        binding come from fleet-agent authentication; request-controlled identity cannot select another policy.
      security:
      - fleetAgentAuth: []
      parameters:
      - $ref: '#/components/parameters/FleetProtocolVersion'
      responses:
        '200':
          description: Active privacy-policy assignment bound to the authenticated fleet agent's tenant
          content:
            application/json:
              schema: {$ref: '#/components/schemas/PrivacyPolicyAssignmentEnvelope'}
        '401': {description: Fleet agent authentication failed, content: {application/json: {schema: {$ref: '#/components/schemas/Error'}}}}
        '403': {$ref: '#/components/responses/Forbidden'}
        '404': {$ref: '#/components/responses/NotFound'}
        '503': {description: Privacy-policy delivery is unavailable, content: {application/json: {schema: {$ref: '#/components/schemas/Error'}}}}
        '500': {$ref: '#/components/responses/InternalServerError'}

  /api/v1/fleet/coverage-windows:
    get:
      tags: [fleet]
      operationId: listFleetCoverageWindows
      summary: List immutable telemetry coverage-window revisions
      description: >
        Human/operator read of tenant-scoped coverage revisions derived from signed sensor state,
        immutable telemetry accounting, agent-origin gaps, and inferred delivery gaps. Tenant identity
        comes only from the authenticated principal. Filters use half-open overlap semantics; the
        response excludes raw telemetry, source payloads, signing material, and request-controlled
        tenant identity. A gap, loss fact, or unhealthy sensor state cannot be represented as complete
        coverage. Coverage may lower Confidence but never changes true Risk.
      security:
      - bearerAuth: []
      parameters:
      - name: agent_id
        in: query
        schema: {type: string, minLength: 1}
      - name: asset_id
        in: query
        schema: {type: string, minLength: 1}
      - name: host_id
        in: query
        schema: {type: string, minLength: 1}
      - name: since
        in: query
        description: RFC 3339 lower overlap bound; when both bounds are present it must be before until.
        schema: {type: string, format: date-time}
      - name: until
        in: query
        description: RFC 3339 exclusive upper overlap bound; when both bounds are present it must be after since.
        schema: {type: string, format: date-time}
      - name: limit
        in: query
        description: Maximum revisions to return; omit or set zero for the server default.
        schema: {type: integer, minimum: 0, maximum: 1000}
      responses:
        '200':
          description: Ordered immutable coverage-window revisions
          content:
            application/json:
              schema: {$ref: '#/components/schemas/CoverageWindowList'}
        '400': {$ref: '#/components/responses/BadRequest'}
        '401': {$ref: '#/components/responses/Unauthorized'}
        '403': {$ref: '#/components/responses/Forbidden'}
        '500': {$ref: '#/components/responses/InternalServerError'}

  /api/v1/fleet/workloads:
    get:
      tags: [fleet]
      operationId: listFleetWorkloads
      summary: List Kubernetes workloads with the images they run
      description: >
        Human/operator read of the tenant's Kubernetes workloads mapped to the container image digests
        they run, derived from the cluster-inventory asset graph (workload depends_on image). It answers
        "which deployment or statefulset does this image come from": a container CVE found on an image
        digest can be attributed to every workload that runs it. Tenant identity comes only from the
        authenticated principal. Empty until a cluster agent ingests a snapshot.
      security:
      - bearerAuth: []
      responses:
        '200':
          description: Workloads and the images they run
          content:
            application/json:
              schema:
                type: object
                required: [workloads]
                properties:
                  workloads:
                    type: array
                    items:
                      type: object
                      required: [cluster, namespace, kind, name, images]
                      properties:
                        cluster: {type: string}
                        namespace: {type: string}
                        kind: {type: string, description: 'controller kind: Deployment, StatefulSet, DaemonSet, ...'}
                        name: {type: string}
                        service_account: {type: string}
                        images:
                          type: array
                          items:
                            type: object
                            required: [ref, digest]
                            properties:
                              ref: {type: string, description: image reference as declared}
                              digest: {type: string, description: resolved image digest (the image asset natural key)}
        '401': {$ref: '#/components/responses/Unauthorized'}
        '403': {$ref: '#/components/responses/Forbidden'}
        '500': {$ref: '#/components/responses/InternalServerError'}

  /api/v1/fleet/sensor-states:
    post:
      tags: [fleet]
      operationId: ingestFleetSensorState
      summary: Verify and persist a signed P0 sensor-state report
      description: >
        Persists restart-safe sensor-state and class-coverage history after verifying
        the enrolled agent, session, server-authoritative asset binding, telemetry-signing
        key lifecycle, purpose-bound signature, and payload digest. Unknown JSON fields
        fail closed and reports are idempotent by immutable report identity.
      security:
      - fleetAgentAuth: []
      parameters:
      - $ref: '#/components/parameters/FleetProtocolVersion'
      - name: Content-Encoding
        in: header
        required: false
        description: Optional wire compression. Omit for raw JSON.
        schema: {type: string, enum: [gzip]}
      requestBody:
        required: true
        content:
          application/vnd.synapse.sensor-state+json:
            schema: {$ref: '#/components/schemas/SensorStateReport'}
      responses:
        '200':
          description: Sensor-state report durably acknowledged or idempotently observed
          content:
            application/json:
              schema: {$ref: '#/components/schemas/SensorStateAcknowledgement'}
        '400': {$ref: '#/components/responses/BadRequest'}
        '401': {description: Fleet agent authentication failed, content: {application/json: {schema: {$ref: '#/components/schemas/Error'}}}}
        '403': {$ref: '#/components/responses/Forbidden'}
        '404': {$ref: '#/components/responses/NotFound'}
        '409': {description: Immutable sensor-state report identity conflicts with different content, content: {application/json: {schema: {$ref: '#/components/schemas/Error'}}}}
        '413': {description: Encoded or decoded sensor-state body exceeds the bounded transport limit, content: {application/json: {schema: {$ref: '#/components/schemas/Error'}}}}
        '415': {description: Unsupported content encoding or media type, content: {application/json: {schema: {$ref: '#/components/schemas/Error'}}}}
        '503': {description: Backpressure; the agent keeps the report pending and retries according to Retry-After, content: {application/json: {schema: {$ref: '#/components/schemas/Error'}}}}
        '500': {$ref: '#/components/responses/InternalServerError'}

  /api/v1/fleet/processes:
    post:
      tags: [fleet]
      operationId: reportFleetProcesses
      summary: Report the host's running processes for the behavior baseline
      description: >
        Agent-plane endpoint (#594 D input). An enrolled agent ships its running-process snapshot
        (pid, comm, resolved exe path, liveness) read from procfs. The host asset the report is
        attributed to is resolved server-side from the authenticated agent's canonical telemetry
        binding, never from the request, so an agent cannot report for a host it does not own. An
        agent with no binding yet (it has not reported inventory) is refused. The snapshot is stored
        as the per-host running-process projection and folded into the asset's behavior baseline.
        Registered only when process reporting is wired.
      security:
      - fleetAgentAuth: []
      parameters:
      - $ref: '#/components/parameters/FleetProtocolVersion'
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                processes:
                  type: array
                  items:
                    type: object
                    properties:
                      pid: {type: integer}
                      comm: {type: string}
                      path: {type: string}
                      running: {type: boolean}
      responses:
        '200':
          description: Report accepted
          content:
            application/json:
              schema:
                type: object
                properties:
                  asset_id: {type: string}
                  saved: {type: integer}
                  learned: {type: boolean}
        '400': {$ref: '#/components/responses/BadRequest'}
        '401': {$ref: '#/components/responses/Unauthorized'}
        '404': {description: Process reporting not enabled}

  /api/v1/fleet/telemetry:
    post:
      tags: [fleet]
      operationId: ingestFleetTelemetry
      summary: Ingest one signed raw telemetry batch or one signed durable agent gap
      description: >
        Agent-plane endpoint for A3 telemetry transport. The authenticated tenant and agent
        identity are authoritative; AgentID, HostID, enrollment Session, server-derived delivery
        StreamID, and AssetID carried on the wire are validated against server state and cannot
        redirect a delivery lane. Telemetry batch manifests and gap reports are purpose-bound
        Ed25519 signed and fail closed on unknown keys, unsupported protocol/schema versions,
        digest mismatches, stale incarnations, or invalid identity bindings. Batch delivery is
        at-least-once and idempotent. The returned ACK is the highest contiguous transport-batch
        sequence durably ingested. Agent-origin gap coordinates are local WAL-record coordinates:
        they are persisted for audit and completeness reporting but never advance the transport-batch
        ACK. Content-Encoding gzip is accepted for either media type. Request decoding is strict;
        unknown JSON fields fail. A missing Content-Type remains backward-compatible with legacy
        batch clients and is interpreted as application/json; an explicit unsupported media type fails 415.
      security:
      - fleetAgentAuth: []
      parameters:
      - $ref: '#/components/parameters/FleetProtocolVersion'
      - name: Content-Encoding
        in: header
        required: false
        description: Optional wire compression. Omit for raw JSON.
        schema: {type: string, enum: [gzip]}
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              additionalProperties: false
              required: [Manifest, Events]
              properties:
                Manifest:
                  type: object
                  additionalProperties: false
                  required: [ProtocolVersion, SchemaVersion, BatchID, AgentID, HostID, AssetID, StreamID, Position, EventTimeMin, EventTimeMax, ObservedCount, KeptCount, SampledOutCount, TruncatedCount, DroppedCount, SamplingPolicyDigest, Events, PayloadDigest, KeyID, Signature]
                  properties:
                    ProtocolVersion: {type: integer, const: 1}
                    SchemaVersion: {type: integer, minimum: 1, maximum: 2}
                    BatchID: {type: string, minLength: 1}
                    AgentID: {type: string, minLength: 1}
                    HostID: {type: string, minLength: 1}
                    AssetID: {type: string, minLength: 1}
                    StreamID: {type: string, minLength: 1, description: Server-derived delivery stream identity; agent choice is rejected}
                    Position:
                      type: object
                      additionalProperties: false
                      required: [Priority, Epoch, Sequence, Session, Boot]
                      properties:
                        Priority: {type: integer, minimum: 0, maximum: 3, description: P0..P3 encoded as 0..3}
                        Epoch: {type: integer, format: uint64, minimum: 1}
                        Sequence: {type: integer, format: uint64, minimum: 1, description: Transport-batch sequence within the priority/epoch lane}
                        Session: {type: string, minLength: 1}
                        Boot: {type: string, minLength: 1}
                    PreviousSequence: {type: integer, format: uint64, minimum: 0}
                    EventTimeMin: {type: string, format: date-time}
                    EventTimeMax: {type: string, format: date-time}
                    ObservedCount: {type: integer, minimum: 0}
                    KeptCount: {type: integer, minimum: 0, description: May be zero when every observed event was honestly sampled out, dropped, or otherwise accounted without fabricating a retained event}
                    SampledOutCount: {type: integer, minimum: 0}
                    TruncatedCount: {type: integer, minimum: 0}
                    DroppedCount: {type: integer, minimum: 0}
                    SamplingPolicyDigest: {type: string, minLength: 1}
                    Events:
                      type: array
                      items:
                        type: object
                        additionalProperties: false
                        required: [ID, Digest]
                        properties:
                          ID: {type: string, minLength: 1}
                          Digest: {type: string, minLength: 1}
                    PayloadDigest: {type: string, minLength: 1}
                    KeyID: {type: string, minLength: 1}
                    Signature: {type: string, description: "Base64 Ed25519 signature produced by encoding/json for []byte"}
                Events:
                  type: array
                  items:
                    type: object
                    additionalProperties: false
                    required: [EventID, Class, Payload, ObservedAt]
                    properties:
                      EventID: {type: string, minLength: 1}
                      Class: {type: string, minLength: 1}
                      Payload: {type: string, format: byte, description: Base64-encoded event bytes}
                      ObservedAt: {type: string, format: date-time}
          application/vnd.synapse.telemetry-gap+json:
            schema:
              type: object
              additionalProperties: false
              required: [ProtocolVersion, GapID, AgentID, HostID, AgentSessionID, AssetID, StreamID, Priority, Epoch, KnownSequence, FromSequence, ToSequence, Count, Reason, FromAt, ToAt, KeyID, Signature]
              properties:
                ProtocolVersion: {type: integer, const: 1}
                GapID: {type: string, minLength: 1}
                AgentID: {type: string, minLength: 1}
                HostID: {type: string, minLength: 1}
                AgentSessionID: {type: string, minLength: 1}
                AssetID: {type: string, minLength: 1}
                StreamID: {type: string, minLength: 1, description: Server-derived delivery stream identity}
                Priority: {type: integer, minimum: 0, maximum: 3}
                Epoch: {type: integer, format: uint64, minimum: 1}
                KnownSequence: {type: boolean, description: True when the durable loss report has an exact local WAL-record range; this never advances the transport-batch ACK}
                FromSequence: {type: integer, format: uint64, minimum: 0, description: First lost local WAL-record sequence when KnownSequence is true}
                ToSequence: {type: integer, format: uint64, minimum: 0, description: Last lost local WAL-record sequence when KnownSequence is true}
                Count: {type: integer, format: uint64, minimum: 1}
                Reason: {type: string, enum: [quota_eviction, quota_backpressure, corrupt_frame, torn_write, io_failure, unsynced_tail, state_recovery]}
                FromAt: {type: string, format: date-time}
                ToAt: {type: string, format: date-time}
                KeyID: {type: string, minLength: 1}
                Signature: {type: string, description: "Base64 Ed25519 signature produced by encoding/json for []byte"}
      responses:
        '200':
          description: Batch persisted/idempotently observed, or signed gap durably acknowledged
          content:
            application/json:
              schema:
                oneOf:
                - type: object
                  additionalProperties: false
                  required: [accepted, duplicate, ack, provenance, gap_open]
                  properties:
                    accepted: {type: boolean}
                    duplicate: {type: boolean}
                    ack: {type: integer, format: uint64, minimum: 0, description: Highest contiguous durably ingested transport-batch sequence}
                    provenance: {type: string}
                    gap_open: {type: boolean}
                - type: object
                  additionalProperties: false
                  required: [acknowledged, gap_id]
                  properties:
                    acknowledged: {type: boolean, const: true}
                    gap_id: {type: string, minLength: 1}
        '400': {$ref: '#/components/responses/BadRequest'}
        '401': {description: Fleet agent authentication failed, content: {application/json: {schema: {$ref: '#/components/schemas/Error'}}}}
        '403': {$ref: '#/components/responses/Forbidden'}
        '404': {$ref: '#/components/responses/NotFound'}
        '409': {description: Replay/incarnation, immutable gap identity, or stream-state conflict, content: {application/json: {schema: {$ref: '#/components/schemas/Error'}}}}
        '413': {description: Encoded or decoded telemetry body exceeds the bounded transport limit, content: {application/json: {schema: {$ref: '#/components/schemas/Error'}}}}
        '415': {description: Unsupported content encoding or explicit media type, content: {application/json: {schema: {$ref: '#/components/schemas/Error'}}}}
        '429': {description: Backpressure; the agent preserves WAL state and retries according to Retry-After, content: {application/json: {schema: {$ref: '#/components/schemas/Error'}}}}
        '500': {$ref: '#/components/responses/InternalServerError'}

  /api/v1/integration-providers:
    get:
      tags: [integrations]
      operationId: listIntegrationProviders
      summary: List code-owned integration provider descriptors
      responses:
        '200': {description: Provider descriptors without secrets, content: {application/json: {schema: {type: array, items: {$ref: '#/components/schemas/IntegrationProviderDescriptor'}}}}}
        '401': {$ref: '#/components/responses/Unauthorized'}
        '403': {$ref: '#/components/responses/Forbidden'}

  /api/v1/integrations:
    get:
      tags: [integrations]
      operationId: listIntegrations
      summary: List tenant integrations
      parameters:
      - name: include_archived
        in: query
        schema: {type: boolean, default: false}
      responses:
        '200': {description: Integrations with credential presence only, content: {application/json: {schema: {type: array, items: {$ref: '#/components/schemas/Integration'}}}}}
        '401': {$ref: '#/components/responses/Unauthorized'}
        '403': {$ref: '#/components/responses/Forbidden'}
    post:
      tags: [integrations]
      operationId: createIntegration
      summary: Create a disabled integration
      requestBody:
        required: true
        content: {application/json: {schema: {$ref: '#/components/schemas/CreateIntegrationRequest'}}}
      responses:
        '201': {description: Created integration, content: {application/json: {schema: {$ref: '#/components/schemas/Integration'}}}}
        '400': {$ref: '#/components/responses/BadRequest'}
        '401': {$ref: '#/components/responses/Unauthorized'}
        '403': {$ref: '#/components/responses/Forbidden'}
        '409': {description: Active provider endpoint already exists, content: {application/json: {schema: {$ref: '#/components/schemas/Error'}}}}

  /api/v1/integrations/{id}:
    parameters: [{$ref: '#/components/parameters/IntegrationId'}]
    get:
      tags: [integrations]
      operationId: getIntegration
      summary: Get an integration without credential plaintext
      responses:
        '200': {description: Integration, content: {application/json: {schema: {$ref: '#/components/schemas/Integration'}}}}
        '401': {$ref: '#/components/responses/Unauthorized'}
        '403': {$ref: '#/components/responses/Forbidden'}
        '404': {$ref: '#/components/responses/NotFound'}
    put:
      tags: [integrations]
      operationId: updateIntegration
      summary: Update integration configuration with optimistic concurrency
      requestBody:
        required: true
        content: {application/json: {schema: {$ref: '#/components/schemas/UpdateIntegrationRequest'}}}
      responses:
        '200': {description: Updated integration, content: {application/json: {schema: {$ref: '#/components/schemas/Integration'}}}}
  /api/v1/fleet/assets/{id}/desired-capabilities:
    parameters: [{$ref: '#/components/parameters/FleetAssetId'}]
    put:
      tags: [fleet]
      operationId: setFleetAssetDesiredCapabilities
      summary: Declare the capabilities a canonical host or cluster asset should have
      description: >
        Operator-owned desired state, deliberately separate from the capabilities an untrusted agent
        observes and advertises. The tenant comes from the authenticated principal, the asset from the
        path, and the actor from the authenticated principal; none of the three is a body field. The
        submitted list is normalized (trimmed, de-duplicated, sorted) before storage, so the stored set
        may be shorter than the one sent. Replacing an existing set increments Version within the same
        PolicyID; clearing and recreating mints a new PolicyID. Only host and cluster assets are
        accepted.
      security:
      - bearerAuth: []
      requestBody:
        required: true
        content:
          application/json:
            schema: {$ref: '#/components/schemas/SetFleetDesiredCapabilitiesRequest'}
      responses:
        '200':
          description: Stored desired-capability state after normalization
          content:
            application/json:
              schema: {$ref: '#/components/schemas/FleetDesiredCapabilityState'}
        '400': {$ref: '#/components/responses/BadRequest'}
        '401': {$ref: '#/components/responses/Unauthorized'}
        '403': {$ref: '#/components/responses/Forbidden'}
        '404': {$ref: '#/components/responses/NotFound'}
        '409': {$ref: '#/components/responses/Conflict'}
        '500': {$ref: '#/components/responses/InternalServerError'}
    get:
      tags: [fleet]
      operationId: getFleetAssetDesiredCapabilities
      summary: Read the desired capabilities declared for one asset
      description: Tenant comes from the authenticated principal; an asset with no declared intent answers 404.
      security:
      - bearerAuth: []
      responses:
        '200':
          description: Current desired-capability state
          content:
            application/json:
              schema: {$ref: '#/components/schemas/FleetDesiredCapabilityState'}
        '400': {$ref: '#/components/responses/BadRequest'}
        '401': {$ref: '#/components/responses/Unauthorized'}
        '403': {$ref: '#/components/responses/Forbidden'}
        '404': {$ref: '#/components/responses/NotFound'}
        '409': {description: Version conflict, content: {application/json: {schema: {$ref: '#/components/schemas/Error'}}}}

    delete:
      tags: [fleet]
      operationId: clearFleetAssetDesiredCapabilities
      summary: Clear the desired capabilities declared for one asset
      description: >
        Idempotent: an asset with no declared intent also answers 204. Clearing ends the policy
        lifecycle, so a later declaration receives a new PolicyID and Version 1.
      security:
      - bearerAuth: []
      responses:
        '204': {description: Desired capabilities cleared, or none were declared}
        '400': {$ref: '#/components/responses/BadRequest'}
        '401': {$ref: '#/components/responses/Unauthorized'}
        '403': {$ref: '#/components/responses/Forbidden'}
        '500': {$ref: '#/components/responses/InternalServerError'}
  /api/v1/integrations/{id}/enable:
    parameters: [{$ref: '#/components/parameters/IntegrationId'}]
    post:
      tags: [integrations]
      operationId: enableIntegration
      summary: Enable an integration after a successful connection test
      requestBody: {required: true, content: {application/json: {schema: {$ref: '#/components/schemas/IntegrationVersionRequest'}}}}
      responses:
        '200': {description: Enabled integration, content: {application/json: {schema: {$ref: '#/components/schemas/Integration'}}}}
        '400': {$ref: '#/components/responses/BadRequest'}
        '401': {$ref: '#/components/responses/Unauthorized'}
        '403': {$ref: '#/components/responses/Forbidden'}
        '404': {$ref: '#/components/responses/NotFound'}
        '409': {description: Missing successful test or version conflict, content: {application/json: {schema: {$ref: '#/components/schemas/Error'}}}}

  /api/v1/integrations/{id}/disable:
    parameters: [{$ref: '#/components/parameters/IntegrationId'}]
    post:
      tags: [integrations]
      operationId: disableIntegration
      summary: Disable scheduled polling for an integration
      requestBody: {required: true, content: {application/json: {schema: {$ref: '#/components/schemas/IntegrationVersionRequest'}}}}
      responses:
        '200': {description: Disabled integration, content: {application/json: {schema: {$ref: '#/components/schemas/Integration'}}}}
        '400': {$ref: '#/components/responses/BadRequest'}
        '401': {$ref: '#/components/responses/Unauthorized'}
        '403': {$ref: '#/components/responses/Forbidden'}
        '404': {$ref: '#/components/responses/NotFound'}
        '409': {description: Version conflict, content: {application/json: {schema: {$ref: '#/components/schemas/Error'}}}}

  /api/v1/integrations/{id}/archive:
    parameters: [{$ref: '#/components/parameters/IntegrationId'}]
    post:
      tags: [integrations]
      operationId: archiveIntegration
      summary: Archive an integration while retaining history
      requestBody: {required: true, content: {application/json: {schema: {$ref: '#/components/schemas/IntegrationVersionRequest'}}}}
      responses:
        '204': {description: Archived}
        '400': {$ref: '#/components/responses/BadRequest'}
        '401': {$ref: '#/components/responses/Unauthorized'}
        '403': {$ref: '#/components/responses/Forbidden'}
        '404': {$ref: '#/components/responses/NotFound'}
        '409': {description: Version conflict, content: {application/json: {schema: {$ref: '#/components/schemas/Error'}}}}

  /api/v1/integrations/{id}/credentials:
    parameters: [{$ref: '#/components/parameters/IntegrationId'}]
    put:
      tags: [integrations]
      operationId: putIntegrationCredential
      summary: Set or replace a write-only encrypted credential bundle
      description: Plaintext is accepted only in this request and is never returned by any API.
      requestBody: {required: true, content: {application/json: {schema: {$ref: '#/components/schemas/IntegrationCredentialRequest'}}}}
      responses:
        '204': {description: Credential encrypted and stored}
        '400': {$ref: '#/components/responses/BadRequest'}
        '401': {$ref: '#/components/responses/Unauthorized'}
        '403': {$ref: '#/components/responses/Forbidden'}
        '404': {$ref: '#/components/responses/NotFound'}
        '409': {description: Version or connection revision conflict, content: {application/json: {schema: {$ref: '#/components/schemas/Error'}}}}
    delete:
      tags: [integrations]
      operationId: deleteIntegrationCredential
      summary: Delete the integration credential bundle
      requestBody: {required: true, content: {application/json: {schema: {$ref: '#/components/schemas/IntegrationRevisionRequest'}}}}
      responses:
        '204': {description: Credential deleted}
        '400': {$ref: '#/components/responses/BadRequest'}
        '401': {$ref: '#/components/responses/Unauthorized'}
        '403': {$ref: '#/components/responses/Forbidden'}
        '404': {$ref: '#/components/responses/NotFound'}
        '409': {description: Version or connection revision conflict, content: {application/json: {schema: {$ref: '#/components/schemas/Error'}}}}

  /api/v1/integrations/{id}/operations:
    parameters: [{$ref: '#/components/parameters/IntegrationId'}]
    get:
      tags: [integrations]
      operationId: listIntegrationOperations
      summary: List durable integration operations
      parameters:
      - name: limit
        in: query
        schema: {type: integer, minimum: 1, maximum: 200, default: 50}
      responses:
        '200': {description: Operation history, content: {application/json: {schema: {type: array, items: {$ref: '#/components/schemas/IntegrationOperation'}}}}}
        '401': {$ref: '#/components/responses/Unauthorized'}
        '403': {$ref: '#/components/responses/Forbidden'}
        '404': {$ref: '#/components/responses/NotFound'}
    post:
      tags: [integrations]
      operationId: startIntegrationOperation
      summary: Queue a connection test, discovery, or poll operation
      requestBody: {required: true, content: {application/json: {schema: {$ref: '#/components/schemas/StartIntegrationOperationRequest'}}}}
      responses:
        '202': {description: Durable operation accepted, content: {application/json: {schema: {$ref: '#/components/schemas/IntegrationOperation'}}}}
        '400': {$ref: '#/components/responses/BadRequest'}
        '401': {$ref: '#/components/responses/Unauthorized'}
        '403': {$ref: '#/components/responses/Forbidden'}
        '404': {$ref: '#/components/responses/NotFound'}
        '409': {description: Active operation or lifecycle conflict, content: {application/json: {schema: {$ref: '#/components/schemas/Error'}}}}

  /api/v1/integration-operations/{operationID}:
    parameters: [{$ref: '#/components/parameters/IntegrationOperationId'}]
    get:
      tags: [integrations]
      operationId: getIntegrationOperation
      summary: Get one durable integration operation
      responses:
        '200': {description: Operation, content: {application/json: {schema: {$ref: '#/components/schemas/IntegrationOperation'}}}}
        '401': {$ref: '#/components/responses/Unauthorized'}
        '403': {$ref: '#/components/responses/Forbidden'}
        '404': {$ref: '#/components/responses/NotFound'}

  /api/v1/integration-operations/{operationID}/cancel:
    parameters: [{$ref: '#/components/parameters/IntegrationOperationId'}]
    post:
      tags: [integrations]
      operationId: cancelIntegrationOperation
      summary: Cancel a queued or running integration operation
      requestBody: {required: true, content: {application/json: {schema: {type: object, additionalProperties: false}}}}
      responses:
        '200': {description: Cancelled operation, content: {application/json: {schema: {$ref: '#/components/schemas/IntegrationOperation'}}}}
        '400': {$ref: '#/components/responses/BadRequest'}
        '401': {$ref: '#/components/responses/Unauthorized'}
        '403': {$ref: '#/components/responses/Forbidden'}
        '404': {$ref: '#/components/responses/NotFound'}
        '409': {description: Operation already terminal, content: {application/json: {schema: {$ref: '#/components/schemas/Error'}}}}

  /api/v1/integrations/{id}/bindings:
    parameters: [{$ref: '#/components/parameters/IntegrationId'}]
    get:
      tags: [integrations]
      operationId: listIntegrationBindings
      summary: List Project bindings for an integration
      responses:
        '200': {description: Bindings, content: {application/json: {schema: {type: array, items: {$ref: '#/components/schemas/IntegrationBinding'}}}}}
        '401': {$ref: '#/components/responses/Unauthorized'}
        '403': {$ref: '#/components/responses/Forbidden'}
        '404': {$ref: '#/components/responses/NotFound'}
    post:
      tags: [integrations]
      operationId: createIntegrationBinding
      summary: Bind a discovered external pipeline to a tenant Project
      requestBody: {required: true, content: {application/json: {schema: {$ref: '#/components/schemas/CreateIntegrationBindingRequest'}}}}
      responses:
        '201': {description: Binding, content: {application/json: {schema: {$ref: '#/components/schemas/IntegrationBinding'}}}}
        '500': {$ref: '#/components/responses/InternalServerError'}
  /api/v1/fleet/desired-capabilities/gaps:
    get:
      tags: [fleet]
      operationId: listFleetDesiredCapabilityGaps
      summary: List desired capabilities not covered by a healthy bound agent
      description: >
        Reconciles every declared desired capability in the tenant against the current agent bindings
        and the observed agent fleet, and returns only the uncovered rows. The read is side-effect
        free. A capability is covered when a healthy bound agent advertises it; otherwise the row
        carries the most informative representative agent and a closed-set gap reason.
      security:
      - bearerAuth: []
      responses:
        '200':
          description: Uncovered desired capabilities, ordered by asset then capability
          content:
            application/json:
              schema: {$ref: '#/components/schemas/FleetDesiredCapabilityGapList'}
        '400': {$ref: '#/components/responses/BadRequest'}
        '401': {$ref: '#/components/responses/Unauthorized'}
        '403': {$ref: '#/components/responses/Forbidden'}
        '500': {$ref: '#/components/responses/InternalServerError'}

  /api/v1/fleet/assets/{id}/behavior-baseline/rebaseline:
    parameters: [{$ref: '#/components/parameters/FleetAssetId'}]
    post:
      tags: [fleet]
      operationId: rebaselineBehaviorBaseline
      summary: Re-baseline a drifted or poisoned behavior baseline for one host asset
      description: >
        A behavior baseline that latched on drift (or was refused as poisoned) abstains from scoring
        until it is re-baselined. This resets it (reset_pending -> learning), clearing its accumulators
        so it re-learns from fresh eligible windows. A baseline not in a re-baselineable state is a 400.
        The reset is audited. Registered only when the behavior baseline is wired.
      security:
      - bearerAuth: []
      responses:
        '200':
          description: Baseline reset to re-learn
          content:
            application/json:
              schema:
                type: object
                properties:
                  asset_id: {type: string}
                  rebaselined: {type: boolean}
        '400': {$ref: '#/components/responses/BadRequest'}
        '401': {$ref: '#/components/responses/Unauthorized'}
        '403': {$ref: '#/components/responses/Forbidden'}
        '404': {description: Behavior baseline re-baseline not enabled}

  /api/v1/fleet/assets/{id}/processes:
    parameters: [{$ref: '#/components/parameters/FleetAssetId'}]
    post:
      tags: [fleet]
      operationId: reportFleetAssetProcesses
      summary: Report the running-process snapshot for one host asset
      description: >
        Upserts the reported snapshots by (tenant, asset, entity); re-reporting a process in place
        flips Running rather than adding a row. The tenant is the authenticated fleet tenant and the
        asset is the path id, so a report cannot claim another tenant's or asset's processes. At most
        10000 processes per report. Reporting also folds the profile into the asset's behavioural
        baseline when that learner is wired; a learn failure is logged and does not fail the report.
      security:
      - bearerAuth: []
      requestBody:
        required: true
        content:
          application/json:
            schema: {$ref: '#/components/schemas/ReportFleetEndpointProcessesRequest'}
      responses:
        '200':
          description: Snapshots durably upserted
          content:
            application/json:
              schema: {$ref: '#/components/schemas/ReportFleetEndpointProcessesResult'}
        '400': {$ref: '#/components/responses/BadRequest'}
        '401': {$ref: '#/components/responses/Unauthorized'}
        '403': {$ref: '#/components/responses/Forbidden'}
        '500': {$ref: '#/components/responses/InternalServerError'}
    get:
      tags: [fleet]
      operationId: listFleetAssetProcesses
      summary: List the currently running processes on one host asset
      description: Returns only snapshots whose Running flag is set, ordered by EntityID. An asset with none returns an empty array.
      security:
      - bearerAuth: []
      responses:
        '200':
          description: Running-process projection for the asset
          content:
            application/json:
              schema: {$ref: '#/components/schemas/FleetEndpointProcessList'}
        '400': {$ref: '#/components/responses/BadRequest'}
        '401': {$ref: '#/components/responses/Unauthorized'}
        '403': {$ref: '#/components/responses/Forbidden'}
        '500': {$ref: '#/components/responses/InternalServerError'}

  /api/v1/fleet/assets/{id}/timeline:
    parameters: [{$ref: '#/components/parameters/FleetAssetId'}]
    get:
      tags: [fleet]
      operationId: queryFleetAssetTimeline
      summary: Query the endpoint state timeline for one host asset
      description: >
        Reads the per-asset projection of accepted telemetry, ordered by event time (when the
        transition happened on the host), never by ingest order. The tenant is the authenticated
        fleet tenant. Entries stay explainable after the raw telemetry that produced them expires.
      security:
      - bearerAuth: []
      parameters:
      - name: from
        in: query
        description: RFC 3339 inclusive lower bound on event time. Omit for no lower bound.
        schema: {type: string, format: date-time}
      - name: to
        in: query
        description: RFC 3339 inclusive upper bound on event time. Omit for no upper bound.
        schema: {type: string, format: date-time}
      - name: entity
        in: query
        description: Restrict the window to one endpoint entity id.
        schema: {type: string}
      - name: limit
        in: query
        description: >
          Maximum entries to return. An absent, unparsable, or non-positive value falls back to 500;
          a value above 5000 is clamped to 5000.
        schema: {type: integer, minimum: 1, maximum: 5000, default: 500}
      responses:
        '200':
          description: Event-time ordered timeline window
          content:
            application/json:
              schema: {$ref: '#/components/schemas/FleetEndpointTimelineList'}
        '400': {$ref: '#/components/responses/BadRequest'}
        '401': {$ref: '#/components/responses/Unauthorized'}
        '403': {$ref: '#/components/responses/Forbidden'}
        '500': {$ref: '#/components/responses/InternalServerError'}

  /api/v1/fleet/assets/{id}/retro-hunt:
    parameters: [{$ref: '#/components/parameters/FleetAssetId'}]
    post:
      tags: [fleet]
      operationId: retroHuntFleetAssetTimeline
      summary: Re-hunt the endpoint timeline window around a pivot time
      description: >
        Read-only analysis: pivots to the transitions in [around - before_seconds, around +
        after_seconds] on the asset, optionally focused on one entity. Fail-closed on a missing pivot,
        a negative look-back or look-forward, or a zero-width window. Truncated is honest: the service
        queries one entry past the effective limit so a capped window is always reported as partial.
      security:
      - bearerAuth: []
      requestBody:
        required: true
        content:
          application/json:
            schema: {$ref: '#/components/schemas/FleetRetroHuntRequest'}
      responses:
        '200':
          description: The surrounding timeline window
          content:
            application/json:
              schema: {$ref: '#/components/schemas/FleetRetroHuntResult'}
        '400': {$ref: '#/components/responses/BadRequest'}
        '401': {$ref: '#/components/responses/Unauthorized'}
        '403': {$ref: '#/components/responses/Forbidden'}
        '500': {$ref: '#/components/responses/InternalServerError'}

  /api/v1/fleet/engagements/{id}/legal-hold:
    parameters: [{$ref: '#/components/parameters/EngagementId'}]
    put:
      tags: [fleet]
      operationId: placeFleetLegalHold
      summary: Place a legal hold on an engagement's data
      description: >
        A hold suspends retention expiry: held data survives its retention window until the hold is
        released. Idempotent, so re-placing returns the existing active hold unchanged. The reason is
        mandatory for accountability; the placing operator and the tenant are server-side.
      security:
      - bearerAuth: []
      requestBody:
        required: true
        content:
          application/json:
            schema: {$ref: '#/components/schemas/PlaceFleetLegalHoldRequest'}
      responses:
        '200':
          description: The active hold, newly placed or already in force
          content:
            application/json:
              schema: {$ref: '#/components/schemas/FleetLegalHold'}
        '400': {$ref: '#/components/responses/BadRequest'}
        '401': {$ref: '#/components/responses/Unauthorized'}
        '403': {$ref: '#/components/responses/Forbidden'}
        '500': {$ref: '#/components/responses/InternalServerError'}
    delete:
      tags: [fleet]
      operationId: releaseFleetLegalHold
      summary: Release the active legal hold on an engagement's data
      description: >
        Idempotent: an engagement with no active hold also answers 204. The released hold is retained
        as append-only history so the hold and release trail stays auditable.
      security:
      - bearerAuth: []
      responses:
        '204': {description: Hold released, or none was active}
        '400': {$ref: '#/components/responses/BadRequest'}
        '401': {$ref: '#/components/responses/Unauthorized'}
        '403': {$ref: '#/components/responses/Forbidden'}
        '500': {$ref: '#/components/responses/InternalServerError'}

  /api/v1/fleet/legal-holds:
    get:
      tags: [fleet]
      operationId: listFleetLegalHolds
      summary: List the tenant's active legal holds
      description: Active holds only, ordered by engagement id. Tenant identity comes from the authenticated principal.
      security:
      - bearerAuth: []
      responses:
        '200':
          description: Active legal holds
          content:
            application/json:
              schema: {$ref: '#/components/schemas/FleetLegalHoldList'}
        '400': {$ref: '#/components/responses/BadRequest'}
        '401': {$ref: '#/components/responses/Unauthorized'}
        '403': {$ref: '#/components/responses/Forbidden'}
        '500': {$ref: '#/components/responses/InternalServerError'}

  /api/v1/fleet/engagements/{id}/privacy-export:
    parameters: [{$ref: '#/components/parameters/EngagementId'}]
    get:
      tags: [fleet]
      operationId: exportFleetEngagementPrivacyData
      summary: Export the governance data held for one engagement
      description: >
        Data-subject and DPO export: the detection projection rows the control plane holds for the
        engagement plus the active legal holds covering them. Read-only and mutates nothing; the
        immutable evidence chain is untouched. The export itself is an audited governance access, so
        an audit-write failure fails the request rather than returning an unlogged bundle.
      security:
      - bearerAuth: []
      responses:
        '200':
          description: The export bundle
          content:
            application/json:
              schema: {$ref: '#/components/schemas/FleetPrivacyExportBundle'}
        '400': {$ref: '#/components/responses/BadRequest'}
        '401': {$ref: '#/components/responses/Unauthorized'}
        '403': {$ref: '#/components/responses/Forbidden'}
        '500': {$ref: '#/components/responses/InternalServerError'}

  /api/v1/fleet/engagements/{id}/detection-data:
    parameters: [{$ref: '#/components/parameters/EngagementId'}]
    delete:
      tags: [fleet]
      operationId: purgeFleetEngagementDetectionData
      summary: Delete an engagement's detection projection on demand
      description: >
        Right-to-erasure over the queryable projection only: the append-only evidence chain is
        preserved. Destructive and irreversible for the projection, so a reason is mandatory and the
        deletion is audited with actor and reason. Fail-closed on a legal hold, which answers 403:
        preservation trumps erasure. An engagement with no detection rows answers 200 with a zero
        count.
      security:
      - bearerAuth: []
      requestBody:
        required: true
        content:
          application/json:
            schema: {$ref: '#/components/schemas/PurgeFleetDetectionDataRequest'}
      responses:
        '200':
          description: Projection rows deleted
          content:
            application/json:
              schema: {$ref: '#/components/schemas/PurgeFleetDetectionDataResult'}
        '400': {$ref: '#/components/responses/BadRequest'}
        '401': {$ref: '#/components/responses/Unauthorized'}
        '403': {$ref: '#/components/responses/Forbidden'}
        '409': {$ref: '#/components/responses/Conflict'}
        '500': {$ref: '#/components/responses/InternalServerError'}

  /api/v1/fleet/engagements/{id}/correlate:
    parameters: [{$ref: '#/components/parameters/EngagementId'}]
    post:
      tags: [fleet]
      operationId: correlateFleetEngagementDetections
      summary: Fold an engagement's sealed detections into incidents
      description: >
        No request body: the detections are read server-side and the actor is the authenticated
        principal. Idempotent, because the correlator mints deterministic incident ids, so a re-run
        over the same detections creates nothing new. Tri-scoring each new incident is best-effort
        when the reassessor is wired: a scoring failure never rolls back a durably recorded incident,
        it is counted in reassess_failed so a caller sees that scoring was partial.
      security:
      - bearerAuth: []
      responses:
        '200':
          description: The incidents created by this pass and how many were scored
          content:
            application/json:
              schema: {$ref: '#/components/schemas/FleetCorrelationResult'}
        '400': {$ref: '#/components/responses/BadRequest'}
        '401': {$ref: '#/components/responses/Unauthorized'}
        '403': {$ref: '#/components/responses/Forbidden'}
        '500': {$ref: '#/components/responses/InternalServerError'}

  /api/v1/fleet/incidents/{id}/risk/reassess:
    parameters: [{$ref: '#/components/parameters/FleetIncidentId'}]
    post:
      tags: [fleet]
      operationId: reassessFleetIncidentRisk
      summary: Re-run the tri-score risk assessment for one incident
      description: >
        No request body: the Threat, Exposure, Behaviour and telemetry Coverage factors are gathered
        server-side and the actor is the authenticated principal. The deterministic scorer records a
        RiskAssessment as an event on the append-only incident log and the updated projection is
        returned. Risk, Confidence and Coverage stay separate fields: missing coverage lowers
        Coverage, never Risk.
      security:
      - bearerAuth: []
      responses:
        '200':
          description: The incident projection with its new risk assessment
          content:
            application/json:
              schema: {$ref: '#/components/schemas/FleetIncident'}
        '400': {$ref: '#/components/responses/BadRequest'}
        '401': {$ref: '#/components/responses/Unauthorized'}
        '403': {$ref: '#/components/responses/Forbidden'}
        '404': {$ref: '#/components/responses/NotFound'}
        '409': {description: Pipeline already bound, content: {application/json: {schema: {$ref: '#/components/schemas/Error'}}}}

  /api/v1/integrations/{id}/bindings/{bindingID}:
    parameters:
    - $ref: '#/components/parameters/IntegrationId'
    - $ref: '#/components/parameters/IntegrationBindingId'
    delete:
      tags: [integrations]
      operationId: deleteIntegrationBinding
      summary: Delete a Project binding
      responses:
        '204': {description: Binding deleted}
        '401': {$ref: '#/components/responses/Unauthorized'}
        '403': {$ref: '#/components/responses/Forbidden'}
        '404': {$ref: '#/components/responses/NotFound'}

  /api/v1/integrations/{id}/external-runs:
    parameters: [{$ref: '#/components/parameters/IntegrationId'}]
    get:
      tags: [integrations]
      operationId: listIntegrationExternalRuns
      summary: List normalized external CI/CD runs and correlation state
      parameters:
      - name: limit
        in: query
        schema: {type: integer, minimum: 1, maximum: 500, default: 100}
      responses:
        '200': {description: External runs, content: {application/json: {schema: {type: array, items: {$ref: '#/components/schemas/IntegrationExternalRun'}}}}}
        '401': {$ref: '#/components/responses/Unauthorized'}
        '403': {$ref: '#/components/responses/Forbidden'}
        '404': {$ref: '#/components/responses/NotFound'}
        '500': {$ref: '#/components/responses/InternalServerError'}

  /api/v1/users:
    get:
      tags: [users]
      summary: List the operators of the caller's own tenant
      description: >
        Requires the `administer` capability (admin role). The roster is scoped to the caller's
        tenant; no caller lists another tenant's operators. The API-key hash is never serialized.
      responses:
        "200":
          description: The tenant's operators
          content:
            application/json:
              schema:
                type: array
                items: {$ref: "#/components/schemas/User"}
        "401": {$ref: "#/components/responses/Unauthorized"}
        "403": {$ref: "#/components/responses/Forbidden"}
    post:
      tags: [users]
      summary: Provision an operator and return its API key once
      description: >
        Requires the `administer` capability. The key is shown exactly once and is not recoverable.
        `tenant_id` defaults to the caller's own tenant; a different tenant is refused with `403`
        unless the caller is the bootstrap platform principal, which is the only identity allowed to
        seed another tenant's first admin.
      requestBody:
        required: true
        content:
          application/json:
            schema: {$ref: "#/components/schemas/CreateUserRequest"}
      responses:
        "201":
          description: Created; the API key is returned once
          content:
            application/json:
              schema: {$ref: "#/components/schemas/UserWithAPIKey"}
        "400": {$ref: "#/components/responses/BadRequest"}
        "401": {$ref: "#/components/responses/Unauthorized"}
        "403": {description: Missing the administer capability, or the requested tenant is not the caller's own}

  /api/v1/users/{id}:
    parameters:
    - name: id
      in: path
      required: true
      schema: {type: string}
    patch:
      tags: [users]
      summary: Update an operator's name and role
      description: >
        Requires the `administer` capability. An omitted or empty field is left unchanged. Demoting
        the tenant's last enabled admin is refused with `409`, so a tenant cannot be locked out.
        Deleting an operator is deliberately not offered: an identity owns its audit, evidence, and
        finding attribution, so access is revoked by disabling the account or rotating its key.
      requestBody:
        required: true
        content:
          application/json:
            schema: {$ref: "#/components/schemas/UpdateUserRequest"}
      responses:
        "200":
          description: The updated operator
          content:
            application/json:
              schema: {$ref: "#/components/schemas/User"}
        "400": {$ref: "#/components/responses/BadRequest"}
        "401": {$ref: "#/components/responses/Unauthorized"}
        "403": {$ref: "#/components/responses/Forbidden"}
        "404": {$ref: "#/components/responses/NotFound"}
        "409": {description: The change would leave the tenant with no enabled admin}

  /api/v1/users/{id}/disable:
    parameters:
    - name: id
      in: path
      required: true
      schema: {type: string}
    post:
      tags: [users]
      summary: Disable an operator's credentials
      description: >
        Requires the `administer` capability. The identity and its attribution are preserved; only
        authentication stops, immediately, for the operator's API key. Disabling the tenant's last
        enabled admin is refused with `409`.
      responses:
        "200":
          description: The disabled operator
          content:
            application/json:
              schema: {$ref: "#/components/schemas/User"}
        "401": {$ref: "#/components/responses/Unauthorized"}
        "403": {$ref: "#/components/responses/Forbidden"}
        "404": {$ref: "#/components/responses/NotFound"}
        "409": {description: The operator is the tenant's last enabled admin}

  /api/v1/users/{id}/enable:
    parameters:
    - name: id
      in: path
      required: true
      schema: {type: string}
    post:
      tags: [users]
      summary: Re-enable a disabled operator
      description: Requires the `administer` capability. The operator keeps the API key it had.
      responses:
        "200":
          description: The re-enabled operator
          content:
            application/json:
              schema: {$ref: "#/components/schemas/User"}
        "401": {$ref: "#/components/responses/Unauthorized"}
        "403": {$ref: "#/components/responses/Forbidden"}
        "404": {$ref: "#/components/responses/NotFound"}

  /api/v1/users/{id}/rotate-key:
    parameters:
    - name: id
      in: path
      required: true
      schema: {type: string}
    post:
      tags: [users]
      summary: Rotate an operator's API key and return the new key once
      description: >
        Requires the `administer` capability. The previous key stops authenticating immediately,
        which is how a leaked key is revoked. The new key is shown exactly once.
      responses:
        "200":
          description: The operator and its new API key
          content:
            application/json:
              schema: {$ref: "#/components/schemas/UserWithAPIKey"}
        "401": {$ref: "#/components/responses/Unauthorized"}
        "403": {$ref: "#/components/responses/Forbidden"}
        "404": {$ref: "#/components/responses/NotFound"}

components:
  securitySchemes:
    bearerAuth:
      type: http
      scheme: bearer
      description: Human/operator API token.
    fleetAgentAuth:
      type: http
      scheme: bearer
      description: Enrolled fleet-agent bearer credential. A deployment may instead authenticate through its configured trusted mTLS proxy path.

  parameters:
    IntegrationId:
      name: id
      in: path
      required: true
      schema: {type: string}
    IntegrationOperationId:
      name: operationID
      in: path
      required: true
      schema: {type: string}
    IntegrationBindingId:
      name: bindingID
      in: path
      required: true
      schema: {type: string}
    FleetProtocolVersion:
      name: X-Synapse-Fleet-Proto
      in: header
      required: true
      description: Fleet transport protocol version. Only version 1 is accepted; unsupported values fail closed.
      schema: {type: string, const: "1"}
    EngagementId:
      name: id
      in: path
      required: true
      schema: {type: string}
    DetectionId:
      name: did
      in: path
      required: true
      schema: {type: string, minLength: 1}
    FindingId:
      name: fid
      in: path
      required: true
      schema: {type: string, minLength: 1}
    AgentId:
      name: id
      in: path
      required: true
      schema: {type: string, minLength: 1}
    FleetAssetId:
      name: id
      in: path
      required: true
      description: Canonical host or cluster asset identifier
      schema: {type: string, minLength: 1}
    FleetIncidentId:
      name: id
      in: path
      required: true
      description: Phase-C incident identifier
      schema: {type: string, minLength: 1}
    SigningKeyId:
      name: keyID
      in: path
      required: true
      schema: {type: string, minLength: 1}
    BusinessAssetId:
      name: assetID
      in: path
      required: true
      schema: {type: string, minLength: 1}
    HostAssetId:
      name: assetID
      in: path
      required: true
      description: Server-issued id of a Kind=host fleet asset.
      schema: {type: string, minLength: 1}
    AITriageReviewId:
      name: rid
      in: path
      required: true
      schema: {type: string, minLength: 1}
    ProjectKey:
      name: key
      in: path
      required: true
      description: Unique identifier of the project
      schema:
        type: string
        pattern: ^[a-z0-9_-]+$
    AnalysisId:
      name: id
      in: path
      required: true
      description: Immutable Project analysis identifier
      schema: {type: string, minLength: 1}
    LensParam:
      name: lens
      in: query
      description: Filter by lens (overall or new-code)
      schema:
        type: string
        enum:
        - overall
        - new-code
    VulnerabilityAdvisoryId:
      name: id
      in: path
      required: true
      schema: {type: string, minLength: 1}
    VulnerabilityOccurrenceId:
      name: id
      in: path
      required: true
      schema: {type: string, minLength: 1}
    VulnerabilityRunId:
      name: id
      in: path
      required: true
      schema: {type: string, minLength: 1}
    VulnerabilitySourceId:
      name: id
      in: path
      required: true
      schema: {type: string, minLength: 1}
    VulnerabilityLimit:
      name: limit
      in: query
      schema: {type: integer, minimum: 1, maximum: 500, default: 100}
    VulnerabilityCursorTime:
      name: before_time
      in: query
      schema: {type: string, format: date-time}
      description: RFC 3339 timestamp; must be supplied together with `before_id`
    VulnerabilityCursorId:
      name: before_id
      in: query
      schema: {type: string}
      description: Stable tie-breaker; must be supplied together with `before_time`

  responses:
    BadRequest:
      description: Invalid request
      content:
        application/json:
          schema: {$ref: "#/components/schemas/Error"}
    Unauthorized:
      description: Missing or invalid bearer token
      content:
        application/json:
          schema: {$ref: "#/components/schemas/Error"}
    Forbidden:
      description: AUP not accepted, or target outside scope / authorization window
      content:
        application/json:
          schema: {$ref: "#/components/schemas/Error"}
    NotFound:
      description: Resource not found
      content:
        application/json:
          schema: {$ref: "#/components/schemas/Error"}
    Conflict:
      description: Request conflicts with immutable or current resource state
      content:
        application/json:
          schema: {$ref: "#/components/schemas/Error"}
    InternalServerError:
      description: Internal server error
      content:
        application/json:
          schema: {$ref: "#/components/schemas/Error"}

  schemas:
    IntegrationFieldDescriptor:
      type: object
      additionalProperties: false
      required: [name, label, kind, required]
      properties:
        name: {type: string, pattern: '^[a-z][a-z0-9_]*$'}
        label: {type: string}
        kind: {type: string, enum: [text, password, boolean]}
        required: {type: boolean}
        description: {type: string}

    IntegrationProviderDescriptor:
      type: object
      additionalProperties: false
      required: [provider, name, description, capabilities, config_fields, secret_fields]
      properties:
        provider: {type: string, pattern: '^[a-z][a-z0-9]*(?:-[a-z0-9]+)*$'}
        name: {type: string}
        description: {type: string}
        capabilities: {type: array, uniqueItems: true, items: {type: string, enum: [test_connection, discover_pipelines, read_runs]}}
        config_fields: {type: array, items: {$ref: '#/components/schemas/IntegrationFieldDescriptor'}}
        secret_fields: {type: array, items: {$ref: '#/components/schemas/IntegrationFieldDescriptor'}}

    Integration:
      type: object
      additionalProperties: false
      required: [id, provider, name, endpoint, config, allow_private_network, poll_interval_seconds, enabled, archived, version, connection_revision, credential_revision, credential_configured, created_at, updated_at]
      properties:
        id: {type: string}
        provider: {type: string}
        name: {type: string, maxLength: 120}
        endpoint: {type: string, format: uri, pattern: '^https://'}
        config: {type: object, additionalProperties: true}
        allow_private_network: {type: boolean}
        poll_interval_seconds: {type: integer, minimum: 30, maximum: 86400}
        enabled: {type: boolean}
        archived: {type: boolean}
        version: {type: integer, minimum: 1}
        connection_revision: {type: integer, minimum: 1}
        credential_revision: {type: integer, minimum: 0}
        credential_configured: {type: boolean, description: Presence only; plaintext is never returned.}
        created_at: {type: string, format: date-time}
        updated_at: {type: string, format: date-time}

    CreateIntegrationRequest:
      type: object
      additionalProperties: false
      required: [provider, name, endpoint, config, allow_private_network, poll_interval_seconds]
      properties:
        provider: {type: string, pattern: '^[a-z][a-z0-9]*(?:-[a-z0-9]+)*$'}
        name: {type: string, minLength: 1, maxLength: 120}
        endpoint: {type: string, format: uri, pattern: '^https://'}
        config: {type: object, additionalProperties: true}
        allow_private_network: {type: boolean, default: false}
        poll_interval_seconds: {type: integer, minimum: 30, maximum: 86400, default: 300}

    UpdateIntegrationRequest:
      type: object
      additionalProperties: false
      required: [name, endpoint, config, allow_private_network, poll_interval_seconds, version]
      properties:
        name: {type: string, minLength: 1, maxLength: 120}
        endpoint: {type: string, format: uri, pattern: '^https://'}
        config: {type: object, additionalProperties: true}
        allow_private_network: {type: boolean, default: false}
        poll_interval_seconds: {type: integer, minimum: 30, maximum: 86400, default: 300}
        version: {type: integer, minimum: 1}

    IntegrationVersionRequest:
      type: object
      additionalProperties: false
      required: [version]
      properties:
        version: {type: integer, minimum: 1}

    IntegrationRevisionRequest:
      type: object
      additionalProperties: false
      required: [version, connection_revision]
      properties:
        version: {type: integer, minimum: 1}
        connection_revision: {type: integer, minimum: 1}

    IntegrationCredentialRequest:
      type: object
      additionalProperties: false
      required: [secrets, version, connection_revision]
      properties:
        version: {type: integer, minimum: 1}
        connection_revision: {type: integer, minimum: 1}
        secrets:
          type: object
          minProperties: 1
          maxProperties: 32
          additionalProperties: {type: string, maxLength: 16384, writeOnly: true}

    StartIntegrationOperationRequest:
      type: object
      additionalProperties: false
      required: [type]
      properties:
        type: {type: string, enum: [test, discover, poll]}

    IntegrationPipeline:
      type: object
      additionalProperties: false
      required: [external_key, name, full_name, kind]
      properties:
        external_key: {type: string, maxLength: 1024}
        name: {type: string, maxLength: 255}
        full_name: {type: string, maxLength: 1024}
        kind: {type: string, maxLength: 64}
        url: {type: string, format: uri, maxLength: 2048}

    IntegrationOperationCounts:
      type: object
      additionalProperties: false
      required: [pipelines, runs, linked, unlinked, errors]
      properties:
        pipelines: {type: integer, minimum: 0}
        runs: {type: integer, minimum: 0}
        linked: {type: integer, minimum: 0}
        unlinked: {type: integer, minimum: 0}
        errors: {type: integer, minimum: 0}

    IntegrationOperation:
      type: object
      additionalProperties: false
      required: [id, tenant_id, integration_id, type, state, counts, errors, job_id, actor, created_at, updated_at]
      properties:
        id: {type: string}
        tenant_id: {type: string}
        integration_id: {type: string}
        type: {type: string, enum: [test, discover, poll]}
        state: {type: string, enum: [queued, running, succeeded, partial, failed, cancelled]}
        checkpoint: {type: string, maxLength: 65536}
        counts: {$ref: '#/components/schemas/IntegrationOperationCounts'}
        errors: {type: array, maxItems: 20, items: {type: string, maxLength: 256}}
        pipelines: {type: array, maxItems: 1000, items: {$ref: '#/components/schemas/IntegrationPipeline'}}
        job_id: {type: string}
        actor: {type: string}
        started_at: {type: [string, 'null'], format: date-time}
        finished_at: {type: [string, 'null'], format: date-time}
        created_at: {type: string, format: date-time}
        updated_at: {type: string, format: date-time}

    CreateIntegrationBindingRequest:
      type: object
      additionalProperties: false
      required: [project_id, external_key, external_name]
      properties:
        project_id: {type: string}
        external_key: {type: string, minLength: 1, maxLength: 1024}
        external_name: {type: string, minLength: 1, maxLength: 1024}

    IntegrationBinding:
      type: object
      additionalProperties: false
      required: [id, tenant_id, integration_id, project_id, external_key, external_name, version, created_at, updated_at]
      properties:
        id: {type: string}
        tenant_id: {type: string}
        integration_id: {type: string}
        project_id: {type: string}
        external_key: {type: string}
        external_name: {type: string}
        version: {type: integer, minimum: 1}
        created_at: {type: string, format: date-time}
        updated_at: {type: string, format: date-time}

    IntegrationExternalRun:
      type: object
      additionalProperties: false
      required: [id, tenant_id, integration_id, provider_key, pipeline_key, lifecycle, result, correlation, provider_updated_at, created_at, updated_at]
      properties:
        id: {type: string}
        tenant_id: {type: string}
        integration_id: {type: string}
        binding_id: {type: string}
        provider_key: {type: string, maxLength: 1024}
        pipeline_key: {type: string, maxLength: 1024}
        number: {type: string, maxLength: 128}
        url: {type: string, format: uri, maxLength: 2048}
        lifecycle: {type: string, enum: [queued, running, completed]}
        result: {type: string, enum: [success, failure, unstable, aborted, not_built, unknown]}
        revision: {type: string, maxLength: 128}
        branch: {type: string, maxLength: 512}
        analysis_id: {type: string}
        correlation: {type: string, enum: [linked, missing, ambiguous]}
        queued_at: {type: [string, 'null'], format: date-time}
        started_at: {type: [string, 'null'], format: date-time}
        finished_at: {type: [string, 'null'], format: date-time}
        provider_updated_at: {type: string, format: date-time}
        created_at: {type: string, format: date-time}
        updated_at: {type: string, format: date-time}

    DetectionProvenanceStatus:
      type: string
      enum: [pending, complete, expired, broken]

    DetectionProvenanceCurrent:
      type: object
      additionalProperties: false
      required: [tenant_id, engagement_id, detection_id, status, updated_at]
      properties:
        tenant_id: {type: string, minLength: 1}
        engagement_id: {type: string, minLength: 1}
        detection_id: {type: string, minLength: 1}
        status: {$ref: '#/components/schemas/DetectionProvenanceStatus'}
        evidence_id: {type: string, minLength: 1}
        updated_at: {type: string, format: date-time}

    DetectionProvenanceList:
      type: object
      additionalProperties: false
      required: [provenance]
      properties:
        provenance:
          type: array
          items: {$ref: '#/components/schemas/DetectionProvenanceCurrent'}

    DetectionProvenanceTransitionKind:
      type: string
      enum: [received, telemetry_durable, commitment_pending, commitment_sealed, acknowledged, expired, broken]

    TelemetryReference:
      type: object
      additionalProperties: false
      required: [stream_id, epoch, sequence, event_id, digest]
      properties:
        stream_id: {type: string, minLength: 1}
        epoch: {type: integer, format: uint64, minimum: 1}
        sequence: {type: integer, format: uint64, minimum: 1}
        event_id: {type: string, minLength: 1}
        digest: {type: string, pattern: '^[0-9a-f]{64}$'}

    DetectionProvenanceTransition:
      type: object
      additionalProperties: false
      required: [tenant_id, engagement_id, detection_id, sequence, kind, status, occurred_at]
      properties:
        tenant_id: {type: string, minLength: 1}
        engagement_id: {type: string, minLength: 1}
        detection_id: {type: string, minLength: 1}
        sequence: {type: integer, format: uint64, minimum: 1}
        kind: {$ref: '#/components/schemas/DetectionProvenanceTransitionKind'}
        status: {$ref: '#/components/schemas/DetectionProvenanceStatus'}
        evidence_id: {type: string, minLength: 1}
        telemetry_refs:
          type: array
          items: {$ref: '#/components/schemas/TelemetryReference'}
        agent_id: {type: string, minLength: 1}
        asset_id: {type: string, minLength: 1}
        reason: {type: string}
        occurred_at: {type: string, format: date-time}

    DetectionProvenanceTransitionList:
      type: object
      additionalProperties: false
      required: [transitions]
      properties:
        transitions:
          type: array
          items: {$ref: '#/components/schemas/DetectionProvenanceTransition'}

    CoverageClassState:
      type: object
      additionalProperties: false
      required: [class, host_id, agent_id, state, since]
      properties:
        class: {type: string, enum: [process, network, file, privilege]}
        host_id: {type: string, minLength: 1}
        agent_id: {type: string, minLength: 1}
        state: {type: string, enum: [active, degraded, failed, disabled]}
        reason: {type: string}
        since: {type: string, format: date-time}

    CoverageVector:
      type: object
      additionalProperties: false
      required: [process, network, file, privilege, reasons]
      properties:
        process: {type: integer, minimum: 0, maximum: 100}
        network: {type: integer, minimum: 0, maximum: 100}
        file: {type: integer, minimum: 0, maximum: 100}
        privilege: {type: integer, minimum: 0, maximum: 100}
        reasons:
          type: array
          items: {type: string}

    PrivacyPolicy:
      type: object
      additionalProperties: false
      required: [dispositions, redact_secrets, max_arg_len, max_arg_count, max_path_len, version]
      properties:
        dispositions:
          type: object
          description: Closed-set source field categories mapped to allow, redact, hash, or drop. Omitted categories use the server-defined allow behavior subject to the non-relaxable source-privacy floor.
          propertyNames:
            enum: [process_arg, process_path, process_comm, process_env, file_path, file_comm, network_comm, privilege_comm]
          additionalProperties:
            type: string
            enum: [allow, redact, hash, drop]
        redact_secrets: {type: boolean, description: Scrub recognized secret patterns before telemetry WAL serialization}
        max_arg_len: {type: integer, minimum: 0, description: Maximum retained argument length; zero means unbounded subject to the source-privacy floor}
        max_arg_count: {type: integer, minimum: 0, description: Maximum retained argument count; zero means unbounded subject to the source-privacy floor}
        max_path_len: {type: integer, minimum: 0, description: Maximum retained path length; zero means unbounded subject to the source-privacy floor}
        hash_salt: {type: string, description: Sensitive. Salt for hash-disposition pseudonymization; required on admission when any category uses hash. Returned ONLY on the agent-authenticated policy route, because agents hash at the source. The human read routes (active pointer, history, admission echo) always omit it, since a principal who can read hashed telemetry and also learn the salt could de-anonymize it offline. Identify a policy by its digest instead.}
        version: {type: string, minLength: 1, description: Immutable policy-version alias; not part of the redaction behavior digest}

    PrivacyPolicyAssignment:
      type: object
      additionalProperties: false
      required: [tenant_id, policy, digest, created_by, created_at]
      properties:
        tenant_id: {type: string, minLength: 1}
        policy: {$ref: '#/components/schemas/PrivacyPolicy'}
        digest: {type: string, pattern: '^[0-9a-f]{64}$', description: Immutable assignment digest}
        created_by: {type: string, minLength: 1}
        created_at: {type: string, format: date-time}

    PrivacyPolicyAdmissionRequest:
      type: object
      additionalProperties: false
      required: [policy]
      properties:
        policy: {$ref: '#/components/schemas/PrivacyPolicy'}

    PrivacyPolicyAdmissionResponse:
      type: object
      additionalProperties: false
      required: [assignment, created]
      properties:
        assignment: {$ref: '#/components/schemas/PrivacyPolicyAssignment'}
        created: {type: boolean}

    PrivacyPolicyAssignmentEnvelope:
      type: object
      additionalProperties: false
      required: [assignment]
      properties:
        assignment: {$ref: '#/components/schemas/PrivacyPolicyAssignment'}

    PrivacyPolicyHistoryResponse:
      type: object
      additionalProperties: false
      required: [assignments]
      properties:
        assignments:
          type: array
          items: {$ref: '#/components/schemas/PrivacyPolicyAssignment'}

    PrivacyPolicyActivationRequest:
      type: object
      additionalProperties: false
      required: [digest, operation_id]
      properties:
        digest: {type: string, pattern: '^[0-9a-f]{64}$'}
        operation_id: {type: string, minLength: 1, description: Stable idempotency identity for this activation transition}

    CoverageWindow:
      type: object
      additionalProperties: false
      required: [asset_id, agent_id, host_id, since, until, input_digest, revision, created_at, states, sampled_count, truncated_count, dropped_count, gap_count, batch_count, coverage]
      properties:
        asset_id: {type: string, minLength: 1}
        agent_id: {type: string, minLength: 1}
        host_id: {type: string, minLength: 1}
        since: {type: string, format: date-time, description: Inclusive coverage-window lower bound}
        until: {type: string, format: date-time, description: Exclusive coverage-window upper bound}
        input_digest: {type: string, pattern: '^[0-9a-f]{64}$'}
        revision: {type: string, pattern: '^[0-9a-f]{64}$'}
        created_at: {type: string, format: date-time}
        states:
          type: array
          items: {$ref: '#/components/schemas/CoverageClassState'}
        sampled_count: {type: integer, minimum: 0}
        truncated_count: {type: integer, minimum: 0}
        dropped_count: {type: integer, minimum: 0}
        gap_count: {type: integer, minimum: 0}
        batch_count: {type: integer, minimum: 0}
        coverage: {$ref: '#/components/schemas/CoverageVector'}

    CoverageWindowList:
      type: object
      additionalProperties: false
      required: [coverage_windows]
      properties:
        coverage_windows:
          type: array
          items: {$ref: '#/components/schemas/CoverageWindow'}

    SensorClassCoverage:
      type: object
      additionalProperties: false
      required: [Class, HostID, AgentID, State, Since]
      properties:
        Class: {type: string, enum: [process, network, file, privilege]}
        HostID: {type: string, minLength: 1}
        AgentID: {type: string, minLength: 1}
        State: {type: string, enum: [active, degraded, failed, disabled]}
        Reason: {type: string}
        Since: {type: string, format: date-time}

    SensorStateReport:
      type: object
      additionalProperties: false
      required: [ProtocolVersion, ReportID, AgentID, HostID, AgentSessionID, AssetID, Kind, ObservedAt, SchemaVersion, PayloadDigest, States, KeyID, Signature]
      properties:
        ProtocolVersion: {type: integer, const: 1}
        ReportID: {type: string, minLength: 1}
        AgentID: {type: string, minLength: 1, description: Must equal the authenticated fleet-agent identity}
        HostID: {type: string, minLength: 1, description: Must equal the enrolled agent host identity}
        AgentSessionID: {type: string, minLength: 1, description: Must equal the current enrollment session}
        AssetID: {type: string, minLength: 1, description: Must equal the server-authoritative asset binding}
        Kind: {type: string, enum: [coverage, sensor_state]}
        ObservedAt: {type: string, format: date-time}
        SchemaVersion: {type: integer, minimum: 1}
        PayloadDigest: {type: string, pattern: '^[0-9a-f]{64}$'}
        States:
          type: array
          minItems: 1
          items: {$ref: '#/components/schemas/SensorClassCoverage'}
        KeyID: {type: string, minLength: 1}
        Signature: {type: string, contentEncoding: base64}

    SensorStateAcknowledgement:
      type: object
      additionalProperties: false
      required: [acknowledged, report_id]
      properties:
        acknowledged: {type: boolean, const: true}
        report_id: {type: string, minLength: 1}

    AgentSigningKeyRegistration:
      type: object
      additionalProperties: false
      required: [public_key, purpose, not_before, not_after, proof]
      properties:
        public_key: {type: string, contentEncoding: base64, description: Raw 32-byte Ed25519 public key}
        purpose: {type: string, enum: [telemetry-batch, detection-batch, response-result]}
        not_before: {type: string, format: date-time}
        not_after: {type: string, format: date-time}
        proof: {type: string, contentEncoding: base64, description: Ed25519 signature over the purpose-bound key binding message}

    AgentSigningKeyRegistrationResult:
      type: object
      required: [key_id, agent_id, purpose, not_before, not_after]
      properties:
        key_id: {type: string}
        agent_id: {type: string}
        purpose: {type: string, enum: [telemetry-batch, detection-batch, response-result]}
        not_before: {type: string, format: date-time}
        not_after: {type: string, format: date-time}

    AgentSigningKeySummary:
      type: object
      required: [key_id, purpose, algorithm, not_before, not_after, revoked, replaced_by]
      properties:
        key_id: {type: string}
        purpose: {type: string, enum: [telemetry-batch, detection-batch, response-result]}
        algorithm: {type: string, const: ed25519}
        not_before: {type: string, format: date-time}
        not_after: {type: string, format: date-time}
        revoked: {type: boolean}
        replaced_by: {type: string}

    AgentSigningKeyList:
      type: object
      required: [agent_id, keys]
      properties:
        agent_id: {type: string}
        keys:
          type: array
          items: {$ref: '#/components/schemas/AgentSigningKeySummary'}

    DetectionBatchRefV2:
      type: object
      additionalProperties: false
      required: [id, content_sha256, asset_id, telemetry_refs, rulepack, redaction_policy_digest]
      properties:
        id: {type: string, minLength: 1}
        content_sha256: {type: string, pattern: '^[0-9a-f]{64}$'}
        asset_id: {type: string, minLength: 1}
        telemetry_refs:
          type: array
          minItems: 1
          items: {$ref: '#/components/schemas/TelemetryReference'}
        rulepack: {$ref: '#/components/schemas/RulepackReference'}
        redaction_policy_digest: {type: string, pattern: '^[0-9a-f]{64}$'}

    RulepackReference:
      type: object
      additionalProperties: false
      required: [id, version, digest]
      properties:
        id: {type: string, minLength: 1}
        version: {type: integer, minimum: 1}
        digest: {type: string, pattern: '^[0-9a-f]{64}$'}

    AgentDetectionBatchV2:
      type: object
      additionalProperties: false
      required: [context, version, agent_id, engagement_id, sequence, key_id, signature, detections]
      properties:
        context: {type: string, const: 'synapse-agent-detection-batch:v2'}
        version: {type: integer, const: 2}
        agent_id: {type: string, description: Must equal the authenticated agent identity}
        engagement_id: {type: string, minLength: 1}
        sequence: {type: integer, format: uint64, minimum: 1}
        key_id: {type: string, minLength: 1}
        signature: {type: string, contentEncoding: base64}
        detections:
          type: array
          minItems: 1
          items: {$ref: '#/components/schemas/DetectionBatchRefV2'}

    DetectionBatchItemV2:
      type: object
      additionalProperties: false
      required: [id, detection, asset_id, telemetry_refs, rulepack, redaction_policy_digest]
      properties:
        id: {type: string, minLength: 1}
        detection:
          type: object
          description: Typed detection.Detection body whose canonical attribution must match its signed V2 reference.
          additionalProperties: true
        asset_id: {type: string, minLength: 1}
        telemetry_refs:
          type: array
          minItems: 1
          items: {$ref: '#/components/schemas/TelemetryReference'}
        rulepack: {$ref: '#/components/schemas/RulepackReference'}
        redaction_policy_digest: {type: string, pattern: '^[0-9a-f]{64}$'}

    AgentDetectionBatchRequest:
      type: object
      additionalProperties: false
      required: [batch_v2, items_v2]
      properties:
        batch_v2: {$ref: '#/components/schemas/AgentDetectionBatchV2'}
        items_v2:
          type: array
          minItems: 1
          items: {$ref: '#/components/schemas/DetectionBatchItemV2'}

    AgentDetectionBatchResult:
      type: object
      required: [engagement_id, sealed, skipped, missing, replay]
      properties:
        engagement_id: {type: string}
        sealed: {type: integer, minimum: 0}
        skipped: {type: integer, minimum: 0}
        missing: {type: integer, format: uint64, minimum: 0}
        replay: {type: boolean}

    SetFleetDesiredCapabilitiesRequest:
      type: object
      required: [capabilities]
      properties:
        capabilities:
          type: array
          description: >
            Capability names the asset should have. At most 256 raw entries are accepted; blank
            entries are dropped and the rest are trimmed, de-duplicated and sorted, after which at
            most 64 distinct names may remain. An empty canonical result is rejected: clear the
            intent with DELETE instead.
          maxItems: 256
          items: {type: string, maxLength: 128}

    FleetDesiredCapabilityState:
      type: object
      description: Serialized from the domain struct (PascalCase keys).
      required: [TenantID, AssetID, PolicyID, Capabilities, UpdatedBy, Version, Audit]
      properties:
        TenantID: {type: string}
        AssetID: {type: string, description: The canonical host or cluster asset the intent is declared for.}
        PolicyID: {type: string, description: Stable for the life of one policy; clearing and recreating mints a new one.}
        Capabilities:
          type: array
          description: Canonical form; trimmed, unique and sorted.
          maxItems: 64
          items: {type: string, minLength: 1, maxLength: 128}
        UpdatedBy: {type: string}
        Version: {type: integer, format: int64, minimum: 1, description: Starts at 1 and increments once per semantic replacement within the PolicyID lifecycle.}
        Audit:
          type: object
          properties:
            CreatedAt: {type: string, format: date-time}
            UpdatedAt: {type: string, format: date-time}
            CreatedBy: {type: string}
            UpdatedBy: {type: string}

    FleetAgentHealth:
      type: string
      description: Derived liveness of a fleet agent. Stale is first-class; an idle agent is never reported as healthy.
      enum: [healthy, stale, revoked, decommissioned]

    FleetDesiredCapabilityGapReason:
      type: string
      description: Closed set naming why a desired capability is not covered by a healthy bound agent.
      enum: [agent_missing, agent_stale, agent_revoked, agent_decommissioned, capability_missing]

    FleetDesiredCapabilityGap:
      type: object
      required: [asset_id, policy_id, policy_version, capability, covered, last_seen]
      properties:
        asset_id: {type: string}
        policy_id: {type: string}
        policy_version: {type: integer, format: int64, minimum: 1}
        agent_id:
          type: string
          description: >
            The witness that satisfied the capability, or the best available representative when it is
            uncovered. Absent when no current agent is bound to the asset.
        capability: {type: string}
        agent_health: {$ref: '#/components/schemas/FleetAgentHealth'}
        covered: {type: boolean}
        gap_reason: {$ref: '#/components/schemas/FleetDesiredCapabilityGapReason'}
        detail: {type: string, description: Human-readable explanation of the gap.}
        last_seen:
          type: string
          format: date-time
          description: Last heartbeat of the representative agent; the zero time when no observed agent row backs the row.

    FleetDesiredCapabilityGapList:
      type: object
      required: [gaps]
      properties:
        gaps: {type: array, items: {$ref: '#/components/schemas/FleetDesiredCapabilityGap'}}

    ReportFleetEndpointProcessesRequest:
      type: object
      required: [processes]
      properties:
        processes:
          type: array
          maxItems: 10000
          items:
            type: object
            required: [entity_id]
            properties:
              entity_id: {type: string, minLength: 1, description: Stable endpoint entity id; the upsert key together with the server-side tenant and asset.}
              pid: {type: integer, minimum: 0}
              comm: {type: string, description: Short kernel command name.}
              path: {type: string, description: Resolved executable path.}
              running: {type: boolean, description: False marks a process that has exited; the row is kept and flipped in place.}
              last_seen_at: {type: string, format: date-time}

    ReportFleetEndpointProcessesResult:
      type: object
      required: [saved]
      properties:
        saved: {type: integer, minimum: 0, description: Number of snapshots upserted.}

    FleetEndpointProcess:
      type: object
      description: Serialized from the port struct (PascalCase keys).
      required: [TenantID, AssetID, EntityID, PID, Comm, Path, Running, LastSeenAt]
      properties:
        TenantID: {type: string}
        AssetID: {type: string}
        EntityID: {type: string}
        PID: {type: integer, minimum: 0}
        Comm: {type: string}
        Path: {type: string}
        Running: {type: boolean}
        LastSeenAt: {type: string, format: date-time}

    FleetEndpointProcessList:
      type: object
      required: [processes]
      properties:
        processes: {type: array, items: {$ref: '#/components/schemas/FleetEndpointProcess'}}

    FleetEndpointEntityKind:
      type: string
      enum: [process, network, file, identity, container]

    FleetEndpointTimelineEntryKind:
      type: string
      enum: [process_start, process_exec, network_connect, file_open, file_write, file_rename, privilege_change]

    FleetEndpointTimelineEntry:
      type: object
      description: Serialized from the domain struct (PascalCase keys). One endpoint state transition, ordered by event time.
      required: [OccurredAt, TenantID, AssetID, EntityKind, EntityID, Kind, EventID, Summary]
      properties:
        OccurredAt: {type: string, format: date-time, description: When the transition happened on the host, never the ingest time.}
        TenantID: {type: string}
        AssetID: {type: string}
        EntityKind: {$ref: '#/components/schemas/FleetEndpointEntityKind'}
        EntityID: {type: string}
        Kind: {$ref: '#/components/schemas/FleetEndpointTimelineEntryKind'}
        EventID: {type: string, description: Source envelope event id; the dedupe key and the tiebreak for transitions sharing OccurredAt.}
        Summary: {type: string, description: Self-contained description, so the entry stays explainable after the raw telemetry expires.}

    FleetEndpointTimelineList:
      type: object
      required: [entries]
      properties:
        entries: {type: array, items: {$ref: '#/components/schemas/FleetEndpointTimelineEntry'}}

    FleetRetroHuntRequest:
      type: object
      required: [around]
      properties:
        around: {type: string, format: date-time, description: The pivot time to hunt around. Required.}
        before_seconds: {type: integer, format: int64, minimum: 0, default: 0, description: Look-back from the pivot. At least one of before_seconds and after_seconds must be positive.}
        after_seconds: {type: integer, format: int64, minimum: 0, default: 0, description: Look-forward from the pivot. At least one of before_seconds and after_seconds must be positive.}
        entity_id: {type: string, description: Optionally focus the hunt on one entity's transitions.}
        limit: {type: integer, minimum: 0, default: 0, description: Caps the entries returned. Zero or negative applies the default bound of 1000.}

    FleetRetroHuntResult:
      type: object
      description: Serialized from the usecase struct (PascalCase keys).
      required: [AssetID, From, To, Entries, Truncated]
      properties:
        AssetID: {type: string}
        From: {type: string, format: date-time, description: Pivot minus before_seconds.}
        To: {type: string, format: date-time, description: Pivot plus after_seconds.}
        Entries:
          type: [array, "null"]
          items: {$ref: '#/components/schemas/FleetEndpointTimelineEntry'}
        Truncated: {type: boolean, description: The effective limit capped the window, so the view is partial.}

    PlaceFleetLegalHoldRequest:
      type: object
      required: [reason]
      properties:
        reason: {type: string, minLength: 1, description: Why the data must be preserved. Mandatory for accountability.}

    FleetLegalHold:
      type: object
      description: Serialized from the domain struct (PascalCase keys). Active while ReleasedAt is the zero time.
      required: [TenantID, EngagementID, Reason, PlacedBy, PlacedAt, ReleasedBy, ReleasedAt]
      properties:
        TenantID: {type: string}
        EngagementID: {type: string}
        Reason: {type: string}
        PlacedBy: {type: string}
        PlacedAt: {type: string, format: date-time}
        ReleasedBy: {type: string, description: Empty while the hold is active.}
        ReleasedAt: {type: string, format: date-time, description: The zero time while the hold is active.}

    FleetLegalHoldList:
      type: object
      required: [holds]
      properties:
        holds:
          type: [array, "null"]
          description: Null rather than an empty array when the tenant holds none.
          items: {$ref: '#/components/schemas/FleetLegalHold'}

    PurgeFleetDetectionDataRequest:
      type: object
      required: [reason]
      properties:
        reason: {type: string, minLength: 1, description: Why the projection is being deleted. Mandatory; recorded in the audit log with the actor.}

    PurgeFleetDetectionDataResult:
      type: object
      required: [purged]
      properties:
        purged: {type: integer, minimum: 0, description: Number of detection projection rows deleted.}

    FleetDetectionClass:
      type: string
      enum: [process, network, file, privilege]

    FleetDetectionEvent:
      type: object
      description: >
        Serialized from the domain struct (PascalCase keys). Exactly one per-class payload is present;
        the other three are null, and the one present always matches Class.
      required: [Class, At, Host]
      properties:
        Class: {$ref: '#/components/schemas/FleetDetectionClass'}
        At: {type: string, format: date-time}
        Host: {type: string}
        Process:
          type: [object, "null"]
          properties:
            PID: {type: integer}
            PPID: {type: integer}
            Comm: {type: string}
            Path: {type: string}
            Args: {type: [array, "null"], items: {type: string}}
            UID: {type: integer}
        Network:
          type: [object, "null"]
          properties:
            Proto: {type: string, enum: [tcp, udp]}
            RemoteAddr: {type: string}
            RemotePort: {type: integer}
            Direction: {type: string, enum: [egress, ingress]}
            PID: {type: integer}
            Comm: {type: string}
        File:
          type: [object, "null"]
          properties:
            Path: {type: string}
            Op: {type: string, enum: [read, write, open, unlink]}
            PID: {type: integer}
            Comm: {type: string}
        Privilege:
          type: [object, "null"]
          properties:
            PID: {type: integer}
            Comm: {type: string}
            FromUID: {type: integer}
            ToUID: {type: integer}
            Cap: {type: string, description: Capability gained, if any.}
            Kind: {type: string, enum: [setuid, setgid, capset]}

    FleetDetection:
      type: object
      description: Serialized from the domain struct (PascalCase keys). What a matched rule emitted, with full attribution.
      required: [RuleID, RuleVersion, Class, Severity, HostID, AgentID, Evidence, Truncated, ObservedCount, Observed]
      properties:
        RuleID: {type: string, minLength: 1}
        RuleVersion: {type: integer, minimum: 1}
        Class: {$ref: '#/components/schemas/FleetDetectionClass'}
        Severity: {type: string, enum: [critical, high, medium, low, info], description: A stored detection never carries the unknown severity; ingest rejects it.}
        HostID: {type: string, minLength: 1}
        AgentID: {type: string, minLength: 1}
        Evidence:
          type: array
          description: The triggering event plus surrounding context. A detection with no evidence is a claim, not a detection, so at least one event is always present.
          minItems: 1
          maxItems: 64
          items: {$ref: '#/components/schemas/FleetDetectionEvent'}
        Truncated: {type: boolean, description: The evidence window was capped, so Evidence is not the complete sequence.}
        ObservedCount: {type: integer, description: Events seen before truncation; at least the length of Evidence.}
        Observed: {type: string, format: date-time}

    FleetDetectionRecord:
      type: object
      description: Serialized from the domain struct (PascalCase keys). One sealed, chain-linked detection projection row.
      required: [ID, TenantID, EngagementID, AssetID, AgentID, Detection, EvidenceID, BatchSeq, RecordedAt, ExpiresAt]
      properties:
        ID: {type: string}
        TenantID: {type: string}
        EngagementID: {type: string}
        AssetID: {type: string, description: The asset the detection was observed on.}
        AgentID: {type: string}
        Detection: {$ref: '#/components/schemas/FleetDetection'}
        EvidenceID: {type: string, description: The evidence-chain link that sealed this detection.}
        BatchSeq: {type: integer, format: int64, minimum: 0, description: The agent batch sequence the detection arrived in.}
        RecordedAt: {type: string, format: date-time}
        ExpiresAt: {type: string, format: date-time, description: The zero time means the row never expires; a set value is enforced by audited retention.}

    FleetPrivacyExportBundle:
      type: object
      required: [engagement_id, generated_at, detections, legal_holds, detection_count]
      properties:
        engagement_id: {type: string}
        generated_at: {type: string, format: date-time}
        detections:
          type: [array, "null"]
          description: Null rather than an empty array when the engagement holds no detection rows.
          items: {$ref: '#/components/schemas/FleetDetectionRecord'}
        legal_holds:
          type: [array, "null"]
          description: Active holds covering this engagement's data; null when there are none.
          items: {$ref: '#/components/schemas/FleetLegalHold'}
        detection_count: {type: integer, minimum: 0}

    FleetIncidentState:
      type: string
      description: Where the incident sits in the response workflow. Independent of Disposition and of risk.
      enum: [new, open, triaged, investigating, contained, remediated, resolved, closed, reopened]

    FleetIncidentDisposition:
      type: string
      description: The analyst's verdict. Independent of State and never mutates the risk score.
      enum: [unknown, true_positive, benign_positive, false_positive, duplicate, test]

    FleetRiskScore:
      type: integer
      description: Normalized 0..100 factor, integer so scoring stays deterministic and reproducible.
      minimum: 0
      maximum: 100

    FleetIncidentRiskAssessment:
      type: object
      description: >
        Serialized from the domain struct (PascalCase keys). Risk, Confidence and Coverage are three
        different things and are carried separately: missing coverage lowers Coverage, never Risk.
      required: [AssessmentID, IncidentRevision, ScorerVersion, PolicyVersion, InputSnapshotHash, Risk, Confidence, Coverage, CoverageVector, Context, FactorContributions, ReasonCodes, CreatedAt]
      properties:
        AssessmentID: {type: string}
        IncidentRevision: {type: integer}
        ScorerVersion: {type: string}
        PolicyVersion: {type: string}
        InputSnapshotHash: {type: string, description: With ScorerVersion and PolicyVersion, makes the assessment reproducible and auditable.}
        Risk: {$ref: '#/components/schemas/FleetRiskScore'}
        Confidence: {$ref: '#/components/schemas/FleetRiskScore'}
        Coverage: {$ref: '#/components/schemas/FleetRiskScore'}
        CoverageVector:
          type: object
          description: Coverage per telemetry class, never averaged into a scalar.
          properties:
            Process: {$ref: '#/components/schemas/FleetRiskScore'}
            Network: {$ref: '#/components/schemas/FleetRiskScore'}
            File: {$ref: '#/components/schemas/FleetRiskScore'}
            Privilege: {$ref: '#/components/schemas/FleetRiskScore'}
            Reasons: {type: [array, "null"], items: {type: string}}
        Context:
          type: object
          description: The three independent risk factors. Exposure and Behavior are zero until those pillars fill them.
          properties:
            Threat: {$ref: '#/components/schemas/FleetRiskScore'}
            Exposure: {$ref: '#/components/schemas/FleetRiskScore'}
            Behavior: {$ref: '#/components/schemas/FleetRiskScore'}
        FactorContributions:
          type: [array, "null"]
          description: Weighted inputs to Risk, each with a human reason, so a score is explainable.
          items:
            type: object
            properties:
              Factor: {type: string}
              Points: {$ref: '#/components/schemas/FleetRiskScore'}
              Reason: {type: string}
        ReasonCodes: {type: [array, "null"], items: {type: string}}
        CreatedAt: {type: string, format: date-time}

    FleetIncident:
      type: object
      description: >
        Serialized from the domain struct (PascalCase keys). The projection folded from the incident's
        append-only event log, which remains the source of truth.
      required: [ID, AssetID, Title, Severity, State, Disposition, OwnerID, DetectionIDs, Risk, MergedInto, Comments, Responses, Revision, CreatedAt, UpdatedAt]
      properties:
        ID: {type: string}
        AssetID: {type: string}
        Title: {type: string}
        Severity: {type: string, enum: ["", critical, high, medium, low, info, unknown], description: Empty when the created event carried no severity; the fold accepts that and the scorer abstains on it.}
        State: {$ref: '#/components/schemas/FleetIncidentState'}
        Disposition: {$ref: '#/components/schemas/FleetIncidentDisposition'}
        OwnerID: {type: string, description: Empty until an owner is assigned.}
        DetectionIDs:
          type: [array, "null"]
          description: Every detection folded in, so an auditor can descend to the individual hash-chained records.
          items: {type: string}
        Risk:
          description: Null until a risk assessment has been recorded on the log.
          anyOf: [{$ref: '#/components/schemas/FleetIncidentRiskAssessment'}, {type: 'null'}]
        MergedInto: {type: string, description: Empty unless the incident was merged into another.}
        Comments:
          type: [array, "null"]
          items:
            type: object
            properties:
              At: {type: string, format: date-time}
              Actor: {type: string}
              Text: {type: string}
        Responses:
          type: [array, "null"]
          description: Governed response actions linked to the incident, with whether their post-condition was telemetry-verified.
          items:
            type: object
            properties:
              ActionID: {type: string}
              Verified: {type: boolean}
        Revision: {type: integer, description: The number of events folded, so a projection is comparable to any point in the log.}
        CreatedAt: {type: string, format: date-time}
        UpdatedAt: {type: string, format: date-time}

    FleetCorrelationResult:
      type: object
      required: [created, reassessed, reassess_failed]
      properties:
        created:
          type: [array, "null"]
          description: The incidents this pass recorded. Null when the pass created none.
          items: {$ref: '#/components/schemas/FleetIncident'}
        reassessed: {type: integer, minimum: 0, description: How many of the created incidents were tri-scored.}
        reassess_failed: {type: integer, minimum: 0, description: How many scoring attempts failed; the incidents are still recorded.}

    EngagementTransitionRequest:
      type: object
      required: [status]
      properties:
        status:
          type: string
          enum: [active, completed, archived]
          description: Target lifecycle status. draft is the creation state and cannot be returned to.

    RehearseChainRequest:
      type: object
      required: [steps]
      properties:
        steps:
          type: array
          minItems: 1
          description: The ordered chain steps to rehearse. A state-changing step must declare a cleanup path.
          items:
            type: object
            required: [technique, target, blast_radius]
            properties:
              technique: {type: string, description: An offensive-policy register technique id.}
              target: {type: string, description: The asset id the step targets.}
              blast_radius: {type: string, enum: [read_only, state_changing]}
              cleanup:
                type: array
                items: {type: string}
                description: Cleanup steps for a state-changing step (required for state_changing, forbidden for read_only).
              cleanup_verification: {type: string, description: How to confirm the cleanup succeeded.}

    ChainRehearsalResult:
      type: object
      properties:
        chain_id: {type: string}
        state: {type: string, description: The chain's terminal state (e.g. succeeded, failed, cleaned).}
        steps: {type: integer}
        simulated: {type: boolean, description: Always true; a rehearsal never touches a host.}

    RunEmulationRequest:
      type: object
      required: [target]
      properties:
        target:
          type: string
          description: Asset id the techniques run against and the coverage is attributed to.

    EmulationRunResult:
      type: object
      description: One adversary-emulation run and the purple-team coverage it produced.
      properties:
        run:
          type: object
          description: The emulation run with its per-technique coverage records (expected detections).
        coverage:
          type: object
          description: The computed purple coverage (per-technique covered/gap, bonus detections, work items).

    SetOffensiveRoERequest:
      type: object
      description: >
        Offensive rules of engagement. The offensive governance policy refuses adversary emulation and
        exploitation chains until every field is set (an empty risk ceiling leaves the pillar refused).
      properties:
        customer_contact:
          type: string
          description: Who to reach on the customer side when an offensive action goes wrong.
        emergency_contact:
          type: string
          description: Out-of-hours emergency contact for offensive actions.
        risk_ceiling:
          type: string
          enum: ["", low, medium, high, prohibited]
          description: Highest risk class an offensive action may carry. Empty leaves the pillar refused.
        exclusions_checked:
          type: boolean
          description: The operator confirmed the out-of-scope list was reviewed.

    Capability:
      type: object
      required: [key, name, enabled, switch]
      properties:
        key:
          type: string
          description: Stable subsystem identifier a client keys its navigation off.
          example: fleet
        name:
          type: string
          description: Human name for display.
          example: Agent fleet transport
        enabled:
          type: boolean
          description: Whether this deployment serves the subsystem's routes.
        switch:
          type: string
          description: The SYNAPSE_* environment variable an operator sets to change `enabled`.
          example: SYNAPSE_FLEET_ENABLED
        requires:
          type: array
          description: >
            Keys of the capabilities this one depends on. Present only when the subsystem needs more
            than its own switch, so a client can explain an enabled switch that is still disabled.
          items: {type: string}

    User:
      type: object
      required: [id, name, role, disabled, createdAt]
      properties:
        id: {type: string}
        name: {type: string}
        role: {type: string, enum: [admin, consultant, reviewer, readonly, member]}
        disabled:
          type: boolean
          description: A disabled operator keeps its identity and history; its API key stops authenticating.
        createdAt: {type: string, format: date-time}

    UserWithAPIKey:
      type: object
      required: [user, apiKey]
      properties:
        user: {$ref: "#/components/schemas/User"}
        apiKey:
          type: string
          description: The raw bearer key, shown exactly once and never recoverable afterwards.
          example: syn_0123456789abcdef

    CreateUserRequest:
      type: object
      required: [name]
      properties:
        name: {type: string}
        role:
          type: string
          enum: [admin, consultant, reviewer, readonly, member]
          default: member
        tenant_id:
          type: string
          description: >
            Tenant the new operator belongs to. Empty means the calling admin's own tenant. A
            different tenant is refused with 403 unless the caller is the bootstrap platform principal.

    UpdateUserRequest:
      type: object
      properties:
        name:
          type: string
          description: Empty leaves the current name unchanged.
        role:
          type: string
          enum: [admin, consultant, reviewer, readonly, member]
          description: Empty leaves the current role unchanged.

    Readiness:
      type: object
      required: [status, checks]
      properties:
        status:
          type: string
          enum: [ready, not_ready]
        checks:
          type: object
          additionalProperties:
            type: string
            enum: [ok, failed]

    SLATier:
      type: string
      enum: [emergency, critical, high, medium, low, exception]

    SLARemediationStatus:
      type: string
      enum: [open, mitigating, remediated, accepted_risk]

    SLAWeights:
      type: object
      required: [severity, exploitability, threat_intel, exposure, criticality, feasibility_relief]
      properties:
        severity: {type: number, minimum: 0}
        exploitability: {type: number, minimum: 0}
        threat_intel: {type: number, minimum: 0}
        exposure: {type: number, minimum: 0}
        criticality: {type: number, minimum: 0}
        feasibility_relief: {type: number, minimum: 0}

    SLAThresholds:
      type: object
      description: Inclusive lower score bounds; values must be strictly descending from emergency to medium.
      required: [emergency, critical, high, medium]
      properties:
        emergency: {type: number, minimum: 0, maximum: 100}
        critical: {type: number, minimum: 0, maximum: 100}
        high: {type: number, minimum: 0, maximum: 100}
        medium: {type: number, exclusiveMinimum: 0, maximum: 100}

    SLADueRange:
      type: object
      description: Go duration values encoded as integer nanoseconds; mitigate_within must not exceed remediate_within.
      required: [mitigate_within, remediate_within]
      properties:
        mitigate_within: {type: integer, format: int64, minimum: 0, example: 86400000000000}
        remediate_within: {type: integer, format: int64, minimum: 0, example: 604800000000000}

    SLADueRanges:
      type: object
      required: [emergency, critical, high, medium, low, exception]
      properties:
        emergency: {$ref: '#/components/schemas/SLADueRange'}
        critical: {$ref: '#/components/schemas/SLADueRange'}
        high: {$ref: '#/components/schemas/SLADueRange'}
        medium: {$ref: '#/components/schemas/SLADueRange'}
        low: {$ref: '#/components/schemas/SLADueRange'}
        exception: {$ref: '#/components/schemas/SLADueRange'}

    SLAConfig:
      type: object
      required: [weights, thresholds, due_ranges, version]
      properties:
        weights: {$ref: '#/components/schemas/SLAWeights'}
        thresholds: {$ref: '#/components/schemas/SLAThresholds'}
        due_ranges: {$ref: '#/components/schemas/SLADueRanges'}
        version: {type: string, minLength: 1, example: sla-v2}

    SLAPolicy:
      type: object
      required: [tenant_id, config, sha256, created_by, created_at]
      properties:
        tenant_id: {type: string}
        config: {$ref: '#/components/schemas/SLAConfig'}
        sha256: {type: string, pattern: '^[0-9a-f]{64}$'}
        created_by: {type: string}
        created_at: {type: string, format: date-time}

    SLAPolicyList:
      type: object
      required: [active, policies]
      properties:
        active: {$ref: '#/components/schemas/SLAPolicy'}
        policies: {type: array, items: {$ref: '#/components/schemas/SLAPolicy'}}

    SLAPolicyActivation:
      type: object
      required: [policy, created]
      properties:
        policy: {$ref: '#/components/schemas/SLAPolicy'}
        created: {type: boolean}

    SLAInputs:
      type: object
      required: [severity, cvss_score, kev, epss, public_poc, active_exploitation]
      properties:
        severity: {type: string, enum: ['', info, low, medium, high, critical]}
        cvss_score: {type: number, minimum: 0, maximum: 10}
        kev: {type: boolean}
        epss: {type: number, minimum: 0, maximum: 1}
        public_poc: {type: boolean}
        active_exploitation: {type: boolean}
        criticality: {type: string, enum: ['', low, medium, high]}
        exposure: {type: string, enum: ['', internal, external]}
        feasibility: {type: string, enum: ['', patch_available, change_window, compensating_control, no_patch]}

    SLABreakdown:
      type: object
      required: [severity, exploitability, threat_intel, exposure, criticality, feasibility]
      properties:
        severity: {type: number}
        exploitability: {type: number}
        threat_intel: {type: number}
        exposure: {type: number}
        criticality: {type: number}
        feasibility: {type: number}
        overrides: {type: array, items: {type: string}}

    SLAResult:
      type: object
      required: [tier, score, breakdown, mitigate_by, remediate_by, reason, computed_at, config_version]
      properties:
        tier: {$ref: '#/components/schemas/SLATier'}
        score: {type: number, minimum: 0, maximum: 100}
        breakdown: {$ref: '#/components/schemas/SLABreakdown'}
        mitigate_by: {type: string, format: date-time}
        remediate_by: {type: string, format: date-time}
        reason: {type: string}
        computed_at: {type: string, format: date-time}
        config_version: {type: string}

    SLAAssessment:
      type: object
      required: [tenant_id, id, engagement_id, finding_id, inputs, result, input_hash, config_hash, deadline_anchor_at, assessed_at, created_at]
      properties:
        tenant_id: {type: string}
        id: {type: string}
        engagement_id: {type: string}
        finding_id: {type: string}
        source_risk_assessment_id: {type: string}
        inputs: {$ref: '#/components/schemas/SLAInputs'}
        result: {$ref: '#/components/schemas/SLAResult'}
        input_hash: {type: string, pattern: '^[0-9a-f]{64}$'}
        config_hash: {type: string, pattern: '^[0-9a-f]{64}$'}
        previous_assessment_id: {type: string}
        deadline_anchor_at: {type: string, format: date-time}
        assessed_at: {type: string, format: date-time}
        created_at: {type: string, format: date-time}

    SLALifecycle:
      type: object
      required: [tenant_id, engagement_id, finding_id, assessment_id, status, version, updated_by, updated_at]
      properties:
        tenant_id: {type: string}
        engagement_id: {type: string}
        finding_id: {type: string}
        assessment_id: {type: string}
        status: {$ref: '#/components/schemas/SLARemediationStatus'}
        version: {type: integer, minimum: 1}
        reason: {type: string}
        compensating_control: {type: string}
        accepted_by: {type: string}
        accepted_at: {type: string, format: date-time}
        acceptance_expires_at: {type: string, format: date-time}
        updated_by: {type: string}
        updated_at: {type: string, format: date-time}

    SLAView:
      type: object
      required: [assessment, lifecycle, effective_state, overdue, acceptance_expired]
      properties:
        assessment: {$ref: '#/components/schemas/SLAAssessment'}
        lifecycle: {$ref: '#/components/schemas/SLALifecycle'}
        effective_state: {$ref: '#/components/schemas/SLARemediationStatus'}
        overdue: {type: boolean}
        acceptance_expired: {type: boolean}

    SLALifecycleEvent:
      type: object
      required: [tenant_id, id, engagement_id, finding_id, assessment_id, from, to, reason, actor, before_version, after_version, at]
      properties:
        tenant_id: {type: string}
        id: {type: string}
        engagement_id: {type: string}
        finding_id: {type: string}
        assessment_id: {type: string}
        from: {$ref: '#/components/schemas/SLARemediationStatus'}
        to: {$ref: '#/components/schemas/SLARemediationStatus'}
        reason: {type: string}
        compensating_control: {type: string}
        acceptance_expires_at: {type: string, format: date-time}
        actor: {type: string}
        before_version: {type: integer, minimum: 1}
        after_version: {type: integer, minimum: 2}
        at: {type: string, format: date-time}

    SLATransitionRequest:
      type: object
      required: [to, reason, version]
      properties:
        to: {$ref: '#/components/schemas/SLARemediationStatus'}
        reason: {type: string, minLength: 1}
        compensating_control: {type: string}
        acceptance_expires_at: {type: string, format: date-time}
        version: {type: integer, minimum: 1}

    VulnerabilityCursor:
      type: object
      required: [before_time, before_id]
      properties:
        before_time: {type: string, format: date-time}
        before_id: {type: string}

    VulnerabilityOverview:
      type: object
      required: [enabled_sources, stale_or_failed_sources, changed_advisories_24_hours, open_high_critical_exposure, pending_risk_actions, queue_depth, dead_letters]
      properties:
        enabled_sources: {type: integer, minimum: 0}
        stale_or_failed_sources: {type: integer, minimum: 0}
        last_successful_sync: {type: string, format: date-time}
        changed_advisories_24_hours: {type: integer, minimum: 0}
        oldest_unevaluated_revision: {$ref: '#/components/schemas/VulnerabilityEvaluationLag'}
        open_high_critical_exposure: {type: integer, minimum: 0, description: Current detected occurrences whose latest risk severity is high or critical}
        pending_risk_actions: {type: integer, minimum: 0}
        queue_depth: {type: integer, minimum: 0}
        dead_letters: {type: integer, minimum: 0}

    VulnerabilityEvaluationLag:
      type: object
      required: [advisory_id, current_revision, evaluated_revision, changed_at]
      properties:
        advisory_id: {type: string}
        current_revision: {type: integer, minimum: 1}
        evaluated_revision: {type: integer, minimum: 0}
        changed_at: {type: string, format: date-time}

    VulnerabilityCanonicalAdvisory:
      type: object
      description: Canonical advisory projection. Nested fields retain legacy Go field names.
      additionalProperties: true
      properties:
        Advisory: {type: object, additionalProperties: true}
        Status: {type: string, enum: [active, rejected, withdrawn]}
        PublishedAt: {type: string, format: date-time}
        ModifiedAt: {type: string, format: date-time}
        KEV: {type: [boolean, 'null']}
        EPSS: {type: [number, 'null'], minimum: 0, maximum: 1}
        EPSSPercentile: {type: [number, 'null'], minimum: 0, maximum: 1}
        PublicExploit: {type: [boolean, 'null']}
        ActiveExploitation: {type: [boolean, 'null']}
        Sources: {type: array, items: {type: string}}

    VulnerabilityAdvisoryItem:
      type: object
      required: [canonical, revision, active_affected_count, affected_asset_count, affected_component_count, risk_priority, previous_risk_priority, risk_trend, risk_score, risk_score_change]
      properties:
        canonical: {$ref: '#/components/schemas/VulnerabilityCanonicalAdvisory'}
        revision: {type: integer, minimum: 1}
        changed_fields: {type: array, items: {type: string, enum: [identity, aliases, summary, cvss, dates, affected, status, exploitability, sources]}}
        changed_at: {type: string, format: date-time}
        active_affected_count: {type: integer, minimum: 0, description: Current tenant occurrences in detected state}
        affected_asset_count: {type: integer, minimum: 0}
        affected_component_count: {type: integer, minimum: 0}
        risk_priority: {type: integer, minimum: 0, maximum: 5, description: Zero when no current assessment exists}
        previous_risk_priority: {type: integer, minimum: 0, maximum: 5, description: Zero when no previous assessment exists}
        risk_trend: {type: string, enum: [none, new, increased, decreased, unchanged]}
        risk_score: {type: number, minimum: 0}
        risk_score_change: {type: number}
        detection_states: {type: array, items: {type: string, enum: [detected, no_longer_detected, withdrawn]}}
        action_states: {type: array, items: {type: string, enum: [open, acknowledged, resolved]}}
        last_evaluation: {type: string, format: date-time}

    VulnerabilityAdvisoryPage:
      type: object
      required: [items]
      properties:
        items: {type: array, items: {$ref: '#/components/schemas/VulnerabilityAdvisoryItem'}}
        next: {type: string}

    VulnerabilityAdvisoryRevisionItem:
      type: object
      required: [canonical, revision]
      properties:
        canonical: {$ref: '#/components/schemas/VulnerabilityCanonicalAdvisory'}
        revision: {type: integer, minimum: 1}
        changed_fields: {type: array, items: {type: string}}
        sync_run_ids: {type: array, items: {type: string}, description: Tenant-visible synchronization runs that created this canonical revision}
        changed_at: {type: string, format: date-time}

    VulnerabilityAdvisoryRevisionPage:
      type: object
      required: [items]
      properties:
        items: {type: array, items: {$ref: '#/components/schemas/VulnerabilityAdvisoryRevisionItem'}}
        next: {type: integer, minimum: 1}

    VulnerabilityOccurrence:
      type: object
      description: Legacy domain serialization; property names are PascalCase.
      additionalProperties: true
      properties:
        TenantID: {type: string}
        ID: {type: string}
        EngagementID: {type: string}
        AdvisoryID: {type: string}
        AdvisoryRevision: {type: integer, minimum: 1}
        State: {type: string, enum: [detected, no_longer_detected, withdrawn]}
        UpdatedAt: {type: string, format: date-time}

    VulnerabilityOccurrencePage:
      type: object
      required: [items]
      properties:
        items: {type: array, items: {$ref: '#/components/schemas/VulnerabilityOccurrence'}}
        next: {$ref: '#/components/schemas/VulnerabilityCursor'}

    VulnerabilityAssessmentPage:
      type: object
      required: [items]
      properties:
        items: {type: array, items: {type: object, additionalProperties: true, description: Immutable legacy PascalCase risk assessment}}
        next: {$ref: '#/components/schemas/VulnerabilityCursor'}

    VulnerabilityTransitionPage:
      type: object
      required: [items]
      properties:
        items: {type: array, items: {type: object, additionalProperties: true, description: Immutable legacy PascalCase risk transition}}
        next: {$ref: '#/components/schemas/VulnerabilityCursor'}

    VulnerabilitySource:
      type: object
      required: [id, key, name, adapter_type, endpoint, enabled, archived, cadence_seconds, stale_after_seconds, sync_mode, credential_configured, version, created_at, updated_at, health]
      properties:
        id: {type: string}
        key: {type: string}
        name: {type: string}
        adapter_type: {type: string, enum: [osv, csaf, oval, nvd, cisa_kev, first_epss, public_exploit]}
        endpoint: {type: string, format: uri, pattern: '^https://'}
        enabled: {type: boolean}
        archived: {type: boolean}
        cadence_seconds: {type: integer, minimum: 1}
        stale_after_seconds: {type: integer, minimum: 1}
        sync_mode: {type: string, enum: [incremental, full]}
        adapter_config: {type: object, additionalProperties: true}
        credential_configured: {type: boolean}
        version: {type: integer, minimum: 1}
        created_at: {type: string, format: date-time}
        updated_at: {type: string, format: date-time}
        health: {type: object, additionalProperties: true}

    VulnerabilitySyncState:
      type: string
      enum: [queued, running, succeeded, partial, failed, superseded]
      description: "`queued` and `running` are active; all other values are terminal."

    VulnerabilitySyncCounts:
      type: object
      required: [processed, inserted, updated, unchanged, skipped, quarantined]
      properties:
        processed: {type: integer, minimum: 0}
        inserted: {type: integer, minimum: 0}
        updated: {type: integer, minimum: 0}
        unchanged: {type: integer, minimum: 0}
        skipped: {type: integer, minimum: 0}
        quarantined: {type: integer, minimum: 0}

    VulnerabilitySyncRun:
      type: object
      required: [id, source_id, adapter_type, mode, trigger, actor, attempts, dead_lettered, counts, state, created_at, updated_at]
      description: Safe ordinary view. It intentionally excludes source_snapshot, credential references, and raw provider payloads.
      properties:
        id: {type: string}
        source_id: {type: string}
        adapter_type: {type: string}
        mode: {type: string, enum: [incremental, full]}
        trigger: {type: string}
        actor: {type: string, maxLength: 256}
        client_idempotency_key: {type: string, maxLength: 256}
        durable_job_id: {type: string}
        attempts: {type: integer, minimum: 0}
        dead_lettered: {type: boolean}
        affected_revisions:
          type: array
          maxItems: 100
          items:
            type: object
            required: [advisory_id, revision, changed_at]
            properties:
              advisory_id: {type: string}
              revision: {type: integer, minimum: 1}
              changed_at: {type: string, format: date-time}
        affected_revisions_truncated: {type: boolean}
        checkpoint: {type: object, additionalProperties: true}
        counts: {$ref: '#/components/schemas/VulnerabilitySyncCounts'}
        error_samples: {type: array, maxItems: 32, items: {type: string}}
        state: {$ref: '#/components/schemas/VulnerabilitySyncState'}
        started_at: {type: string, format: date-time}
        finished_at: {type: string, format: date-time}
        created_at: {type: string, format: date-time}
        updated_at: {type: string, format: date-time}

    VulnerabilitySyncRunPage:
      type: object
      required: [items]
      properties:
        items: {type: array, items: {$ref: '#/components/schemas/VulnerabilitySyncRun'}}
        next: {$ref: '#/components/schemas/VulnerabilityCursor'}

    VulnerabilityAction:
      type: object
      required: [id, engagement_id, occurrence_id, finding_id, transition_id, type, status, title, reason_codes, created_at, updated_at]
      properties:
        id: {type: string}
        engagement_id: {type: string}
        occurrence_id: {type: string}
        finding_id: {type: string}
        transition_id: {type: string}
        type: {type: string, enum: [new_exposure, escalation, withdrawal, reexposure, retest_required, risk_review]}
        status: {type: string, enum: [open, acknowledged, resolved]}
        title: {type: string}
        reason_codes: {type: array, items: {type: string}}
        acknowledged_by: {type: string}
        acknowledged_at: {type: string, format: date-time}
        resolved_by: {type: string}
        resolved_at: {type: string, format: date-time}
        created_at: {type: string, format: date-time}
        updated_at: {type: string, format: date-time}

    VulnerabilityActionPage:
      type: object
      required: [items]
      properties:
        items: {type: array, items: {$ref: '#/components/schemas/VulnerabilityAction'}}
        next:
          type: object
          required: [before_created_at, before_id]
          properties:
            before_created_at: {type: string, format: date-time}
            before_id: {type: string}

    VulnerabilitySyncRequest:
      type: object
      properties:
        mode: {type: string, enum: [incremental, full], default: incremental}
        client_idempotency_key: {type: string, maxLength: 256}

    VulnerabilitySyncAccepted:
      type: object
      required: [run_id, source_id, mode, state, created]
      properties:
        run_id: {type: string}
        source_id: {type: string}
        mode: {type: string, enum: [incremental, full]}
        state: {$ref: '#/components/schemas/VulnerabilitySyncState'}
        created: {type: boolean}

    VulnerabilityReconcileRequest:
      type: object
      properties:
        client_idempotency_key: {type: string, maxLength: 256}

    VulnerabilityReconcileAccepted:
      type: object
      required: [run_id, job_id, scope, state, created]
      properties:
        run_id: {type: string}
        job_id: {type: string}
        scope: {type: string, enum: [tenant, advisory]}
        advisory_id: {type: string}
        state: {type: string, enum: [queued, running, succeeded, failed]}
        created: {type: boolean}

    VulnerabilityReconcileRun:
      type: object
      additionalProperties: true
      required: [tenant_id, id, scope, dry_run, durable_job_id, checkpoint, counts, state, snapshot_at, created_at, updated_at]
      properties:
        tenant_id: {type: string}
        id: {type: string}
        scope: {type: string, enum: [tenant, advisory]}
        advisory_id: {type: string}
        dry_run: {type: boolean, description: Pinned when the run is enqueued and unchanged across retries or rollout configuration changes}
        durable_job_id: {type: string}
        checkpoint: {type: object, additionalProperties: true}
        counts:
          $ref: '#/components/schemas/VulnerabilityReconcileCounts'
        state: {type: string, enum: [queued, running, succeeded, failed]}
        snapshot_at: {type: string, format: date-time}
        started_at: {type: string, format: date-time}
        finished_at: {type: string, format: date-time}
        created_at: {type: string, format: date-time}
        updated_at: {type: string, format: date-time}

    VulnerabilityReconcileCounts:
      type: object
      required: [processed, added, updated, unchanged, unmatchable, retired]
      description: For dry-run runs, counts are derived from persisted difference rows and remain idempotent across retries.
      properties:
        processed: {type: integer, minimum: 0, description: Total difference rows across all mismatch classes}
        added: {type: integer, minimum: 0, description: "missing_occurrence rows"}
        updated: {type: integer, minimum: 0, description: "changed_occurrence rows"}
        unchanged: {type: integer, minimum: 0, description: "in_sync rows"}
        unmatchable: {type: integer, minimum: 0, description: "unmatchable_input rows"}
        retired: {type: integer, minimum: 0, description: "stale_occurrence rows"}

    VulnerabilityReconcileDiffClass:
      type: string
      enum: [missing_occurrence, changed_occurrence, in_sync, stale_occurrence, unmatchable_input]

    VulnerabilityReconcileDiff:
      type: object
      required: [tenant_id, run_id, engagement_id, advisory_id, component_fingerprint, class, details, created_at]
      properties:
        tenant_id: {type: string}
        run_id: {type: string}
        engagement_id: {type: string}
        advisory_id: {type: string}
        component_fingerprint: {type: string}
        class: {$ref: '#/components/schemas/VulnerabilityReconcileDiffClass'}
        details: {type: object, additionalProperties: true}
        created_at: {type: string, format: date-time}

    VulnerabilityReconcileDiffCursor:
      type: object
      required: [class, engagement_id, advisory_id, component_fingerprint]
      properties:
        class: {$ref: '#/components/schemas/VulnerabilityReconcileDiffClass'}
        engagement_id: {type: string}
        advisory_id: {type: string}
        component_fingerprint: {type: string}

    VulnerabilityReconcileDiffPage:
      type: object
      required: [items]
      properties:
        items: {type: array, items: {$ref: '#/components/schemas/VulnerabilityReconcileDiff'}}
        next: {$ref: '#/components/schemas/VulnerabilityReconcileDiffCursor'}

    DASTRun:
      type: object
      required: [id, engagement_id, action_id, actor, status, started_at]
      properties:
        id: {type: string}
        engagement_id: {type: string}
        action_id: {type: string, description: The consumed DAST approval the probe bound to}
        actor: {type: string}
        status: {type: string, enum: [queued, running, succeeded, failed]}
        verdict: {type: string, description: The verification's proof class; empty until terminal}
        http_status: {type: integer, minimum: 0, description: The probe's observed HTTP status; zero until observed}
        evidence_id: {type: string, description: The sealed evidence the run produced}
        error_code: {type: string}
        started_at: {type: string, format: date-time}
        finished_at: {type: [string, "null"], format: date-time}

    CSPMRun:
      type: object
      required: [id, engagement_id, actor, status, complete, assets, findings, started_at]
      properties:
        id: {type: string}
        engagement_id: {type: string}
        actor: {type: string}
        status: {type: string, enum: [queued, running, succeeded, partial, failed, cancelled]}
        complete: {type: boolean}
        assets: {type: integer, minimum: 0}
        findings: {type: integer, minimum: 0}
        coverage_issues: {type: array, items: {type: object}}
        error_code: {type: string}
        evidence_refs:
          type: array
          items:
            type: object
            required: [scope_key, id, hash]
            properties:
              scope_key: {type: string}
              id: {type: string}
              hash: {type: string}
        started_at: {type: string, format: date-time}
        finished_at: {type: [string, "null"], format: date-time}

    Error:
      type: object
      properties:
        error: {type: string}

    AITriageReview:
      type: object
      required: [id, tenant_id, engagement_id, finding_id, dedup_key, title, severity, state, verdict, driver, confidence, suspected_fp, proposer_model, proposer_provider, proposer_model_family, independence_policy, prompt_version, verified, policy_version, policy_reason, shadow, would_gate_exempt, gate_exempt, review_required, evidence_ref, created_at, updated_at, version]
      properties:
        id: {type: string}
        tenant_id: {type: string}
        engagement_id: {type: string}
        project_id: {type: string}
        finding_id: {type: string}
        dedup_key: {type: string}
        title: {type: string}
        severity: {type: string, enum: [critical, high, medium, low, info, unknown]}
        cwe: {type: string}
        owner: {type: string}
        state: {type: string, enum: [pending, accepted, rejected]}
        verdict: {type: string}
        driver: {type: string}
        confidence: {type: integer, minimum: 0, maximum: 100}
        suspected_fp: {type: boolean}
        proposer_model: {type: string}
        proposer_provider: {type: string}
        proposer_model_family: {type: string}
        verifier_model: {type: string}
        verifier_provider: {type: string}
        verifier_model_family: {type: string}
        independence_policy: {type: string, enum: ['', model_family, provider]}
        prompt_version: {type: string}
        verified: {type: boolean}
        verifier_verdict: {type: string}
        verifier_driver: {type: string}
        verifier_confidence: {type: integer, minimum: 0, maximum: 100}
        policy_version: {type: string}
        policy_reason: {type: string}
        shadow: {type: boolean}
        would_gate_exempt: {type: boolean}
        gate_exempt: {type: boolean}
        review_required: {type: boolean}
        evidence_ref: {type: string}
        decided_by: {type: string}
        decision_rationale: {type: string}
        created_at: {type: string, format: date-time}
        updated_at: {type: string, format: date-time}
        decided_at: {type: [string, 'null'], format: date-time}
        version: {type: integer, minimum: 1}

    AITriageMetricRow:
      type: object
      required: [value, request_count, average_latency_ms, timeout_count, parse_failure_count, provider_failure_count, circuit_open_count, total_tokens, estimated_cost_micro_usd, comparisons, disagreements, gate_exemptions, findings]
      properties:
        value: {type: string}
        request_count: {type: integer, minimum: 0}
        average_latency_ms: {type: integer, minimum: 0}
        timeout_count: {type: integer, minimum: 0}
        parse_failure_count: {type: integer, minimum: 0}
        provider_failure_count: {type: integer, minimum: 0}
        circuit_open_count: {type: integer, minimum: 0}
        total_tokens: {type: integer, minimum: 0}
        estimated_cost_micro_usd: {type: integer, minimum: 0}
        comparisons: {type: integer, minimum: 0}
        disagreements: {type: integer, minimum: 0}
        gate_exemptions: {type: integer, minimum: 0}
        findings: {type: integer, minimum: 0}
    AITriageAlert:
      type: object
      required: [metric, observed_basis_points, baseline_basis_points, deviation_basis_points, sample_size, message]
      properties:
        metric: {type: string, enum: [disagreement_rate, exemption_rate, parse_failure_rate]}
        observed_basis_points: {type: integer, minimum: 0, maximum: 10000}
        baseline_basis_points: {type: integer, minimum: 0, maximum: 10000}
        deviation_basis_points: {type: integer, minimum: 0, maximum: 10000}
        sample_size: {type: integer, minimum: 0}
        message: {type: string}
    AITriageDashboardAlert:
      type: object
      required: [project_id, project_name, alert]
      properties:
        project_id: {type: string}
        project_name: {type: string}
        alert: {$ref: '#/components/schemas/AITriageAlert'}
    AITriageDistributionSnapshot:
      type: object
      description: Source-free distribution snapshot; every populated dimension sums to 10000 basis points.
      required: [schema_version, sample_size, language_basis_points, cwe_basis_points, project_basis_points]
      properties:
        schema_version: {type: string, const: synapse-ai-triage-distribution-v1}
        sample_size: {type: integer, minimum: 0}
        language_basis_points:
          type: object
          additionalProperties: {type: integer, minimum: 1, maximum: 10000}
        cwe_basis_points:
          type: object
          additionalProperties: {type: integer, minimum: 1, maximum: 10000}
        project_basis_points:
          type: object
          additionalProperties: {type: integer, minimum: 1, maximum: 10000}
    AITriageDashboard:
      type: object
      required: [generated_at, totals, by_model, by_prompt_version, by_cwe, by_project, distribution, alerts]
      properties:
        generated_at: {type: string, format: date-time}
        totals: {$ref: '#/components/schemas/AITriageMetricRow'}
        by_model: {type: array, items: {$ref: '#/components/schemas/AITriageMetricRow'}}
        by_prompt_version: {type: array, items: {$ref: '#/components/schemas/AITriageMetricRow'}}
        by_cwe: {type: array, items: {$ref: '#/components/schemas/AITriageMetricRow'}}
        by_project: {type: array, items: {$ref: '#/components/schemas/AITriageMetricRow'}}
        distribution: {$ref: '#/components/schemas/AITriageDistributionSnapshot'}
        alerts: {type: array, items: {$ref: '#/components/schemas/AITriageDashboardAlert'}}
    OffensiveHaltRequest:
      type: object
      required: [reason]
      properties:
        reason:
          type: string
          description: Why the halt was pulled. Required — a halt nobody can explain afterwards defeats the chain of custody.
    OffensiveHaltResult:
      type: object
      properties:
        requested_at: {type: string, format: date-time}
        completed_at: {type: string, format: date-time}
        duration_ms: {type: integer, description: Measured halt duration at the control plane}
        stated_bound_ms: {type: integer, description: The bound the duration was measured against}
        within_bound: {type: boolean}
        halted: {type: boolean, description: True only when every in-flight offensive order was cancelled}
        cancelled: {type: array, items: {type: string}}
        failed:
          type: object
          additionalProperties: {type: string}
          description: Work orders that could not be cancelled, with the reason. Present means the halt was partial.
        audit_recorded: {type: boolean, description: False when the halt succeeded but could not be audited}
        estate_stop_note:
          type: string
          description: What the bound does NOT cover — a technique already running on a host stops within one further agent poll interval.
    AITriageReviewClaimRequest:
      type: object
      required: [version]
      additionalProperties: false
      properties:
        version: {type: integer, minimum: 1}
    AITriageReviewDecisionRequest:
      type: object
      required: [decision, rationale, version]
      additionalProperties: false
      properties:
        decision: {type: string, enum: [accept, reject]}
        rationale: {type: string, minLength: 3, maxLength: 4000}
        version: {type: integer, minimum: 1}

    DashboardPostureCounts:
      type: object
      required: [critical, high_risk, attention, unknown, good]
      properties:
        critical: {type: integer, minimum: 0}
        high_risk: {type: integer, minimum: 0}
        attention: {type: integer, minimum: 0}
        unknown: {type: integer, minimum: 0}
        good: {type: integer, minimum: 0}
    DashboardCriticalityCounts:
      type: object
      required: [critical, high, medium, low]
      properties:
        critical: {type: integer, minimum: 0}
        high: {type: integer, minimum: 0}
        medium: {type: integer, minimum: 0}
        low: {type: integer, minimum: 0}
    DashboardSeverityCounts:
      type: object
      required: [critical, high, medium, low, info, unknown]
      properties:
        critical: {type: integer, minimum: 0}
        high: {type: integer, minimum: 0}
        medium: {type: integer, minimum: 0}
        low: {type: integer, minimum: 0}
        info: {type: integer, minimum: 0}
        unknown: {type: integer, minimum: 0}
    DashboardTrendPoint:
      type: object
      required: [date, counts]
      properties:
        date: {type: string, format: date}
        counts: {$ref: '#/components/schemas/DashboardSeverityCounts'}
    DashboardSecurityOperations:
      type: object
      required: [range_days, generated_at, asset_posture, assets_by_criticality, active_findings_by_severity, findings_over_time, findings_without_timestamp, external_findings_included]
      properties:
        range_days: {type: integer, enum: [7, 30, 90]}
        generated_at: {type: string, format: date-time}
        asset_posture: {$ref: '#/components/schemas/DashboardPostureCounts'}
        assets_by_criticality: {$ref: '#/components/schemas/DashboardCriticalityCounts'}
        active_findings_by_severity: {$ref: '#/components/schemas/DashboardSeverityCounts'}
        findings_over_time:
          type: array
          minItems: 7
          maxItems: 90
          items: {$ref: '#/components/schemas/DashboardTrendPoint'}
        findings_without_timestamp: {type: integer, minimum: 0}
        external_findings_included: {type: boolean}

    BusinessAsset:
      type: object
      required: [ID, TenantID, Key, Name, Type, Criticality, Lifecycle, Owner, Version, Audit]
      properties:
        ID: {type: string}
        TenantID: {type: string}
        Key: {type: string, maxLength: 128}
        Name: {type: string, maxLength: 256}
        Description: {type: string, maxLength: 4000}
        Type: {type: string, enum: [product, application, system, business_service]}
        Criticality: {type: string, enum: [critical, high, medium, low]}
        Lifecycle: {type: string, enum: [draft, active, decommissioning, retired]}
        Owner: {type: string, maxLength: 256}
        Metadata: {type: object, additionalProperties: {type: string}}
        Version: {type: integer, minimum: 1}
        Audit: {type: object}
    CreateBusinessAssetRequest:
      type: object
      required: [key, name, type, criticality, owner]
      properties:
        key: {type: string, minLength: 1, maxLength: 128}
        name: {type: string, minLength: 1, maxLength: 256}
        description: {type: string, maxLength: 4000}
        type: {type: string, enum: [product, application, system, business_service]}
        criticality: {type: string, enum: [critical, high, medium, low]}
        owner: {type: string, minLength: 1, maxLength: 256}
        metadata: {type: object, maxProperties: 32, additionalProperties: {type: string, maxLength: 512}}
    UpdateBusinessAssetRequest:
      allOf:
      - {$ref: '#/components/schemas/CreateBusinessAssetRequest'}
      - type: object
        required: [version]
        properties:
          version: {type: integer, minimum: 1}
          lifecycle: {type: string, enum: [draft, active, decommissioning, retired]}
    BusinessAssetPage:
      type: object
      required: [items, total, limit, offset]
      properties:
        items: {type: array, items: {$ref: '#/components/schemas/BusinessAsset'}}
        total: {type: integer, minimum: 0}
        limit: {type: integer}
        offset: {type: integer}
    OffensivePolicyTechnique:
      type: object
      properties:
        technique: {type: string}
        taxonomy_ref: {type: string}
        disruption: {type: string}
        reversibility: {type: string}
        risk_class: {type: string}
        approval: {type: string}
        blast_radius: {type: string}
        production_safe: {type: boolean}
        prohibited: {type: boolean}
    OffensivePolicy:
      type: object
      properties:
        legal_review:
          type: object
          properties:
            reviewed: {type: boolean}
            date: {type: string}
            owner: {type: string}
            counsel_reviewed: {type: boolean}
            counsel_date: {type: string}
        techniques: {type: array, items: {$ref: '#/components/schemas/OffensivePolicyTechnique'}}
        prohibited: {type: integer}
        production_safe: {type: integer}
    HostPackages:
      type: object
      properties:
        asset_id: {type: string}
        engagement_id: {type: string}
        recorded_at: {type: string, format: date-time}
        packages:
          type: array
          items:
            type: object
            properties:
              name: {type: string}
              version: {type: string}
              purl: {type: string}
    AlertOutcome:
      type: object
      properties:
        matched: {type: boolean, description: Whether the alert cleared the severity rule.}
        delivered: {type: integer}
        failed: {type: integer}
        audit_failed: {type: integer}
    ResponseActionRequest:
      type: object
      required: [kind, target]
      properties:
        kind: {type: string, enum: [isolate_host, quarantine_file, stop_process], description: The response action kind; each has a mandatory typed reversal.}
        target: {type: string, description: The asset id the action affects; must be in the engagement scope.}
        target_kind: {type: string, description: 'The scope-entry kind the target is authorized as (default: ip).'}
    ResponseRevertRequest:
      type: object
      required: [target]
      properties:
        target: {type: string}
        target_kind: {type: string}
    ResponsePlanStep:
      type: object
      properties:
        label: {type: string}
        argv: {type: array, items: {type: string}}
        blast_radius: {type: string}
    ResponsePlan:
      type: object
      properties:
        kind: {type: string}
        target: {type: string}
        steps: {type: array, items: {$ref: '#/components/schemas/ResponsePlanStep'}}
    ResponseRecord:
      type: object
      properties:
        id: {type: string}
        kind: {type: string}
        target: {type: string}
        state: {type: string, enum: [pending, applied, reverted, cancelled, violation]}
        approver: {type: string}
        verification: {type: string, enum: [pending, succeeded, failed, unknown], description: 'Telemetry-verified post-condition: was the effect confirmed?'}
        evidence_id: {type: string}
    ResponseList:
      type: object
      properties:
        responses: {type: array, items: {$ref: '#/components/schemas/ResponseRecord'}}
    AlertTestResult:
      type: object
      properties:
        outcome: {$ref: '#/components/schemas/AlertOutcome'}
        error: {type: string}
    SCMConnector:
      type: object
      description: A source-control connector's non-secret metadata. The token is never included.
      properties:
        id: {type: string}
        name: {type: string}
        provider: {type: string, enum: [github, gitlab, bitbucket, generic]}
        host: {type: string, description: Normalized git host, e.g. github.com.}
        username: {type: string, description: The git username the token authenticates as.}
        auth_kind: {type: string, enum: [pat]}
        created_at: {type: string, format: date-time}
        updated_at: {type: string, format: date-time}
    SCMConnectorCreate:
      type: object
      required: [name, provider, host, token]
      properties:
        name: {type: string, description: A label for the connector.}
        provider: {type: string, enum: [github, gitlab, bitbucket, generic]}
        host: {type: string, description: The git host, bare or as an https URL; only the host is kept.}
        username: {type: string, description: Optional git username; defaults per provider (x-access-token for GitHub, oauth2 for GitLab).}
        token: {type: string, description: Personal access token. Write-only; sealed at rest and never returned.}
    FleetAsset:
      type: object
      description: Serialized from the fleet asset domain struct (PascalCase keys).
      properties:
        ID: {type: string}
        TenantID: {type: string}
        Kind: {type: string, enum: [host, workload, image, cloud_account, storage, exposure, identity, namespace, cluster, repository]}
        Key: {type: string, description: Natural key per kind; for a host the machine id or hostname.}
        Name: {type: string}
        Attributes: {type: object, additionalProperties: {type: string}}
        Audit: {type: object}
    HostScan:
      type: object
      description: The latest scan job of a host's vulnerability context.
      properties:
        job_id: {type: string}
        status: {type: string, enum: [running, succeeded, failed]}
        stage: {type: string}
        error: {type: string}
        started_at: {type: string, format: date-time}
        finished_at: {type: string, format: date-time}
    HostVulnerabilitySummaryCounts:
      type: object
      properties:
        total: {type: integer}
        critical: {type: integer}
        high: {type: integer}
        medium: {type: integer}
        low: {type: integer}
        info: {type: integer}
        fixable: {type: integer, description: Findings whose advisory names a fixed version.}
        kev: {type: integer, description: Findings on the CISA Known Exploited Vulnerabilities list.}
    HostVulnerabilitySummary:
      type: object
      properties:
        asset: {$ref: '#/components/schemas/FleetAsset'}
        engagement_id: {type: string, description: Hidden vulnerability context; absent until the host reports packages.}
        packages: {type: integer, description: Components in the active package SBOM.}
        recorded_at: {type: string, format: date-time}
        last_scan: {$ref: '#/components/schemas/HostScan'}
        summary: {$ref: '#/components/schemas/HostVulnerabilitySummaryCounts'}
    HostVulnerabilities:
      allOf:
      - $ref: '#/components/schemas/HostVulnerabilitySummary'
      - type: object
        properties:
          findings:
            type: array
            items: {$ref: '#/components/schemas/Finding'}
    AssetMembership:
      type: object
      required: [id, role, provenance]
      properties:
        id: {type: string}
        role: {type: string, enum: [primary, supporting, dependency]}
        provenance: {type: string, maxLength: 256}
    AssetMemberships:
      type: object
      required: [items]
      properties:
        items: {type: array, maxItems: 1000, items: {$ref: '#/components/schemas/AssetMembership'}}
    BusinessAssetReachabilityEvidence:
      type: object
      required: [judgment_id, state, tier, confidence, status, observed_at]
      properties:
        judgment_id: {type: string}
        state: {type: string, enum: [reachable, not_reachable, unknown]}
        tier: {type: string, enum: [tier-0, tier-1, tier-1.5, tier-2]}
        confidence: {type: integer, minimum: 0, maximum: 100}
        path: {type: array, items: {type: string}}
        status: {type: string}
        observed_at: {type: string, format: date-time}
    BusinessAssetReachability:
      type: object
      required: [state, tier, confidence, source, history]
      properties:
        state: {type: string, enum: [reachable, not_reachable, unknown]}
        tier: {type: string, enum: [tier-0, tier-1, tier-1.5, tier-2]}
        confidence: {type: integer, minimum: 0, maximum: 100}
        path: {type: array, items: {type: string}}
        status: {type: string}
        source: {type: string, enum: [none, finding_metadata, judgment, external]}
        history: {type: array, items: {$ref: '#/components/schemas/BusinessAssetReachabilityEvidence'}}
    BusinessAssetFindingProvenance:
      type: object
      required: [ToolName, ToolVersion, RuleID, SourceDigest, IngestedBy, IngestedAt]
      properties:
        ToolName: {type: string}
        ToolVersion: {type: string}
        RuleID: {type: string}
        SourceDigest: {type: string}
        IngestedBy: {type: string}
        IngestedAt: {type: string, format: date-time}
    BusinessAssetFinding:
      type: object
      required: [finding, external, suppressed_by_tool, reachability, engagement_id, engagement_name]
      properties:
        finding: {$ref: '#/components/schemas/Finding'}
        external: {type: boolean}
        can_self_promote: {type: boolean, description: Present for imported findings and always false}
        suppressed_by_tool: {type: boolean}
        provenance: {$ref: '#/components/schemas/BusinessAssetFindingProvenance'}
        reachability: {$ref: '#/components/schemas/BusinessAssetReachability'}
        engagement_id: {type: string}
        engagement_name: {type: string}
    BusinessAssetCoverage:
      type: object
      required: [rows, counts, freshness_target_days]
      properties:
        rows: {type: array, items: {type: object}}
        counts: {type: object, additionalProperties: {type: integer}}
        freshness_target_days: {type: integer, minimum: 1}
    BusinessAssetPosture:
      type: object
      required: [rating, explanation, finding_counts, coverage_counts]
      properties:
        rating: {type: string, enum: [good, attention, high_risk, critical, unknown]}
        explanation: {type: string}
        finding_counts: {type: object, additionalProperties: {type: integer}}
        coverage_counts: {type: object, additionalProperties: {type: integer}}
    BusinessAssetHistoryItem:
      type: object
      required: [engagement_id, name, status, scope_count, finding_count, retest_count, updated_at]
      properties:
        engagement_id: {type: string}
        name: {type: string}
        status: {type: string}
        authorized_from: {type: [string, 'null'], format: date-time}
        authorized_to: {type: [string, 'null'], format: date-time}
        scope_count: {type: integer, minimum: 0}
        finding_count: {type: integer, minimum: 0}
        retest_count: {type: integer, minimum: 0}
        updated_at: {type: string, format: date-time}

    ProjectCodeCapability:
      type: object
      required: [available]
      properties:
        available: {type: boolean}
        reason:
          type: string
          enum: [not_retained, capture_failed, first_analysis, no_comparable_base, unsupported_target, limit_exceeded, binary, non_utf8]

    ProjectCodeCapabilities:
      type: object
      required: [source, comparison, unified_diff, split_diff, highlighting]
      properties:
        source: {$ref: '#/components/schemas/ProjectCodeCapability'}
        comparison: {$ref: '#/components/schemas/ProjectCodeCapability'}
        unified_diff: {$ref: '#/components/schemas/ProjectCodeCapability'}
        split_diff: {$ref: '#/components/schemas/ProjectCodeCapability'}
        highlighting: {$ref: '#/components/schemas/ProjectCodeCapability'}

    ProjectCodeRevision:
      type: object
      required: [artifact_digest]
      properties:
        ref: {type: string}
        commit: {type: string}
        artifact_digest: {type: string, description: Immutable manifest SHA-256 identity}

    ProjectCodeSourceFile:
      type: object
      required: [path, bytes, lines, generated, available]
      properties:
        path: {type: string}
        digest: {type: string}
        bytes: {type: integer, minimum: 0}
        lines: {type: integer, minimum: 0}
        generated: {type: boolean}
        available: {type: boolean}
        reason: {type: string}

    ProjectCodeLineRange:
      type: object
      required: [start, end]
      properties:
        start: {type: integer, minimum: 1}
        end: {type: integer, minimum: 1}

    ProjectCodeDiffRow:
      type: object
      required: [kind, text]
      properties:
        kind: {type: string, enum: [context, added, removed]}
        old_line: {type: integer, minimum: 1}
        new_line: {type: integer, minimum: 1}
        text: {type: string}
        no_final_newline: {type: boolean}

    ProjectCodeDiffHunk:
      type: object
      required: [old_start, old_lines, new_start, new_lines, rows]
      properties:
        old_start: {type: integer, minimum: 0}
        old_lines: {type: integer, minimum: 0}
        new_start: {type: integer, minimum: 0}
        new_lines: {type: integer, minimum: 0}
        rows:
          type: array
          items: {$ref: '#/components/schemas/ProjectCodeDiffRow'}

    ProjectCodeFileChange:
      type: object
      required: [status, binary]
      properties:
        status: {type: string, enum: [added, modified, deleted, renamed, copied, mode_only]}
        old_path: {type: string}
        new_path: {type: string}
        binary: {type: boolean}
        mode_old: {type: string}
        mode_new: {type: string}
        added:
          type: array
          items: {$ref: '#/components/schemas/ProjectCodeLineRange'}
        removed:
          type: array
          items: {$ref: '#/components/schemas/ProjectCodeLineRange'}
        modified:
          type: array
          items: {$ref: '#/components/schemas/ProjectCodeLineRange'}
        hunks:
          type: array
          items: {$ref: '#/components/schemas/ProjectCodeDiffHunk'}

    ProjectCodeFile:
      type: object
      required: [path, status, lines, finding_count, changed_line_count, binary, generated, source_available]
      properties:
        path: {type: string}
        old_path: {type: string}
        status: {type: string, enum: [unchanged, added, modified, deleted, renamed, copied, mode_only]}
        language: {type: string}
        lines: {type: integer, minimum: 0}
        finding_count: {type: integer, minimum: 0}
        changed_line_count: {type: integer, minimum: 0}
        binary: {type: boolean}
        generated: {type: boolean}
        source_available: {type: boolean}
        source_reason: {type: string}

    ProjectCodeFilesResponse:
      type: object
      required: [analysis_id, head, capabilities, files]
      properties:
        analysis_id: {type: string}
        base: {$ref: '#/components/schemas/ProjectCodeRevision'}
        head: {$ref: '#/components/schemas/ProjectCodeRevision'}
        capabilities: {$ref: '#/components/schemas/ProjectCodeHTTPMetadata'}
        files:
          type: array
          items: {$ref: '#/components/schemas/ProjectCodeFile'}

    ProjectCodeFinding:
      type: object
      required: [id, kind, severity, detection_status, location, new]
      properties:
        id: {type: string}
        kind: {type: string, enum: [issue, hotspot]}
        rule_key: {type: string}
        rule_name: {type: string}
        type: {type: string, enum: [bug, vulnerability, code_smell, security_hotspot]}
        severity: {type: string}
        detection_status: {type: string}
        current_status: {type: string, description: Current mutable issue or hotspot triage state}
        message: {type: string}
        new: {type: boolean}
        location:
          type: object
          required: [file, start_line, end_line]
          properties:
            file: {type: string}
            start_line: {type: integer, minimum: 1}
            end_line: {type: integer, minimum: 1}
            start_column: {type: integer, minimum: 0, description: UTF-16 display column}
            end_column: {type: integer, minimum: 0, description: UTF-16 display column, end-exclusive}

    ProjectCodeLine:
      type: object
      required: [number, content, change, duplicated, coverage]
      properties:
        number: {type: integer, minimum: 1}
        content: {type: string}
        change: {type: string, enum: [unchanged, addition]}
        duplicated: {type: boolean}
        coverage: {type: [string, 'null'], enum: [covered, uncovered, partial, null]}

    ProjectCodeFileView:
      type: object
      required: [analysis_id, head, file, from_line, to_line, total_lines, lines, findings]
      properties:
        analysis_id: {type: string}
        base: {$ref: '#/components/schemas/ProjectCodeRevision'}
        head: {$ref: '#/components/schemas/ProjectCodeRevision'}
        file: {$ref: '#/components/schemas/ProjectCodeFile'}
        from_line: {type: integer, minimum: 1}
        to_line: {type: integer, minimum: 0}
        total_lines: {type: integer, minimum: 0}
        lines:
          type: array
          items: {$ref: '#/components/schemas/ProjectCodeLine'}
        findings:
          type: array
          items: {$ref: '#/components/schemas/ProjectCodeFinding'}

    ProjectCodeFileResponse:
      allOf:
      - $ref: '#/components/schemas/ProjectCodeFileView'
      - type: object
        required: [capabilities]
        properties:
          capabilities: {$ref: '#/components/schemas/ProjectCodeHTTPMetadata'}

    ProjectCodeHTTPMetadata:
      type: object
      required: [source, unified_diff, split_diff, line_coverage]
      properties:
        source: {type: boolean}
        unified_diff: {type: boolean}
        split_diff: {type: boolean}
        line_coverage: {type: boolean}

    ProjectCodeDiffView:
      type: object
      required: [analysis_id, head, path, view, context_truncated, change]
      properties:
        analysis_id: {type: string}
        base: {$ref: '#/components/schemas/ProjectCodeRevision'}
        head: {$ref: '#/components/schemas/ProjectCodeRevision'}
        path: {type: string}
        view: {type: string, enum: [unified, split]}
        context_truncated: {type: boolean, description: True when unchanged context outside the requested window was omitted}
        change: {$ref: '#/components/schemas/ProjectCodeFileChange'}
        base_file: {$ref: '#/components/schemas/ProjectCodeSourceFile'}
        source_file: {$ref: '#/components/schemas/ProjectCodeSourceFile'}

    ProjectCodeDiffResponse:
      type: object
      required: [capabilities, diff]
      properties:
        capabilities: {$ref: '#/components/schemas/ProjectCodeCapabilities'}
        diff: {$ref: '#/components/schemas/ProjectCodeDiffView'}

    AupStatus:
      type: object
      properties:
        version: {type: string}
        text: {type: string}
        accepted: {type: boolean}

    ScopeTargetRequest:
      type: object
      properties:
        kind:
          type: string
          enum: [repo, domain, host, url, image, cidr, cloud_account]
        value: {type: string}

    CreateEngagementRequest:
      type: object
      required: [name]
      properties:
        name: {type: string, example: acme-q3-2026}
        client: {type: string, example: Acme Corp}
        in_scope:
          type: array
          items: {$ref: "#/components/schemas/ScopeTargetRequest"}
        out_of_scope:
          type: array
          items: {$ref: "#/components/schemas/ScopeTargetRequest"}
        authorized_from: {type: string, format: date-time}
        authorized_to: {type: string, format: date-time}
        timezone: {type: string, description: IANA timezone name}
        asset_id: {type: string}

    UploadedSourcePackage:
      type: object
      required: [filename, size, sha256, target, uploaded_by, uploaded_at]
      properties:
        filename: {type: string}
        size: {type: integer, format: int64, minimum: 1, maximum: 536870912}
        sha256: {type: string, pattern: '^[0-9a-f]{64}$'}
        target: {type: string, pattern: '^uploaded-source/sha256/[0-9a-f]{64}$'}
        uploaded_by: {type: string}
        uploaded_at: {type: string, format: date-time}

    Engagement:
      type: object
      required: [id, tenant_id, name, client, status, scope, roe, live_recon_enabled, created_at, updated_at]
      properties:
        id: {type: string}
        tenant_id: {type: string}
        findings_count:
          description: >-
            Open findings of every kind by severity, excluding licence records and findings triaged
            false positive or remediated. Present on list responses when the finding store is wired.
          type: object
          properties:
            total: {type: integer}
            critical: {type: integer}
            high: {type: integer}
            medium: {type: integer}
            low: {type: integer}
            info: {type: integer}
        last_scan_date:
          description: >-
            Finish time (start time while running) of the engagement's latest scan job. Present on
            list responses when a scan ran.
          type: string
          format: date-time
        last_scan_status:
          description: Status of the latest scan job (running, succeeded, failed). Present with last_scan_date.
          type: string
        project_id:
          type: string
          description: Present when the engagement is the analysis context of a code-quality Project.
        business_asset_id:
          type: string
          description: Present when a primary business-level Asset is assigned.
        name: {type: string}
        client: {type: string}
        status: {type: string, enum: [draft, active, completed, archived]}
        scope:
          type: object
          required: [in_scope, out_of_scope]
          properties:
            in_scope:
              type: array
              items: {$ref: "#/components/schemas/ScopeTarget"}
            out_of_scope:
              type: array
              items: {$ref: "#/components/schemas/ScopeTarget"}
        roe:
          type: object
          description: Rules of engagement the execution gate enforces alongside scope and the window.
          required: [allowed_tool_classes, blackouts]
          properties:
            allowed_tool_classes:
              type: array
              description: Empty means no restriction.
              items: {type: string}
            blackouts:
              type: array
              items:
                type: object
                required: [from, to]
                properties:
                  from: {type: string, format: date-time}
                  to: {type: string, format: date-time}
        authorized_from:
          type: [string, "null"]
          format: date-time
          description: Null is open on that side of the authorization window.
        authorized_to: {type: [string, "null"], format: date-time}
        timezone:
          type: string
          description: IANA zone the operator entered the window in; display and audit only.
        live_recon_enabled: {type: boolean}
        offensive_roe:
          type: object
          description: Offensive rules of engagement the governance policy requires before emulation / exploitation.
          properties:
            customer_contact: {type: string}
            emergency_contact: {type: string}
            risk_ceiling: {type: string, enum: ["", low, medium, high, prohibited]}
            exclusions_checked: {type: boolean}
        created_at: {type: string, format: date-time}
        updated_at: {type: string, format: date-time}

    ScopeTarget:
      type: object
      required: [kind, value]
      properties:
        kind: {type: string, enum: [domain, ip, cidr, url, repo, image, cloud_account]}
        value: {type: string}

    CreateManualFindingRequest:
      type: object
      required: [title, severity]
      properties:
        title: {type: string}
        description: {type: string}
        severity: {type: string, enum: [critical, high, medium, low, info, unknown]}
        cvss_vector: {type: string}
        cwe: {type: string}
        asset_id:
          type: string
          description: Authoritative asset ID; when supplied it is validated and creates an observed manual attribution.

    AgentProposeFindingInput:
      type: object
      required: [title, description, severity]
      properties:
        title: {type: string}
        description: {type: string}
        severity: {type: string, enum: [critical, high, medium, low, info]}
        cvss_vector: {type: string}
        cwe: {type: string}
        asset_id: {type: string, description: Required and validated when attribution is configured; optional for legacy deployments.}

    AgentProposeHypothesisInput:
      type: object
      required: [title, description, constituent_ids]
      properties:
        title: {type: string}
        description: {type: string}
        constituent_ids: {type: array, minItems: 2, items: {type: string}}
        severity: {type: string, enum: [critical, high, medium, low, info]}
        asset_id: {type: string, description: Explicit authoritative asset ID; otherwise a configured attributor requires one unambiguous shared constituent binding.}

    AgentProposeJudgmentClaimInput:
      type: object
      properties:
        asset_id: {type: string, description: Explicit asset attribution for standalone threat claims or SAST claims; subject-finding SAST claims inherit their binding when attribution is configured.}
      description: Applies to agent proposal claims. Manual, SARIF, exploitation/hypothesis, and judgment proposals never infer an arbitrary asset from prose.

    AttackPathResult:
      type: object
      required: [paths, bounds]
      properties:
        paths:
          type: array
          items: {$ref: "#/components/schemas/AttackPath"}
        bounds: {$ref: "#/components/schemas/AttackPathBounds"}

    AttackPathBounds:
      type: object
      required: [maxLength, maxPaths, maxDuration, truncated, lengthHit, pathsHit, targetPathsHit, findingPathsHit, wallClockHit]
      properties:
        maxLength: {type: integer, minimum: 1}
        maxPaths: {type: integer, minimum: 1}
        maxDuration: {type: integer, description: Go duration serialized as nanoseconds}
        truncated: {type: boolean}
        lengthHit: {type: boolean}
        pathsHit: {type: boolean}
        targetPathsHit: {type: boolean}
        findingPathsHit: {type: boolean}
        wallClockHit: {type: boolean}

    AttackPath:
      type: object
      required: [id, nodes, steps, edges, uncertainties, confident]
      properties:
        id: {type: string, description: Stable SHA-256 identity of the path inputs}
        nodes:
          type: array
          items: {$ref: "#/components/schemas/AttackPathNode"}
        steps:
          type: array
          items: {$ref: "#/components/schemas/AttackPathStep"}
        edges:
          type: array
          items: {$ref: "#/components/schemas/AttackPathLogicalEdge"}
        uncertainties:
          type: array
          items: {$ref: "#/components/schemas/AttackPathUncertainty"}
        confident: {type: boolean}

    AttackPathNode:
      type: object
      description: Exactly one of asset or finding is present.
      properties:
        asset:
          type: object
          required: [asset]
          properties:
            asset: {$ref: "#/components/schemas/AttackPathAsset"}
        finding:
          type: object
          required: [input]
          properties:
            input: {$ref: "#/components/schemas/AttackPathFindingInput"}

    AttackPathAsset:
      type: object
      required: [ID, TenantID, Kind, Key, Name]
      properties:
        ID: {type: string}
        TenantID: {type: string}
        Kind: {type: string, enum: [host, workload, image, cloud_account, storage, exposure, identity, namespace, cluster, repository]}
        Key: {type: string}
        Name: {type: string}
        Attributes: {type: object, additionalProperties: {type: string}}

    AttackPathFindingInput:
      type: object
      required: [target, finding, reachability, tier, provenance, confirmed, external]
      properties:
        target:
          type: object
          required: [ID, Kind]
          properties:
            ID: {type: string}
            Kind: {type: string, enum: [canonical, imported]}
        finding: {$ref: "#/components/schemas/Finding"}
        reachability: {type: string, enum: ["", reachable, not_reachable, unknown]}
        tier: {type: string, enum: ["", tier-0, tier-1, tier-1.5, tier-2]}
        provenance: {type: string}
        confirmed: {type: boolean}
        external: {type: boolean, description: True only for an immutable imported finding; the embedded finding retains only shared risk comparison fields.}
        importedProvenance:
          $ref: "#/components/schemas/AttackPathImportedFindingProvenance"
      description: The embedded finding's KEV, RiskScore, CVSSVector, and Severity are the existing comparison components; no weighted path score is emitted.

    AttackPathImportedFindingProvenance:
      type: object
      required: [ToolName, ToolVersion, RuleID, SourceDigest, IngestedBy, IngestedAt]
      description: Immutable provenance for an external imported finding. Present only when target.Kind is imported.
      properties:
        ToolName: {type: string}
        ToolVersion: {type: string}
        RuleID: {type: string}
        SourceDigest: {type: string}
        IngestedBy: {type: string}
        IngestedAt: {type: string, format: date-time}

    AttackPathLogicalEdge:
      type: object
      required: [from, to, kind, evidence, observed, finding]
      properties:
        from: {type: string}
        to: {type: string}
        toTargetKind: {type: string, enum: [canonical, imported], description: Finding source table when finding is true.}
        kind: {type: string, enum: [runs, exposes, depends_on, can_assume, reaches, affected_by, mounts, member_of]}
        evidence:
          type: array
          items: {$ref: "#/components/schemas/AttackPathEvidence"}
        observed: {type: boolean}
        finding: {type: boolean}

    AttackPathStep:
      type: object
      required: [from, to, kind, evidence, observed, toFinding]
      properties:
        from: {type: string}
        to: {type: string}
        toTargetKind: {type: string, enum: [canonical, imported], description: Finding source table when finding is true.}
        kind: {type: string, enum: [runs, exposes, depends_on, can_assume, reaches, affected_by, mounts, member_of]}
        evidence:
          type: array
          items: {$ref: "#/components/schemas/AttackPathEvidence"}
        observed: {type: boolean}
        toFinding: {type: boolean}

    AttackPathEvidence:
      type: object
      required: [producer, provenance, confidence]
      properties:
        producer: {type: string, description: Binding producer; empty only for base fleet-edge evidence.}
        provenance: {type: string, description: Immutable evidence provenance.}
        confidence: {type: string, enum: [observed, inferred], description: Required producer-supplied evidence confidence; omitted confidence is invalid.}

    AttackPathUncertainty:
      type: string
      description: inferred_edge is suffixed with its one-based step index.
      pattern: '^(inferred_edge:[1-9][0-9]*|missing_reachability|unknown_reachability|unconfirmed_reachability)$'

    AttackPathScoreComponents:
      type: object
      description: Existing flat finding risk components used for path comparison; no weighted path score is emitted.
      properties:
        Severity: {type: string, enum: [critical, high, medium, low, info, unknown]}
        CVSSVector: {type: string}
        KEV: {type: boolean}
        RiskScore: {type: number}

    Finding:
      allOf:
      - $ref: "#/components/schemas/AttackPathScoreComponents"
      - type: object
        description: Serialized from the domain struct (PascalCase keys).
        properties:
          ID: {type: string}
          EngagementID: {type: string}
          Title: {type: string}
          Description: {type: string}
          CWE: {type: string}
          Status: {type: string, enum: [open, triage, confirmed, false_positive, remediated]}
          DedupKey: {type: string}
          EvidenceScore: {type: integer}
          Kind: {type: string, enum: [sca, recon, exploitation, manual, sast, secret, misconfig, cloud_posture, dast, threat, hypothesis, quality, reliability]}
          RuleKey: {type: string}
          SourceLocation:
            type: [object, "null"]
            properties:
              File: {type: string}
              StartLine: {type: integer}
              EndLine: {type: integer}
          Assignee: {type: string}
          Version: {type: integer}
          Sources: {type: array, items: {type: string}}
          Confidence: {type: string}
          Class: {type: string}
          Scope: {type: string}
          Reachability: {type: string}
          Impact: {type: string}
          Priority: {type: integer}
          ClassReachability: {type: string}
          ProposedBy: {type: string}
          Audit:
            type: object
            properties:
              CreatedAt: {type: string, format: date-time}
              UpdatedAt: {type: string, format: date-time}
              CreatedBy: {type: string}
              UpdatedBy: {type: string}

    ScanRequest:
      type: object
      required: [engagement_id]
      properties:
        engagement_id: {type: string}
        target: {type: string, description: Path or git URL; must be empty for kind upload and may be empty when an imported SBOM is active.}
        kind:
          type: string
          enum: [local, git, archive, upload, image]
          default: local
        ref: {type: string, description: optional git branch or tag resolved server-side to an immutable commit}
        mode: {type: string, enum: [full, vulnerabilities, licenses], default: full}
        code_quality: {type: boolean, default: false}

    ScanJob:
      type: object
      description: >
        Progress of one asynchronous scan. Debug events carry only metadata and
        counts: never raw tool output, environment values, or unredacted commands.
      properties:
        id: {type: string}
        engagement_id: {type: string}
        target: {type: string}
        kind: {type: string}
        status:
          type: string
          enum: [running, succeeded, failed]
        stage: {type: string}
        progress: {type: integer, minimum: 0, maximum: 100}
        error: {type: string}
        started_at: {type: string, format: date-time}
        finished_at: {type: string, format: date-time}
        debug_events:
          type: array
          items: {type: object}

    ScanResult:
      type: object
      properties:
        target: {type: string}
        languages:
          type: array
          items: {type: object}
        sbom:
          type: object
          description: SBOM with Components and Dependencies (PascalCase keys).
        vulnerabilities:
          type: array
          items: {type: object}
        licenses:
          type: array
          items: {type: object}
        findings:
          type: array
          items: {$ref: "#/components/schemas/Finding"}
        tool_versions:
          type: object
          additionalProperties: {type: string}
        vuln_db_snapshot: {type: string}

    ProjectDependencyGraph:
      type: object
      required: [analysis_id, roots, nodes, edges, summary]
      properties:
        analysis_id: {type: string, minLength: 1}
        roots:
          type: array
          items: {type: string, minLength: 1}
        nodes:
          type: array
          maxItems: 50000
          items: {$ref: "#/components/schemas/ProjectDependencyNode"}
        edges:
          type: array
          maxItems: 250000
          items: {$ref: "#/components/schemas/ProjectDependencyEdge"}
        summary: {$ref: "#/components/schemas/ProjectDependencySummary"}

    ProjectDependencyNode:
      type: object
      required: [id, name, version, scope, direct, depth, licenses, license_risk, license_verdict, vulnerabilities, vulnerability_count]
      properties:
        id: {type: string, minLength: 1}
        name: {type: string}
        version: {type: string}
        purl: {type: string}
        scope: {type: string}
        reachability: {type: string}
        direct: {type: boolean}
        depth:
          type: integer
          minimum: -1
          description: Shortest distance from a graph root; -1 means cyclic or unrooted.
        licenses:
          type: array
          items: {$ref: "#/components/schemas/ProjectDependencyLicense"}
        license_risk: {type: boolean}
        license_verdict: {type: string, enum: ["", allow, warn, deny]}
        vulnerabilities:
          type: array
          items: {$ref: "#/components/schemas/ProjectDependencyVulnerability"}
        vulnerability_count: {type: integer, minimum: 0}
        worst_severity: {type: string, enum: [critical, high, medium, low, info, unknown]}

    ProjectDependencyLicense:
      type: object
      required: [id, name, category]
      properties:
        id: {type: string}
        name: {type: string}
        category: {type: string, enum: [permissive, weak-copyleft, copyleft, proprietary, unknown]}

    ProjectDependencyVulnerability:
      type: object
      required: [id, source, severity]
      properties:
        id: {type: string}
        source: {type: string}
        severity: {type: string, enum: [critical, high, medium, low, info, unknown]}
        fixed_version: {type: string}

    ProjectDependencyEdge:
      type: object
      required: [from, to]
      properties:
        from: {type: string, minLength: 1}
        to: {type: string, minLength: 1}

    ProjectDependencySummary:
      type: object
      required: [components, direct, transitive, vulnerable, license_risk, edges]
      properties:
        components: {type: integer, minimum: 0}
        direct: {type: integer, minimum: 0}
        transitive: {type: integer, minimum: 0}
        vulnerable: {type: integer, minimum: 0}
        license_risk: {type: integer, minimum: 0}
        edges: {type: integer, minimum: 0}

    ProjectOverviewState:
      type: string
      enum: [not_analyzed, analyzed]

    ProjectOverviewMetricAvailability:
      type: string
      enum: [available, unavailable, not_supplied, not_applicable]

    ProjectOverviewUnavailableReason:
      type: string
      enum:
      - no_analysis
      - rating_not_available
      - issue_lifecycle_not_available
      - security_hotspots_not_available
      - changed_line_metrics_not_available
      - coverage_not_supplied
      - no_executable_lines
      - duplication_not_available

    ProjectOverviewGrade:
      type: string
      enum: [A, B, C, D, E]

    ProjectOverviewGateStatus:
      type: string
      enum: [passed, failed, incomplete]

    ProjectOverviewGateOperator:
      type: string
      enum: ["<=", ">=", "==", "<", ">"]

    ProjectOverviewGateSource:
      type: string
      description: >
        Supported immutable gate policy sources currently produced by the
        application. Legacy empty source values are represented as null.
      enum: [default, repository, managed]

    ProjectOverviewGateMetric:
      type: string
      enum:
      - new_critical
      - new_high
      - new_medium
      - new_secret
      - new_vulnerability
      - new_issues
      - total_critical
      - duplication_density
      - coverage
      - security_rating
      - reliability_rating
      - maintainability_rating

      - security_hotspots_reviewed
      - new_security_hotspots_reviewed
    ProjectBranches:
      type: object
      required: [branches]
      properties:
        branches:
          type: array
          description: Distinct branch values recorded across the Project's analyses, sorted.
          items: {type: string}
      example:
        branches: [feature/x, main]

    ProjectOverview:
      oneOf:
      - $ref: "#/components/schemas/ProjectOverviewNotAnalyzed"
      - $ref: "#/components/schemas/ProjectOverviewAnalyzed"

    ProjectOverviewNotAnalyzed:
      type: object
      required: [state, project, latest_analysis, gate, issue_summary, lenses]
      properties:
        state:
          type: string
          enum: [not_analyzed]
        project: {$ref: "#/components/schemas/ProjectOverviewProject"}
        latest_analysis:
          type: "null"
        gate:
          type: "null"
        issue_summary: {$ref: "#/components/schemas/ProjectOverviewIssueSummary"}
        lenses: {$ref: "#/components/schemas/ProjectOverviewLenses"}

    ProjectOverviewAnalyzed:
      type: object
      required: [state, project, latest_analysis, gate, issue_summary, lenses]
      properties:
        state:
          type: string
          enum: [analyzed]
        project: {$ref: "#/components/schemas/ProjectOverviewProject"}
        latest_analysis: {$ref: "#/components/schemas/ProjectOverviewAnalysis"}
        gate: {$ref: "#/components/schemas/ProjectOverviewGate"}
        issue_summary: {$ref: "#/components/schemas/ProjectOverviewIssueSummary"}
        lenses: {$ref: "#/components/schemas/ProjectOverviewLenses"}

    ProjectOverviewProject:
      type: object
      required: [key, name]
      properties:
        key: {type: string, minLength: 1}
        name: {type: string, minLength: 1}

    ProjectOverviewAnalysis:
      type: object
      required: [id, created_at, source_ref, source_commit, new_code]
      properties:
        id: {type: string, minLength: 1}
        created_at: {type: string, format: date-time}
        source_ref: {type: string}
        source_commit: {type: string}
        new_code: {$ref: "#/components/schemas/ProjectOverviewNewCodePeriod"}

    ProjectOverviewNewCodePeriod:
      oneOf:
      - type: object
        required: [first_analysis, has_baseline, baseline_analysis_id]
        properties:
          first_analysis:
            type: boolean
            const: true
          has_baseline:
            type: boolean
            const: false
          baseline_analysis_id:
            type: "null"
      - type: object
        required: [first_analysis, has_baseline, baseline_analysis_id]
        properties:
          first_analysis:
            type: boolean
            const: false
          has_baseline:
            type: boolean
            const: true
          baseline_analysis_id:
            type: string
            minLength: 1

    ProjectOverviewGate:
      type: object
      required: [status, key, name, source, failed_conditions]
      properties:
        status: {$ref: "#/components/schemas/ProjectOverviewGateStatus"}
        key:
          type: [string, "null"]
        name:
          type: [string, "null"]
        source:
          anyOf:
          - $ref: "#/components/schemas/ProjectOverviewGateSource"
          - type: "null"
        failed_conditions:
          type: array
          items: {$ref: "#/components/schemas/ProjectOverviewGateCondition"}

    ProjectOverviewGateCondition:
      type: object
      required: [metric, operator, threshold, actual]
      properties:
        metric: {$ref: "#/components/schemas/ProjectOverviewGateMetric"}
        operator: {$ref: "#/components/schemas/ProjectOverviewGateOperator"}
        threshold: {type: number}
        actual: {type: number}

    ProjectOverviewIssueSummary:
      type: object
      required: [new_code_total, accepted_overall_total]
      properties:
        new_code_total:
          description: Total findings newly introduced in the immutable New Code snapshot.
          $ref: "#/components/schemas/ProjectOverviewCountMetric"
        accepted_overall_total:
          description: Total accepted findings across the whole project. Unavailable
            until issue lifecycle data lands.
          $ref: "#/components/schemas/ProjectOverviewCountMetric"

    ProjectOverviewLenses:
      type: object
      required: [overall, new_code]
      properties:
        overall: {$ref: "#/components/schemas/ProjectOverviewLens"}
        new_code: {$ref: "#/components/schemas/ProjectOverviewLens"}

    ProjectOverviewLens:
      type: object
      required: [security, reliability, maintainability, security_hotspots_reviewed,
        coverage, duplications]
      properties:
        security: {$ref: "#/components/schemas/ProjectOverviewRatingMetric"}
        reliability: {$ref: "#/components/schemas/ProjectOverviewRatingMetric"}
        maintainability: {$ref: "#/components/schemas/ProjectOverviewRatingMetric"}
        security_hotspots_reviewed: {$ref: "#/components/schemas/ProjectOverviewPercentageMetric"}
        coverage: {$ref: "#/components/schemas/ProjectOverviewPercentageMetric"}
        duplications: {$ref: "#/components/schemas/ProjectOverviewPercentageMetric"}

    ProjectOverviewRatingMetric:
      type: object
      required: [availability, grade, unavailable_reason]
      properties:
        availability: {$ref: "#/components/schemas/ProjectOverviewMetricAvailability"}
        grade:
          anyOf:
          - $ref: "#/components/schemas/ProjectOverviewGrade"
          - type: "null"
        unavailable_reason:
          anyOf:
          - $ref: "#/components/schemas/ProjectOverviewUnavailableReason"
          - type: "null"

    ProjectOverviewPercentageMetric:
      type: object
      required: [availability, value, unavailable_reason]
      properties:
        availability: {$ref: "#/components/schemas/ProjectOverviewMetricAvailability"}
        value:
          type: [number, "null"]
          minimum: 0
          maximum: 100
        unavailable_reason:
          anyOf:
          - $ref: "#/components/schemas/ProjectOverviewUnavailableReason"
          - type: "null"

        grade:
          type: string
    ProjectOverviewCountMetric:
      type: object
      required: [availability, value, unavailable_reason]
      properties:
        availability: {$ref: "#/components/schemas/ProjectOverviewMetricAvailability"}
        value:
          type: [integer, "null"]
          minimum: 0
        unavailable_reason:
          anyOf:
          - $ref: "#/components/schemas/ProjectOverviewUnavailableReason"
          - type: "null"

    ProjectHotspotStatus:
      type: string
      enum: [to_review, acknowledged, fixed, safe]

    ProjectHotspotSeverity:
      type: string
      enum: [unknown, info, low, medium, high, critical]

    ProjectHotspot:
      type: object
      required:
      - id
      - rule_key
      - rule_name
      - title
      - description
      - severity
      - finding_kind
      - cwe
      - location
      - status
      - version
      - first_seen_analysis_id
      - last_seen_analysis_id
      - first_seen_at
      - last_seen_at
      properties:
        id: {type: string}
        rule_key: {type: string}
        rule_name: {type: string}
        title: {type: string}
        description: {type: string}
        severity: {$ref: "#/components/schemas/ProjectHotspotSeverity"}
        finding_kind: {type: string}
        cwe: {type: string}
        location: {type: string}
        status: {$ref: "#/components/schemas/ProjectHotspotStatus"}
        version: {type: integer, minimum: 1}
        first_seen_analysis_id: {type: string}
        last_seen_analysis_id: {type: string}
        first_seen_at: {type: string, format: date-time}
        last_seen_at: {type: string, format: date-time}

    ProjectHotspotCursor:
      type: object
      required: [before_last_seen_at, before_id]
      properties:
        before_last_seen_at: {type: string, format: date-time}
        before_id: {type: string}

    ProjectHotspotFacets:
      type: object
      required: [statuses, rule_keys, severities]
      properties:
        statuses: {type: object, additionalProperties: {type: integer, minimum: 0}}
        rule_keys: {type: object, additionalProperties: {type: integer, minimum: 0}}
        severities: {type: object, additionalProperties: {type: integer, minimum: 0}}

    ProjectHotspotPage:
      type: object
      required:
      - items
      - next
      - facets
      - summary
      properties:
        items:
          type: array
          items: {$ref: "#/components/schemas/ProjectHotspot"}
        next:
          anyOf:
          - $ref: "#/components/schemas/ProjectHotspotCursor"
          - type: "null"
        facets: {$ref: "#/components/schemas/ProjectHotspotFacets"}
        summary:
          $ref: '#/components/schemas/ProjectHotspotSummary'

    RuleType:
      type: string
      enum:
      - bug
      - vulnerability
      - code_smell
      - security_hotspot

    RuleQuality:
      type: string
      enum:
      - security
      - reliability
      - maintainability

    RuleSeverity:
      type: string
      enum:
      - low
      - medium
      - high
      - critical

    RuleDetection:
      type: string
      enum:
      - ast
      - pattern
      - parse
      - metric

    RuleSummary:
      type: object
      required:
      - key
      - name
      - language
      - type
      - qualities
      - default_severity
      - tags
      - cwe
      - owasp
      - description
      - remediation_effort
      - detection
      properties:
        key: {type: string}
        name: {type: string}
        language: {type: string}
        type: {$ref: "#/components/schemas/RuleType"}
        qualities:
          type: array
          items: {$ref: "#/components/schemas/RuleQuality"}
        default_severity: {$ref: "#/components/schemas/RuleSeverity"}
        tags:
          type: array
          items: {type: string}
        cwe:
          type: array
          items: {type: string}
        owasp:
          type: array
          items: {type: string}
        description: {type: string}
        remediation_effort: {type: integer}
        detection: {$ref: "#/components/schemas/RuleDetection"}

    RuleDetail:
      type: object
      required:
      - key
      - name
      - language
      - type
      - qualities
      - default_severity
      - tags
      - cwe
      - owasp
      - description
      - rationale
      - remediation
      - compliant_example
      - noncompliant_example
      - remediation_effort
      - detection
      properties:
        key: {type: string}
        name: {type: string}
        language: {type: string}
        type: {$ref: "#/components/schemas/RuleType"}
        qualities:
          type: array
          items: {$ref: "#/components/schemas/RuleQuality"}
        default_severity: {$ref: "#/components/schemas/RuleSeverity"}
        tags:
          type: array
          items: {type: string}
        cwe:
          type: array
          items: {type: string}
        owasp:
          type: array
          items: {type: string}
        description: {type: string}
        rationale: {type: string}
        remediation: {type: string}
        compliant_example: {type: string}
        noncompliant_example: {type: string}
        remediation_effort: {type: integer}
        detection: {$ref: "#/components/schemas/RuleDetection"}
    ProjectHotspotTransitionRequest:
      type: object
      properties:
        to:
          $ref: '#/components/schemas/ProjectHotspotStatus'
        rationale:
          type: string
          minLength: 3
          maxLength: 4000
        expected_version:
          type: integer
          minimum: 1
      required:
      - to
      - rationale
      - expected_version
    ProjectHotspotTransitionResponse:
      type: object
      properties:
        hotspot:
          $ref: '#/components/schemas/ProjectHotspot'
        event:
          $ref: '#/components/schemas/ProjectHotspotReviewEvent'
      required:
      - hotspot
      - event
    ProjectHotspotReviewEvent:
      type: object
      properties:
        id:
          type: string
        from:
          type: string
        to:
          type: string
        actor:
          type: string
        rationale:
          type: string
        previous_version:
          type: integer
        version:
          type: integer
        created_at:
          type: string
          format: date-time
      required:
      - id
      - from
      - to
      - actor
      - rationale
      - previous_version
      - version
      - created_at
    ProjectHotspotSummary:
      type: object
      properties:
        total:
          type: integer
        reviewed:
          type: integer
        reviewed_pct:
          type: number
        grade:
          type: string
      required:
      - total
      - reviewed
      - reviewed_pct
      - grade


    MeasureAvailabilityState:
      type: string
      enum: [available, unavailable, not_applicable]

    MeasureUnavailableReason:
      type: string
      enum:
        - legacy_analysis
        - function_count_not_available
        - no_commentable_lines
        - complexity_not_available
        - coverage_not_supplied
        - no_executable_lines
        - changed_line_coverage_not_available
        - duplication_not_available
        - issue_type_incomplete
        - issue_attribution_incomplete
        - technical_debt_incomplete
        - rating_not_available

    MeasureCountMetric:
      type: object
      properties:
        availability: {$ref: '#/components/schemas/MeasureAvailabilityState'}
        value: {type: [integer, "null"]}
        unavailable_reason: {anyOf: [{$ref: '#/components/schemas/MeasureUnavailableReason'}, {type: 'null'}]}
      required: [availability, value, unavailable_reason]

    MeasureDecimalMetric:
      type: object
      properties:
        availability: {$ref: '#/components/schemas/MeasureAvailabilityState'}
        value: {type: [number, "null"], format: float}
        unavailable_reason: {anyOf: [{$ref: '#/components/schemas/MeasureUnavailableReason'}, {type: 'null'}]}
      required: [availability, value, unavailable_reason]

    MeasureGradeMetric:
      type: object
      required: [availability, grade, unavailable_reason]
      properties:
        availability:
          $ref: '#/components/schemas/MeasureAvailabilityState'
        grade:
          anyOf:
            - type: string
              enum: [A, B, C, D, E]
            - type: "null"
        unavailable_reason:
          anyOf:
            - $ref: '#/components/schemas/MeasureUnavailableReason'
            - type: "null"

    MeasureSize:
      type: object
      required: [files, ncloc, comment_lines, blank_lines, functions, comment_density]
      properties:
        files: {$ref: '#/components/schemas/MeasureCountMetric'}
        ncloc: {$ref: '#/components/schemas/MeasureCountMetric'}
        comment_lines: {$ref: '#/components/schemas/MeasureCountMetric'}
        blank_lines: {$ref: '#/components/schemas/MeasureCountMetric'}
        functions: {$ref: '#/components/schemas/MeasureCountMetric'}
        comment_density: {$ref: '#/components/schemas/MeasureDecimalMetric'}

    MeasureComplexity:
      type: object
      required: [cyclomatic, cognitive]
      properties:
        cyclomatic: {$ref: '#/components/schemas/MeasureCountMetric'}
        cognitive: {$ref: '#/components/schemas/MeasureCountMetric'}

    MeasureCoverage:
      type: object
      required: [covered_lines, coverable_lines, coverage, new_code_coverage]
      properties:
        covered_lines: {$ref: '#/components/schemas/MeasureCountMetric'}
        coverable_lines: {$ref: '#/components/schemas/MeasureCountMetric'}
        coverage: {$ref: '#/components/schemas/MeasureDecimalMetric'}
        new_code_coverage: {$ref: '#/components/schemas/MeasureDecimalMetric'}

    MeasureDuplication:
      type: object
      required: [duplicated_lines, duplication_blocks, duplication_density]
      properties:
        duplicated_lines: {$ref: '#/components/schemas/MeasureCountMetric'}
        duplication_blocks: {$ref: '#/components/schemas/MeasureCountMetric'}
        duplication_density: {$ref: '#/components/schemas/MeasureDecimalMetric'}

    MeasureIssues:
      type: object
      required: [by_type, by_severity]
      properties:
        by_type:
          type: object
          required: [code_smell, bug, vulnerability, security_hotspot]
          properties:
            code_smell: {$ref: '#/components/schemas/MeasureCountMetric'}
            bug: {$ref: '#/components/schemas/MeasureCountMetric'}
            vulnerability: {$ref: '#/components/schemas/MeasureCountMetric'}
            security_hotspot: {$ref: '#/components/schemas/MeasureCountMetric'}
        by_severity:
          type: object
          required: [info, low, medium, high, critical]
          properties:
            info: {$ref: '#/components/schemas/MeasureCountMetric'}
            low: {$ref: '#/components/schemas/MeasureCountMetric'}
            medium: {$ref: '#/components/schemas/MeasureCountMetric'}
            high: {$ref: '#/components/schemas/MeasureCountMetric'}
            critical: {$ref: '#/components/schemas/MeasureCountMetric'}

    MeasureDebt:
      type: object
      required: [remediation_effort_minutes]
      properties:
        remediation_effort_minutes: {$ref: '#/components/schemas/MeasureCountMetric'}

    MeasureRatings:
      type: object
      required: [security, reliability, maintainability]
      properties:
        security: {$ref: '#/components/schemas/MeasureGradeMetric'}
        reliability: {$ref: '#/components/schemas/MeasureGradeMetric'}
        maintainability: {$ref: '#/components/schemas/MeasureGradeMetric'}

    AnalysisMetadata:
      type: object
      properties:
        id: {type: string}
        created_at: {type: string, format: date-time}
        source_ref: {type: string}
        source_commit: {type: string}
      required: [id, created_at]

    MeasureNode:
      type: object
      properties:
        path: {type: string}
        name: {type: string}
        kind: {type: string, enum: [project, directory, file]}
        language: {type: string}
        size: {$ref: '#/components/schemas/MeasureSize'}
        complexity: {$ref: '#/components/schemas/MeasureComplexity'}
        coverage: {$ref: '#/components/schemas/MeasureCoverage'}
        duplication: {$ref: '#/components/schemas/MeasureDuplication'}
        issues: {$ref: '#/components/schemas/MeasureIssues'}
        debt: {$ref: '#/components/schemas/MeasureDebt'}
        ratings: {$ref: '#/components/schemas/MeasureRatings'}
      required:
        - path
        - name
        - kind

    ChildCollection:
      type: object
      properties:
        items:
          type: array
          items: {$ref: '#/components/schemas/MeasureNode'}
        next_cursor: {type: [string, "null"]}
      required: [items, next_cursor]

    ProjectMetadata:
      type: object
      required: [key, name]
      properties:
        key: {type: string}
        name: {type: string}

    ProjectMeasureResponse:
      oneOf:
      - $ref: '#/components/schemas/ProjectMeasureAnalyzedResponse'
      - $ref: '#/components/schemas/ProjectMeasureNotAnalyzedResponse'

    ProjectMeasureAnalyzedResponse:
      type: object
      properties:
        state: {type: string, enum: [analyzed]}
        project: {$ref: '#/components/schemas/ProjectMetadata'}
        analysis: {$ref: '#/components/schemas/AnalysisMetadata'}
        path: {type: string}
        included_domains:
          type: array
          items: {type: string, enum: [size, complexity, coverage, duplication, issues, debt, ratings]}
        node: {$ref: '#/components/schemas/MeasureNode'}
        children: {$ref: '#/components/schemas/ChildCollection'}
      required:
        - state
        - project
        - analysis
        - path
        - included_domains
        - node
        - children

    ProjectMeasureNotAnalyzedResponse:
      type: object
      properties:
        state: {type: string, enum: [not_analyzed]}
        project: {$ref: '#/components/schemas/ProjectMetadata'}
        analysis: {type: 'null'}
        path: {type: string}
        included_domains:
          type: array
          items: {type: string, enum: [size, complexity, coverage, duplication, issues, debt, ratings]}
        node: {type: 'null'}
        children: {$ref: '#/components/schemas/ChildCollection'}
      required:
        - state
        - project
        - analysis
        - path
        - included_domains
        - node
        - children
